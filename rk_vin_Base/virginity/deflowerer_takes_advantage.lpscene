WHAT: all
WHERE: all
WHEN: 21 - 5
WHO: Actor = getPerson(true); If Actor.isDeflowerer() && Actor:perversion > 70 && Actor:masochist < -30 && Actor:attractiontoplayer > 10 && !Actor.isDating()
OTHER: !isWithCompanion()

    SceneStart()
    
        SecondScreen(Actor)
        Actor.dress()
        Actor.show()
    
        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 50
    
            Actor(Excited):: "I'm horny. Come over. I want to fuck you!"
            Actor(Aggressive):: "What's taking you so long? Don't pretend like you don't want it."
            Actor(Flirty):: "I know you fantasize about me every day. I'm your first after all."
            "What kind of texts are these?  <Actor.He_or_She> is becoming bolder and bolder, but since <Actor.he_or_she> hasn't mentioned about actual romance once, am I really okay with letting <Actor.him_or_her> use me as a booty call that's always available."
            0:: "Come over and let <Actor.him_or_her> fuck me"
            1:: "Retain some dignity."
    
            If 0
        
                masochist += 1
                Dest = Actor.getBuilding(home)
                moveTo(Dest)
                Sex(Actor, Player)
                passTime(1, 1.50)
            
            Else
        
                "I should just admit to myself that I made a mistake and give my virginity to an <Actor.asshole_or_bitch>. I should forget about <Actor.him_or_her> and not let myself be taken advantage of any longer."
                Player(Angry)::"Go fuck yourself! Who you think you are? My master... in your dream!"
                masochist -= 1
                Actor:attractiontoplayer -= 1
                Actor:rapportwithplayer -= 1
            
            EndIf
        
        ElseIf Actor.isInterestedIn(Player) && Actor:arousal > 25
    
            Actor(Flirty)::"I'm so horny <Player.name>. Do you want me to show you new sex positions?"
            "What should I say?"
            0::"Yes"
            1::"No"
        
            If 0
        
                Actor(Flirty)::"All the way baby, I'm horny as you!"
                "We undress right away and start to caress each other sensually"
                Sex(Player, Actor)
                Actor:attractiontoplayer += 5
                Actor:rapportwithplayer += 5
                passTime(1, 2.50)
            
            EndIf
        
        ElseIf Actor.isInterestedIn(Player)
        
            Actor(Flirty)::"I'm so horny <Player.name>. Do you want me to show you new sex positions?"
            Player(Sad)::"Not now, I'm not horny as you!"
            Actor(Serene)::"Don't be sad, I'll wait!"
        
        Else
        
            Actor(Flirty)::"I'm so horny <Player.name>. Do you want me to show you new sex positions?"
            Player(Sad)::"I'm not interested anymore, you gotta give me more attention if you want sex from me!"
            Actor(Sad)::"Oh well, we'll see!"
            Actor:attractiontoplayer -= 1
            Actor:rapportwithplayer -= 1
            
        EndIf
    
    SceneEnd()
    timeout(100, deflowerer_takes_advantage)