WHAT: all
WHERE: home
WHEN: 8 - 21
WHO: Actor = getSpecific(Dating); If Actor:age > age - 10 + likes_older*0.5 && Actor:age < age + 10 + likes_older*0.5
OTHER:


    // If you're already in an age-gap relationship, your SO may introduce you to a sibling / cousin / one of their close friends who's similar age as your SO and you may find hot as well. You can either ask for your SO's permission to bang their friend / relative (may lead to threesome) or trick them to leave you alone with this person and cheat

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "I was just in my bedroom when I heard the sound of my <Actor.boyfriend_or_girlfriend> coming home and asking for me."
        secondScreen(Actor)
        Actor(Happy):: "Honey, can you come here for a moment please? I want to introduce you to someone."
        "I wonder who would be so important ..."
        secondScreen()
        setBackgroundCustom(livingroom)
        Actor.dress()
        Actor.show()
        
        Actor2 = Actor.generatePersonTemporaryMatchRace()
        While !Actor.isSameGender(Actor2)
            Actor2 = Actor.generatePersonTemporaryMatchRace()
        Endwhile
        Actor2.makeInterested(Player)
        
        Actor2:age => Actor:age
        Actor2:age += Random(-3, 3)
        If Actor2:age < 18
            Actor2:age => 18
        Endif
        Actor2.randomizeFace()
        Actor2.randomizeHairs()
        Actor2:likes_older => 2*[age - Actor2:age]
        Actor2:attractiontoplayer += Random(25, 50)
        
        Actor2.dress()
        Actor2.show()
        
        If Random(0, 100) < 33
            Actor2.makePermanent()
            rela = "cousin"
            addNpcRelationship(Cousins, Actor, Actor2)
        Elseif Random(0, 100) < 50
            Actor2.makePermanent()
            If Actor2.isMale()
                rela = "brother"
            Else
                rela = "sister"
            Endif
            addNpcRelationship(Siblings, Actor, Actor2)
        Else
            rela = "friend"
        Endif
        
        Actor(Happy):: "This is my <rela>, <Actor2.name>."
        Player(Happy):: "Nice to meet you, <Actor2.name>."
        "It should have been a straightforward enough introduction between a <Player.boyfriend_or_girlfriend> and <Player.his_or_her> <Actor.boyfriend_or_girlfriend>'s new <rela>."
        "However, just as our eyes meet, I couldn't help but feel some mutual sexual attraction right away."
        "It's no surprise, after all I'm dating <Actor.name> partly because I have always been attracted to <Actor2.men_or_woman> <Actor.his_or_her> age, and being as close as they are, it's hardly a shock that <Actor.name> and <Actor.his_or_her> <rela> would have similar tastes in <Player.men_or_women> ..."
        "Of course, this isn't just any random <Actor2.guy_or_girl> that I could approach and flirt with. I'm not single anymore and it's my <Actor.boyfriend_or_girlfriend>'s <rela> after all ... and <Actor.name> is even right there ... unless ..."
        0:: "Ask for <Actor.name>'s permission to bang <Actor.his_or_her> <rela>"
        1:: "Trick my <Actor.boyfriend_or_girlfriend> to leave me alone with their <Actor2.name>"
        99:: "Leave them to it"
        
        If 0
            Player.moveToPersonStand(Actor, 100)
            "I took my <Actor.boyfriend_or_girlfriend> to one side and admitted that I found <Actor.his_or_her> <rela> hot as well and tried to convince <Actor.him_or_her> to let me fuck <Actor2.name>."
            If OpenRelationship() || Actor:rapportwithplayer > 50 || Actor:perversion > 60 || Actor:masochist > 25
                Actor(Happy):: "Of course, baby. I would be a hypocrite if I turn you down. After all, it's hardly a secret that you find <Actor.men_or_women> my age attractive ..."
                Actor.moveToPersonStand(Actor2, 100)
                "<Actor.name> then took <Actor.his_or_her> <rela> aside and whispered into <Actor2.name>'s ear our naughty idea."
                Actor.hide()
                Actor2.hide()
                "While they are talking, I go back to my room, undress and wait for my present..."
                If Actor2:arousal > 15 && Actor2:perversion > 50
                    Actor2.show()
                    "<Actor2.He_or_She> was surprised at first, but quickly accepted the offer.."
                    setBackground(bedroom)
                    Actor2(Surprised)::"Oh hi! Ok! You were waiting for me! Nice!"
                    Player(Flirty)::"Come lay down next to me beautiful!"
                    Actor2.strip()
                    Actor2(Flirty)::"Of course, I'm coming!"
                    Sex(Player, Actor2)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    If Random(50, 100) < Actor:masochist
                        Actor.moveToPersonStand(Player, 100)
                        Actor.show()
                        Actor.strip()
                        "<Actor.name> watched the whole thing and masturbated when <Actor.he_or_she> saw us do it. This must be a long time fantasy of <Actor.his_or_hers>."
                        Filter(Solo)
                        Sex(Actor)
                        Actor:arousal += Random(1, 5)
                    EndIf
                    If Actor:arousal > 25
                        "Soon, <Actor.name> joined us as well for a threesome..."
                        Actor.strip()
                        If Actor2.isInterestedIn(Actor)
                            "... and we were all happy to have a threesome ..."
                            "I'm always insanely turned on by <Actor2.men_or_women> around that age, to have two at the same time, it's really my dream come true."
                            Sex(Player, Actor, Actor2)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                        Else
                            Actor2(Shocked):: "Wait? No way! The agreement did not include a threesome...especially with you!"
                            Actor2(Shocked):: "No offense my dear friend, but right now I think I'll go home! Bye!"
                            Actor2 -= 10
                            Actor2.hide()
                        EndIf
                    Endif
                Else
                    Actor2(Sad):: "I'll remember your offer but right now I'm not in the mood to fuck!"
                Endif
            Else
                Actor(Shocked):: "What? No way! I'm not letting you fuck my <rela>! Do you take me for a cuck or something?"
            Endif
        Elseif 1
            theEnd = true
            "They were going to have a movie night so I insisted on making some snacks and joining them."
            "I then told <Actor.name> that we ran out of pizza rolls and asked <Actor.him_or_her> to get some from the supermarket."
            Actor(Annoyed):: "Is that really necessary? Don't we have other snacks ... It would take me a while to get there and back."
            Actor2(Happy):: "Actually, pizza rolls sound great. I don't mind waiting ..."
            "Could it be that <Actor2.name> realized that I was trying to get <Actor.name> out of the house and went in on my plot as well ..."
            If Actor2:arousal > 25 && Actor2:perversion > 40
                Actor(Happy):: "Okay, fine ... I'll be back. Don't start the movie without me ..."
                Actor2:arousal += Random(1, 3)
            Else
                Actor(Happy):: "Okay, fine ... I'll be back. Don't start the movie without me ..."
                Actor2(Happy):: "I can come with you if you want, I got a car ..."
                Actor(Happy):: "Ok, cool ..."
                "Well <Actor2.he_or_she> just scrapped my plan!"
                theEnd = false
            Endif
            If theEnd
                Actor.hide()
                Player.moveToPersonStand(Actor2, 100)
                Player(Happy):: "Let me get you some drinks at least ... It's only right to treat my <Actor.boyfriend_or_girlfriend>'s <rela> well on <Actor.his_or_her> behalf."
                Actor2(Happy):: "That'll be great. Thank you!"
                Actor2.animate(drinkglass)
                Player.animate(drinkglass)
                "We had some drinks and chatted with each other. As expected, there was definitely some instant spark just like how I felt when I first met <Actor.name>, we actually had a very nice conversation and got along just fine. We enjoyed each other's company very much ..."
                "Furthermore, it was undeniable that there was some chemistry between us, sexual tension even ... and at times, <Actor2.name> wasn't shy to flirt with me ..."
                "But am I really going to seduce my <Actor.boyfriend_or_girlfriend>'s <rela>? We're alone and my bedroom is even right there, there's probably only one way this is going to end if I decide to escalate things ..."
                0:: "Seduce <Actor2.name>"
                1:: "Just exchange numbers for another day"
                2:: "Put <Actor2.him_or_her> off"
                
                If 0
                    continuS = false
                    "I made clear to <Actor2.name> that I had a thing for <Actor2.men_or_women> <Actor2.his_or_her> age and deliberately tricked my <Actor.boyfriend_or_girlfriend> so that I could spend time alone with <Actor2.him_or_her>."
                    "To my pleasant surprise, <Actor2.name> also admitted that <Actor2.he_or_she> found <Actor2.himself_or_herself> instantly attracted to me the moment <Actor.name> first showed <Actor2.him_or_her> my photo and that I was the reason <Actor2.he_or_she> insisted on this movie night with <Actor.name>."
                    animatePair(Player, Actor2, Kissing)
                    If Actor2:arousal > 25 && Actor2:perversion > 40
                        "With those admissions out of the way, one thing led to another and soon..."
                        "What I/we should do?"
                        0::"Offer oral sex"
                        1::"Ask for oral sex"
                        2::"Offer a handjob"
                        3::"Ask for handjob"
                        4::"Make out"
                        5::"Do nothing"
                        
                        If 0
                            offer1 = true
                            continuS = true
                        ElseIf 1
                            offer2 = true
                            continuS = true
                        ElseIf 2
                            offer3 = true
                            continuS = true
                        ElseIf 3
                            offer4 = true
                            continuS = true
                        ElseIf 4
                            offer5 = true
                            continuS = true
                        Endif
                    
                        If continuS
                            "Which room is the best?"
                            0::"My room"
                            1::"Livingroom"
                            2::"Kitchen"
                            3::"Hallway"
                            4::"Random bedroom"
                            5::"Bathroom"
                            
                            If 0
                                setBackground(home)
                                "And not long after, we ended up in my bedroom."
                                StillConti = true
                            ElseIf 1
                                setBackgroundCustom(livingroom)
                                "And not long after, we ended up in the livingroom."
                                StillConti = true
                            ElseIf 2
                                setBackgroundCustom(kitchen)
                                "And not long after, we ended up in the kitchen."
                                StillConti = true
                            ElseIf 3
                                setBackgroundCustom(hallway)
                                "And not long after, we ended up in the hallway."
                                StillConti = true
                            ElseIf 4
                                setBackground(bedroom)
                                "And not long after, we ended up in the guest room."
                                StillConti = true
                            ElseIf 5
                                setBackground3D(Modules/rk_vin_Base/World/toilets___vin01.lpworld)
                                "And not long after, we ended up in the bathroom."
                                StillConti = true
                            EndIf
                        EndIf
                        If StillConti
                            GetCaught = false
                            WeAloneD = true
                            FactorR = 0
                            While WeAloneD && FactorR < 7
                                fuckT = Random(0, 10)
                                If offer1 
                                    Actor2.strip()
                                    "<Actor2.name> immediately undresses and spreads <Actor2.his_or_her> legs slightly waiting for my mouth and tongue games to arrive."
                                    If !Player.isMale()
                                        Filter(LickPussy)
                                    Else
                                        Filter(Blowjob)
                                    Endif
                                    Sex(Player, Actor2)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    FactorR += 1
                                ElseIf offer2 
                                    Player.strip()
                                    "I immediately undresses and spreads my legs slightly waiting for <Actor2.his_or_her> mouth and tongue games to arrive."
                                    If !Actor2.isMale()
                                        Filter(LickPussy)
                                    Else
                                        Filter(Blowjob)
                                    Endif
                                    Sex(Actor2, Player)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    FactorR += 1
                                ElseIf offer3
                                    Actor2.strip()
                                    "<Actor2.name> immediately undresses and spreads <Actor2.his_or_her> legs slightly waiting for my silky hands to come and caress <Actor2.him_or_her>."
                                    If !Player.isMale()
                                        Filter(Fingering)
                                    Else
                                        Filter(Handjob)
                                    Endif
                                    Sex(Player, Actor2)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    FactorR += 1
                                ElseIf offer4 
                                    Player.strip()
                                    "I immediately undresses and spreads my legs slightly waiting for <Actor2.his_or_her> mouth and tongue games to arrive."
                                    If !Actor2.isMale()
                                        Filter(Fingering)
                                    Else
                                        Filter(Handjob)
                                    Endif
                                    Sex(Actor2, Player)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    FactorR += 1
                                ElseIf offer5
                                    "We immediately undresses and go straight to the point before my <Actor.boyfriend_or_girlfriend> got back."
                                    Actor2.strip()
                                    Player.strip()
                                    Sex(Actor2, Player)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -2)
                                    EndIf
                                    FactorR += 6
                                Endif
                            EndWhile
                            FFRes = FactorR*fuckT
                            If FactorR >= 7 || FFRes >= 70
                                GetCaught = true
                            EndIf
                            If GetCaught
                                If OpenRelationship() && Actor.isInterestedIn(Actor2) || Actor:arousal > 25 && Actor:perversion > 40
                                    Actor(Flirty):: "I must admit ... I kinda enjoyed watching that ..."
                                    Actor:arousal += Random(0, 5)
                                    Actor:perversion += Random(0, 1)
                                    Actor(Flirty):: "Do you mind if I join?"
                                    Player(Flirty)::"Sure babe as always!"
                                    Actor.strip()
                                    Player.strip()
                                    Sex(Actor2, Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -3)
                                    EndIf
                                    "After our passionate session, we got dressed, then we managed to exchange numbers for more fun in the future."
                                    Actor2.makePermanent()
                                    exchangeContact(Actor2)
                                Else
                                    Actor:arousal -= Random(5, 15)
                                    Actor(Angry)::"WTF is going on here!"
                                    Player(Scared)::"Oh shit!"
                                    Player(Shocked):: "<Actor.name>? Oh no ... Please, let me explain. This isn't what it looks like ..."
                                    Actor(Angry):: "Do you take me for a fool? Our relationship is over, you cheating <Player.asshole_or_whore>. Bye!"            
                                    If isPlayerMarried()
                                        Actor(Sad):: "Also, I'm afraid to say this marriage isn't working anymore..."
                                        "And just like that, I divorced that cheating <Actor.boyfriend_or_girlfriend> of mine ..."
                                        mood => 0
                                        Divorce()
                                        LoseDating()
                                        If Player.isDominantSex(Actor) && money > 10000
                                            "Unfortunately for me, the court awarded half of my hard-earned fortune to <Actor.name> ... What a disaster of a marriage that was ..."
                                            money -= money*0.5
                                        Endif        
                                    Else
                                        "And just like that, we broke up. A <Actor.boyfriend_or_girlfriend> like that was good riddance!"
                                        LoseDating()
                                        mood -= 50
                                    Endif
                                    blockContact(Actor)
                                EndIf
                            Else
                                "After a passionate session far more pleasurable than anyone my own age could offer, we got dressed before my <Actor.boyfriend_or_girlfriend> got back. Of course, we managed to exchange numbers for more fun in the future."
                                Actor2.makePermanent()
                                exchangeContact(Actor2)
                            Endif
                        Endif
                    Else
                        Actor2(Shocked)::"Hey! Oh! What are you doing? I'm really interested in you, but not to the point of losing my friendship with your boyfriend."
                        Actor2(Happy)::"But I'll keep this event between us, don't worry!"
                    Endif
                Elseif 1
                    "Maybe this is a start of something special, but it's wiser to take it slow."
                    "I decided to exchange numbers with <Actor2.name> so that we could hang out in our own time without the risk of my <Actor.boyfriend_or_girlfriend> finding out ..."
                    Actor2.makePermanent()
                    exchangeContact(Actor2)
                    Actor2.rapportwithplayer += 2
                Else
                    "Things are getting a bit too heated and somewhat inappropriate even ... Let's try to kill off the conversation now." 
                    Actor2:arousal -= Random(5, 15)
                Endif
                If !GetCaught
                    Actor.show()
                    "After a while, my <Actor.boyfriend_or_girlfriend> returned from the supermarket with the pizza rolls."
                    "<Actor.He_or_She> had no idea ..."
                Endif
                "We finally watched the movie and once the movie was over, we continued to chat and then <Actor2.name> went home."
                Actor2.hide()
            Endif
        Else
            Actor.hide()
            Actor2.hide()
            setBackground(home)
            "I decided to leave them to it and returned to my room after saying hi."
        Endif
    SceneEnd() // Improved by Raddeck
    timeout(150, oldyoung_dating_friend)