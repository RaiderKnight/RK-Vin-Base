WHAT: all, -sleep, -nap, -sleep_hotel, -nap_hotel
WHERE: all
WHEN: 8-24
WHO: Actor = getPerson(true); If Actor.isInterestedIn(Player) && Actor:perversion > 70 && Actor:attractiontoplayer > 30
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        GardeVid = false
        "It's my phone. I've got a new message."
        rk_VB_setCallerVids = rk_VB_setCallerVids.getGlobal()
        Player(Shocked):: "What the hell!?"
        Player.hide()
        setBackground(home)
        Actor.show(2)
        Filter(Solo)
        Sex(Actor)
        "<Actor.name> sent me a video of <Actor.him_or_her> masturbating ..."
        0:: "Say nothing"
        1:: "Ask to cum over..."
        2:: "Tell <Actor.him_or_her> off"
        
        If 0
            GardeVid = true
        ElseIf 1
            GardeVid = true
            Player(Shocked):: "Stay Right there I'm coming over."
            perversion += 1
            moveTo(home)
            Sex(Player, Actor)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
        Else
            "I immediately texted <Actor.name>, making it clear that sending such a video is totally inappropriate and not welcome."
            Actor:attractiontoplayer -= random(0, 1)
        Endif
        If GardeVid
            rk_VB_setCallerVids = rk_VB_setCallerVids.getGlobal()
            tag_RKCVID = Actor.getActorVar(tag_RKCVID)
            ISID = Actor.getID()
            If rk_VB_setCallerVids == 0 || tag_RKCVID == 0
                rk_VB_setCallerVids.setGlobal(ISID)
                Actor.setActorVar(tag_RKCVID, 1)
            EndIf
        EndIf
    SceneEnd()
    timeout(200, co_vi_masturbate) // Improved by Raddeck