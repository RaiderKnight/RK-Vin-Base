WHAT: all
WHERE: home
WHEN: 8 - 22
WHO: Actor = getSpecific(Landlord); If Actor.isInterestedIn(Player) && [Actor:perversion > 70 || Actor:attractiontoplayer > 40 || Actor.hadSex()]
OTHER:

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.dress()
        Actor.show()
        Zero = 0
        CurrentRent = getRent()
        maybeyes = false // Edit Raddeck
        "My perverted <Actor.landlord_or_landlady> offers me not to pay this month's rent if I am willing to sleep with <Actor.him_or_her> ..." // Edit Raddeck
        0:: "Yes"
        1:: "No"
        
        If 0
            "It's just sex, one night with the <Actor.him_or_her> and I can save so much money!"
            perversion += 2
            If random(0, 100) < 33
                Actor2 = getSpecific(Neighbour)
                While !Actor2.isSameGender(Player) && Actor2.isValid()
                    Actor2 = getSpecific(Neighbour)
                Endwhile
                Actor2.show()
                "To my shock, when I arrive in the landlord's bedroom to fulfil my part of the bargain, I find that that one of the other tenants, my neighbour <Actor2.name>, was there as well, already naked no less."
                "I put two and two together and figured that my neighbour has also agreed to this dirty deal with the landlord ..."
                "And now the landlord wants both of us to tend to <Actor.his_or_her> needs at the same time ..."
                "There's no going back now ... I have to do this ... threesome or otherwise."
                Sex(Actor, Actor2, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
                bonuR = random(100, 200)
                bonuRloc = bonuR.convertToLocalCurrency()
                Actor(Flirty)::"Here's a little bonus, <bonuRloc>, because I brought a friend!"
                money += bonuR
            ElseIf random(0, 100) < 50 && !Player.isSameGender(Actor)
                "To my shock, when I arrive in the landlord's bedroom to fulfil my part of the bargain, I find that that <Actor.he_or_she> is already waiting for me there with some of <Actor.his_or_her> friends"
                Actor2 = generatePersonTemporary()
                While !Actor2.isSameGender(Actor)
                    Actor2 = generatePersonTemporary()
                Endwhile
                Actor2:age => Actor:age + Random(-5, 5)
                If Actor2:age < 18
                    Actor2:age => 18
                EndIf
                Actor2.randomizeFace()
                Actor2.randomizeHairs()
                Actor2.dress()
                Actor2.show()
                "The first one introduces <Actor2.himself_or_herself> as <Actor2.name>."
                
                Actor3 = generatePersonTemporary()
                While !Actor3.isSameGender(Actor)
                    Actor3 = generatePersonTemporary()
                Endwhile
                Actor3:age => Actor:age + Random(-5, 5)
                If Actor3:age < 18
                    Actor3:age => 18
                EndIf
                Actor3.randomizeFace()
                Actor3.randomizeHairs()
                Actor3.dress()
                Actor3.show()
                "The second one introduces <Actor3.himself_or_herself> as <Actor3.name>."
                
                Actor4 = generatePersonTemporary()
                While !Actor4.isSameGender(Actor)
                    Actor4 = generatePersonTemporary()
                Endwhile
                Actor4:age => Actor:age + Random(-5, 5)
                If Actor4:age < 18
                    Actor4:age => 18
                EndIf
                Actor4.randomizeFace()
                Actor4.randomizeHairs()
                Actor4.dress()
                Actor4.show()
                "The third one introduces <Actor4.himself_or_herself> as <Actor4.name>."

                "The eager looks on all their faces told me the story right away, I was to pleasure all of them at the same time today ..."
                "There's no going back now ... I have to do this ... gangbang or otherwise."
                Sex(Actor4, Actor3, Actor2, Actor, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -5)
                EndIf
                bonuR = random(100, 200)
                bonuRloc = bonuR.convertToLocalCurrency()
                Actor(Flirty)::"Here's a little bonus, <bonuRloc>, because I brought some friends!"
                money += bonuR*3
            Else
                Sex(Actor, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            EndIf
            If CurrentRent > 0
                SavedRent.setGlobal(CurrentRent)
                Zero.setRent()
            EndIf
        Else
            // Start edit Raddeck
            If Player:arousal > 5 && Player:perversion > 50
                Player(Flirty)::"Not now, maybe next month..."
                maybeyes = true
            ElseIf Player:arousal > 5
                Player(Sad)::"That's very nice of you but no I'm not interested. I'd rather pay with money ..."
                Actor(Sad)::"I understand, so I'll leave you to it... see you next month."
            Else
                Player(Shocked)::"Sorry, I think you have the wrong person, I don't sleep with just anyone, no offense..."
                Actor(Sad)::"Ok, calm down baby, I'm leaving... see you next month."
                Actor:attractiontoplayer -= random(0, 2)
            EndIf
            // End edit Raddeck
            If CurrentRent == 0
                CurrentRent = SavedRent.getGlobal()
                CurrentRent.setRent()
            EndIf
        EndIf
        // Start edit Raddeck
        CurrentRent = getRent()
        HalfCuRent = CurrentRent/2
        If maybeyes
            If Actor:perversion > 50 && Actor:arousal > 15
                AGBlowjob = random(40, 80)
                AGHandjob = random(20, 60)
                AGAssjob = random(150, 280)
                AGBlowjob_loc = AGBlowjob.convertToLocalCurrency()
                AGHandjob_loc = AGHandjob.convertToLocalCurrency()
                AGAssjob_loc = AGAssjob.convertToLocalCurrency()
                HalfCuRent_loc = HalfCuRent.convertToLocalCurrency()
                AGBlowjob1 = random(40, 80)
                AGHandjob1 = random(20, 60)
                AGAssjob1 = random(150, 280)
                AGBlowjob1_loc = AGBlowjob1.convertToLocalCurrency()
                AGHandjob1_loc = AGHandjob1.convertToLocalCurrency()
                AGAssjob1_loc = AGAssjob1.convertToLocalCurrency()
                
                Actor(Grin)::"I don't know if it's me, but you seem interested though... I have other offers if you want?"
                Actor(Grin)::"So instead of reducing your rent I'll give you money directly."
                "Am I interested in <Actor.his_or_her> offer?"
                0::"Ok, let me see your other offers!"
                1::"Still a No!"
                
                If 0
                    Actor(Grin)::"I'm pretty damn horny, so I'm giving you a lots of choices..."
                    0::"<Actor.He_or_She> wants a blowjob. (+<AGBlowjob_loc>)"
                    1::"<Actor.He_or_She> wants a handjob. (+<AGHandjob_loc>)"
                    2::"<Actor.He_or_She> wants to give me a blowjob. (+<AGBlowjob1_loc>)"
                    1::"<Actor.He_or_She> wants to give me a handjob. (+<AGHandjob1_loc>)"
                    4::"<Actor.He_or_She> wants to fuck me in the ass. (+<AGAssjob_loc>)"
                    5::"<Actor.He_or_She> wants to get fuck in the ass. (+<AGAssjob1_loc>)"
                    6::"<Actor.He_or_She> wants to fuck. (+<HalfCuRent_loc>)"
                    99::"I changed my mind!"
                    
                    If 0
                        Player(Flirty)::"Okay, pull down your panties and show me that <Actor.cock_or_pussy>..."
                        Actor.strip()
                        If Actor.isMale()
                            Filter(Blowjob)
                        Else
                            Filter(Licking)
                        EndIf
                        Sex(Player, Actor)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        money += AGBlowjob
                        perversion += 1
                        Actor.dress()
                    ElseIf 1
                        Player(Flirty)::"Ok, pull down your panties and show me that <Actor.dick_or_pussy>..."
                        Actor.strip()
                        If Actor.isMale()
                            Filter(Handjob)
                        Else
                            Filter(Fingering)
                        EndIf
                        Sex(Player, Actor)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        money += AGHandjob
                        perversion += 1
                        Actor.dress()
                    ElseIf 2
                        Actor(Flirty)::"Ok, pull down your panties and show me that <Player.dick_or_pussy>..."
                        Player.strip()
                        If Player.isMale()
                            Filter(Blowjob)
                        Else
                            Filter(Licking)
                        EndIf
                        Sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        money += AGBlowjob1
                        perversion += 1
                        Actor.dress()
                        Player.dress()
                    ElseIf 3
                        Actor(Flirty)::"Ok, pull down your panties and show me that <Player.dick_or_pussy>..."
                        Player.strip()
                        If Player.isMale()
                            Filter(Handjob)
                        Else
                            Filter(Fingering)
                        EndIf
                        Sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        money += AGHandjob1
                        perversion += 1
                        Actor.dress()
                        Player.dress()
                    ElseIf 4
                        Actor(Flirty)::"Ok, pull down your panties and bend down, I will fuck your ass like a god..."
                        Actor.strip()
                        Player.strip()
                        Filter(Anal)
                        Sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        money += AGAssjob
                        perversion += 2
                        Actor.dress()
                        Player.dress()
                    ElseIf 5
                        Player(Flirty)::"Ok, pull down your panties and bend down, I will fuck your ass like a god..."
                        Actor.strip()
                        Player.strip()
                        Filter(Anal)
                        Sex(Player, Actor)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        money += AGAssjob1
                        perversion += 2
                        Actor.dress()
                        Player.dress()
                    ElseIf 6
                        Player(Flirty)::"Let's fuck, I'm horny too so, why not!"
                        Actor(Flirty)::"Good!"
                        Actor.strip()
                        Player.strip()
                        Sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        money += HalfCuRent
                        perversion += 2
                        Actor.dress()
                        Player.dress()
                    Else
                        Actor(Sad)::"I understand, so I'll leave you to it..."
                    EndIf
                Else
                    Random
                        Actor(Sad)::"Okay, I get it, so I'm going to let you... I'll stop pushing to have sex with you... see you next month."
                        Actor(Sad)::"Ok, I get it, I'll let you go... sorry, I didn't mean to push too hard... see you next month."
                        Actor(Sad)::"I understand, so I'll leave you to it... see you next month."
                    EndRandom
                EndIf
            Else
                Actor(Sad)::"I understand, so I'll leave you to it..."
            EndIf
        EndIf
        // End edit Raddeck
    SceneEnd() // Scene improved by Raddeck
    timeoutPrecise(672, free_rent, free_rent_ntr)