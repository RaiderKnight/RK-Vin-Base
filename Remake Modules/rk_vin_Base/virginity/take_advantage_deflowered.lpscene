WHAT: all
WHERE: home
WHEN: 21 - 5
WHO: Actor = getPerson(true); If Actor.isDeflowered() && !Actor.isDating() && Actor:attractiontoplayer > 60
OTHER: !isWithCompanion()

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        SecondScreen(Actor)
        Actor.dress()
        Actor.show()
        "Isn't it good to have a fuckbuddy that's always ready and eager to please you because <Actor.he_or_she> fantasizes about this fucking is gonna turn into 'love' one day."
        "Besides, since <Actor.he_or_she> was a virgin not too long ago, the floodgate isn't wide open yet. Much less risk of STDs than your average fuckbuddies and random hookups."
        "I took <Actor.name>'s virginity and they say you never forget your first time."
        "This is something I can really take advantage of."
        0:: "Offer <Actor.him_or_her> sex"
        1:: "Use <Actor.him_or_her> as a fucktoy"
        2:: "Do nothing!"
    
        If 0
    
            Player(Flirty):: "I'm so horny <Actor.name>. Do you want me to show you new sex positions?"
        
            If Actor.isInterestedIn(Player) && Actor:arousal > 25
        
                Actor(Flirty)::"All the way baby, I'm horny as you!"
                "We undress right away and start to caress each other sensually"
                Sex(Player, Actor)
                Actor:attractiontoplayer += 5
                Actor:rapportwithplayer += 5
                passTime(1, 2.50)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            
            ElseIf Actor.isInterestedIn(Player)
        
                Actor(Sad)::"Not now, I'm not horny as you!"
                Player(Serene)::"Don't be sad, I'll wait!"
                Actor:attractiontoplayer += 1
                Actor:rapportwithplayer += 1
            
            Else
        
                Actor(Sad)::"I'm not interested anymore, you gotta give me more attention if you want sex from me!"
                Player(Sad)::"Oh well, we'll see!"
                Actor:attractiontoplayer -= 1
                Actor:rapportwithplayer -= 1
                mood -= 2
            
            EndIf
    
        ElseIf 1

            Player(Flirty):: "I'm horny. Come over. I want to fuck you!"
            Player(Furious):: "What's taking you so long? Don't pretend like you don't want it."
            Player(Evil):: "I know you fantasize about me every day. I'm your first after all."
            SecondScreen()
            Actor.show()
            "As expected, <Actor.name> couldn't help but bear the humiliation and come to satisfy <Actor.his_or_her> 'special someone', aka me."
            masochist -= 1
            Sex(Player, Actor)
            passTime(1, 1.50)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
        
        Endif
        
    SceneEnd()
    timeout(100, take_advantage_deflowered) // Scene improved by Raddeck