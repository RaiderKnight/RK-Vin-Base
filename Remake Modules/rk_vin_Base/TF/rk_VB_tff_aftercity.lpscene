WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Player.hide()
        "Now, what does the character look like before her transformation? Remember to also give her a name."

    SceneEnd() // Improved by Raddeck
    OpenMenu(CharGen)
    followUp(rk_VB_tff_afterchargen)