WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        
        Player.hide()
        "If you want to start as a woman, choose yes"
        0::"Yes"
        1::"No"
        
        If 0
        
            If Player.isMale()
                Player.changeSex()
            Endif
            followUp(rk_VB_tff_aftercity)
        
        Else
        
            followUp(tf_aftercity)
            
        Endif
    SceneEnd() // Improved by Raddeck