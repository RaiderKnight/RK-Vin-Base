WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Player(Flirty):: "Hey Babe! Would you like to <Player.suck_or_lick> my <Player.cock_or_pussy>?"
        If [!Player.isMale() && Actor:arousal > 15] || [!Player.isMale() && Actor:perversion > 50] || Actor:arousal > 15
            Actor(Excited):: "Uh, of course I want, what's that question..."
            Player.strip()
            Filter(LickPussy)
            sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            Player.dress()
            Actor:attractiontoplayer += Random(0, 3)
            Actor:arousal += Random(0, 2)
        ElseIf [Player.isMale() && Actor:arousal > 15] || [Player.isMale() && Actor:perversion > 50] || Actor:arousal > 15
            Actor(Excited):: "Uh, of course I want, what's that question..."
            Player.strip()
            Filter(Blowjob)
            sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            Player.dress()
            Actor:attractiontoplayer += Random(0, 3)
            Actor:arousal += Random(0, 2)
        ElseIf [Actor:arousal > 25 && Actor:perversion > 50] || [Actor:arousal > 25 && Actor:attractiontoplayer > 50 && Actor:perversion > 50]
            Actor(Flirty):: "Babe, I'm too horny! Let's fuck instead?"
            "What do I say?"
            0::"Sure"
            1::"Later"
            
            If 0
                "We strip right away and started having sex right here!"
                Actor.strip()
                Actor.show()
                Player.strip()
                Actor.strip()
                Player.show()
                Actor.show()
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                Player.dress()
                Actor.dress()
            EndIf
        Else
            Actor(Embarrassed)::"Nah! Not today babe!"
            Actor:attractiontoplayer += Random(0, 1)
        EndIf
    SceneEnd()