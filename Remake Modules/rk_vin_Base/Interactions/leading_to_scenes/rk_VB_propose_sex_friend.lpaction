ACTION_UNIQUEID: rk_VB_propose_sex_friend
ACTION_NAME: Propose sex to friend
WHERE: interaction
WHEN: 0 - 24
MINUTES: 1 - 1
TIMEOUT_MINUTES: 0
ALSO_TIMEOUT: 
EFFECTS:
CONDITIONS:
MOVE_FIRST: true
SCENE_ALWAYS: propose_sex_dating_home
ANIMATION:
WHO: Actor = getTarget(); If !Actor.isCreature() && WHERE == home && [Actor.hadSex() || !Actor.isDating()]