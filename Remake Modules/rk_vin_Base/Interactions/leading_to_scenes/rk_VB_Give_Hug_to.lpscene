WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    sceneStart()
        If isModEnabled(rk_vin_Base) || isModEnabled(rk_MoreInteractions)
            Player.moveToPersonStand(Actor, 40)
            faceEachOther(Player, Actor)
            animatePair(Player, Actor, Hugging)
            Player(Happy)::"..."
            TotalSentences = Player.getActorVar(rk_VB_TotalSentences)
            Hunger = Player.getActorVar(rk_VB_Hunger)
            HungerEnable = rk_VB_EnableHunger.getGlobal()
            UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
            UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
            PoliceMan = generatePersonTemporary()
            Actor1 = generatePersonTemporary()
            GetOffWay = false
            Extork = false
            GetFck = false
            CallPol = false
            GetBeat = false
            If Actor.isProstitute() && !Actor.isDating()
                If !Actor.isInterestedIn(Player) || attractiveness < 50
                    GetOffWay = true
                    If random(0, 50)
                        Extork = true
                    EndIf
                Else
                    "I think <Actor.he_or_she> liked my hug..."
                    Actor:rapportwithplayer += random(-1, 1)
                    Actor:attractiontoplayer += random(-1, 3)
                EndIf
            ElseIf Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                "While I gave <Actor.him_or_her> a hug, <Actor.he_or_she> took the opportunity to grab my ass firmly..."
                If Actor.isMale()
                    "I think I also got him excited while I was hugging him, because I felt a bulge growing in his pants."
                Else
                    "I think I also got her excited while I was hugging her, because I felt her nipples were hard."
                EndIf
                Actor(Flirty)::"If you give me a hug because you want to fuck, I'm in!"
                "What should I say?"
                0::"Sure, let's go somewhere more private"
                1::"No, I just wanted to give you a hug!"
                
                If 0
                    Actor(Flirty)::"I follow!"
                    GetFck = true
                Else
                    Actor(Happy)::"Oh okay! That's nice!"
                    Actor:rapportwithplayer += random(-1, 2)
                    Actor:attractiontoplayer += random(1, 3)
                EndIf
            ElseIf Actor.isInterestedIn(Player) && Actor:perversion > 70
                "While I gave <Actor.him_or_her> a hug, <Actor.he_or_she> took the opportunity to grab my ass firmly..."
                If Actor.isMale()
                    "I think I also got him excited while I was hugging him, because I felt a bulge growing in his pants."
                Else
                    "I think I also got her excited while I was hugging her, because I felt her nipples were hard."
                EndIf
                Actor:arousal += 10
                If random(0, 100) > 50
                    Actor(Flirty)::"If you give me a hug because you want to fuck, I'm in!"
                    "What should I say?"
                    0::"Sure, let's go somewhere more private"
                    1::"No, I just wanted to give you a hug!"
                    
                    If 0
                        Actor(Flirty)::"I follow!"
                        GetFck = true
                    Else
                        Actor(Happy)::"Oh okay! That's nice!"
                        Actor:rapportwithplayer += random(-1, 2)
                        Actor:attractiontoplayer += random(1, 3)
                    EndIf
                EndIf
            ElseIf Actor.isInterestedIn(Player) && Actor:arousal > 25
                If Actor.isMale()
                    "I think I got him excited while I was hugging him, because I felt a bulge growing in his pants."
                Else
                    "I think I got her excited while I was hugging her, because I felt her nipples were hard."
                EndIf
                Actor:arousal += 10
                If random(0, 100) > 75
                    Actor(Flirty)::"If you give me a hug because you want to fuck, I'm in!"
                    "What should I say?"
                    0::"Sure, let's go somewhere more private"
                    1::"No, I just wanted to give you a hug!"
                    
                    If 0
                        Actor(Flirty)::"I follow!"
                        GetFck = true
                    Else
                        Actor(Happy)::"Oh okay! That's nice!"
                        Actor:rapportwithplayer += random(-1, 2)
                        Actor:attractiontoplayer += random(1, 3)
                    EndIf
                EndIf
            ElseIf Actor.isInterestedIn(Player)
                "I think <Actor.he_or_she> liked my hug..."
                Actor:rapportwithplayer += random(0, 1)
                Actor:attractiontoplayer += random(0, 3)
            Else
                CZX = random(0, 100)
                If CZX > 70 && [!Actor.isInterestedIn(Player) || attractiveness < 50]
                    GetOffWay = true
                    Extork = true
                ElseIf CZX > 50 && [!Actor.isInterestedIn(Player) || attractiveness < 50]
                    GetOffWay = true
                Else
                    "I think <Actor.he_or_she> liked my hug..."
                    Actor:rapportwithplayer += random(-1, 1)
                    Actor:attractiontoplayer += random(-1, 3)
                EndIf
            EndIf
            If GetFck
                "I decided to bring <Actor.him_or_her> home..."
                moveTo(home)
                setBackground(home)
                "Once in my room, we undressed immediately and started to caress each other..."
                Actor.strip()
                Player.strip()
                sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                Actor:rapportwithplayer += random(1, 2)
                Actor:attractiontoplayer += random(1, 3)
                Actor.dress()
                Actor(Flirty)::"The fuck was great but I have to go!"
                If !Actor.isContactExchanged()
                    "Should I ask <Actor.him_or_her> for <Actor.him_or_her> phone number?"
                    0::"Yes"
                    1::"No"
                    
                    If 0
                        exchangeContact(Actor)
                        "We exchanged numbers and <Actor.he_or_she> decided to go home!"
                    Else
                        "When it was over, <Actor.he_or_she> decided to go home!"
                    EndIf
                EndIf
                Actor.hide()
            EndIf
            If GetOffWay
                Actor.animate(socialize)
                If Actor:age < 25
                    Actor(Angry)::"Hey idiot, what the fuck are you doing? I don't know you! Get out of my way!"
                Else
                    Actor(Annoyed)::"What are you doing <Player.sir_or_miss>? I don't know you! Get out of my way!"
                EndIf
                If Actor:masochist < masochist || [Actor.isSameGender() && !Actor.isGay()]
                    If !Player.isMale()
                        Actor(Angry)::"Do you want a slap in the face? Don't do that again weirdo!"
                    Else
                        Actor(Angry)::"Do you want my fist in the face? Don't do that again weirdo!"
                    EndIf
                    Actor:rapportwithplayer -= random(0, 3)
                    Actor:attractiontoplayer -= random(0, 3)
                EndIf
                Player.animate(socialize)
                Random
                    TXTF = "I just wanted to be nice"
                    TXTF = "I find you attractive"
                    TXTF = "your perfume attracted me"
                    TXTF = "I didn't mean to bother you"
                EndRandom
                Player(Sad)::"Sorry, <TXTF>!"
                Actor:rapportwithplayer -= random(0, 2)
                Actor:attractiontoplayer -= random(0, 2)
                interpersonal += 0.2
                Actor.animate()
                Player.animate()
            EndIf
            If Extork
                Actor(Angry)::"Since you dared to touch me I'll call the police right away, unless you have some money for me?"
                "What I should do?"
                0::"Push <Actor.him_or_her> and run"
                1::"Pay <Actor.him_or_her>"
                2::"Scold <Actor.him_or_her> gently"
                
                If 0
                    "So I decide to push <Actor.him_or_her> and get out of here as fast as possible...."
                    If fitness > random(0, 30)
                        Actor.hide()
                        If isModEnabled(rk_MoreInteractions)
                            setBackground3D(Modules/rk_MoreInteractions/World/gl_street1.lpworld)
                        Else
                            setBackground3D(Modules/rk_vin_Base/World/gl_street1.lpworld)
                        EndIf
                        "I managed to lose <Actor.him_or_her>..."
                    Else
                        CallPol = true
                    EndIf
                ElseIf 1
                    AFine = random(40, 150)
                    AFineLoc = AFine.convertToLocalCurrency()
                    "So I decide to give <Actor.him_or_her> some money...."
                    If money > AFine
                        Player(Annoyed)::"Okay, how much do you want?"
                        Actor(Grin)::"Give me <AFineLoc> and I'm good!"
                        Player(Annoyed)::"Here's your money, prick!"
                        money -= AFine
                        Actor(Grin)::"Thanks, have a nice day!"
                    Else
                        Player(Annoyed)::"Huh! I don't have any money on me, at least not that amount!"
                        Actor.animate(call)
                        Actor(Grin)::"Ha! Well, sorry for you <Actor.man_or_girl>, your friends are coming to pick you up..."
                        CallPol = true
                    EndIf
                    Actor.animate()
                    "<Actor.He_or_She> turns around and runs way..."
                    Actor.hide()
                Else
                    Player.animate(socialize)
                    Player(Curious)::"Go ahead and call the police! If you really think the police are going to come because I hugged you!"
                    Player(Curious)::"I didn't assault you as far as I know .... Have a nice day!"
                    LotPol = random(1, 5)
                    LotPol = LotPol.round()
                    PFine = random(20, 50)
                    PFineLoc = PFine.convertToLocalCurrency()
                    If LotPol == 3
                        CallPol = true
                    EndIf
                    Player.animate()
                    "<Actor.He_or_She> turns around and continues on <Actor.his_or_her> way..."
                EndIf
                If CallPol
                    "Bad luck day, <Actor.he_or_she> called the police!"
                    Player.animate()
                    PoliceMan.dressUniform(Police)
                    PoliceMan.show(2)
                    Actor1.dressUniform(Police)
                    Actor1.show(4)
                    "And, it looks like the police had nothing to do today, they came right away and gave me a small fine for bothering someone."
                    If money > PFine
                        "I had to pay a <PFineLoc> fine"
                        money -= PFine
                    Else
                        sneak -= 5
                        mood -= 5
                        "Since I don't have the money to pay my fine, they decided to take me to the police station."
                        DayJail = random(1, 2)
                        DayJail = DayJail.round()
                        sentence += DayJail
                        AddSent = Player:sentence 
                        Player.modifyActorVar(rk_VB_TotalSentences, AddSent) 
                        "I got <DayJail> days in jail, we'll see what my lawyer says..."
                        followUp(imprisoned)
                    EndIf
                    Actor.hide()
                EndIf
            EndIf
        EndIf
    sceneEnd() // created by Raddeck