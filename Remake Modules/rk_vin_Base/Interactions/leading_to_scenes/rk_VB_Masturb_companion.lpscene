WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        Player(Flirty):: "Hey Babe! Would you like a <Actor.dick_or_pussy> massage?"
        If [Actor.isFemale() && Actor.isDating() && Actor:arousal > 10] || [Actor.isFemale() && Actor:perversion > 50] || [Actor.isFemale() && Actor:arousal > 15]
            Actor(Excited):: "Uh, of course I want, what's that question..."
            Actor.strip()
            Player.strip()
            Filter(Fingering)
            sex(Player, Actor)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            Actor:attractiontoplayer += Random(0, 3)
            Actor:arousal += Random(0, 2)
        ElseIf [Actor.isMale() && Actor.isDating() && Actor:arousal > 10] || [Actor.isMale() && Actor:perversion > 50] || [Actor.isMale() && Actor:arousal > 15]
            Actor(Excited):: "Uh, of course I want, what's that question..."
            Actor.strip()
            Player.strip()
            Filter(Handjob)
            sex(Player, Actor)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            Actor:attractiontoplayer += Random(0, 3)
            Actor:arousal += Random(0, 2)
        Else
            Actor(Embarrassed)::"Nah! Not now babe!"
            Actor:attractiontoplayer += Random(0, 1)
        EndIf
    SceneEnd()