WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        visit_neighbor = rk_VB_visit_neighbor.getGlobal()
        visit_neighbor2 = rk_VB_visit_neighbor2.getGlobal()
        If isModEnabled(rk_vin_Base) || isModEnabled(rk_MoreInteractions)
            wegonpcfuck = false
            weexchangNumb = false
            UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
            UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
            Hunger = Player.getActorVar(rk_VB_Hunger)
            HungerEnable = rk_VB_EnableHunger.getGlobal()
            NPCFlirt = 0
            NPCFlirt1 = 0
            NPCFlirt2 = 0
            TNPCFlirt = 0
            dancer = false
            IntelKnow = false
            flexing = false
            joke = false
            If visit_neighbor == 1
                Actor = getSpecific(PassedOn)
            Else
                "I decided to flirt this person..."
            EndIf
            Player.moveToPersonStand(Actor, 50)
            faceEachOther(Player, Actor)
            cruisingnpc = true
            While cruisingnpc && TNPCFlirt < 10
                ResultLot = random(1, 16)
                ResultLot = ResultLot.round()
                ResultLot2 = random(1, 14)
                ResultLot2 = ResultLot2.round()
                "What I should do?"
                0::"Use a random pick-up line"
                1::"Seduce"
                2:: !joke ::"Tell a joke"
                3:: "Give <Actor.him_or_her> a wink"
                4:: !IntelKnow ::"Try to impress with my knowledge"
                5:: !flexing && WHERE == sports_centre :: "Do some flexing"
                8:: !dancer && WHERE == biergarten || WHERE == community_centre || WHERE == home || WHERE == nightclub || WHERE == bar || WHERE == pub || WHERE == sports_centre || WHERE == stripclub || WHERE == fraternity || WHERE == university :: "Dance in front of <Actor.him_or_her>"
                99::"Stop charming"
                
                If 0
                    If ResultLot == 1
                        Player(Flirty)::"Can I tie your shoes? Cause I don’t want you to fall for someone else."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"I've heard that one before, go look elsewhere!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 2
                        Player(Flirty)::"I was wondering if you’re an artist because you were so good at drawing me in."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Flirty)::"You're funny! I can draw you in even further if you want!"
                            "What should I say?"
                            0::"Yes"
                            1::"No"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Maybe later! I have something else important to do!"
                                    Actor(Sad)::"Ok that's fine...!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"I've heard that one before, go look elsewhere!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 3
                        Player(Flirty)::"You may fall from the sky, you may fall from a tree, but the best way to fall... is in love with me."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Flirty)::"You're funny! Or I can fall into your bed if you want!"
                            "What should I say?"
                            0::"Anytime"
                            1::"Ignore"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Maybe later! I have something else important to do!"
                                    Actor(Sad)::"Ok that's fine...!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"I've heard this phrase before, go tell it to someone else...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 4
                        Player(Flirty)::"Roses are red, I have a crush, whenever I’m around you, all I do is blush."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"I've heard this phrase before, go tell it to someone else...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 5
                        Player(Flirty)::"Forget about <Actor.Spiderwoman_or_Spiderman>, <Actor.Supergirl_or_Superman>, and <Actor.Catwoman_or_Batman>. I'll be your <Player.man_or_girl>."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"This is the cheapest sentence ever, go tell it to someone else...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 6
                        Player(Flirty)::"There isn't a word in the dictionary for how good you look."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny! Thanks for the compliment!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Maybe, just look in your damn dictionary and leave me alone....!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 7
                        Player(Flirty)::"Excuse me, may I sit here for a moment to recuperate from falling for you?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Ok, then come to my house and we will both fall into my bed and fuck like rabbits.!"
                            "What should I say?"
                            0::"Anytime"
                            1::"Ignore"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Maybe later! I have something else important to do!"
                                    Actor(Sad)::"Ok that's fine...!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Do as you like but I'm not interested in you at all! So don't waste your time!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 8
                        Player(Flirty)::"I’m not a photographer, but I can picture me and you together."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Yeah! Keep dreaming...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 9
                        Player(Flirty)::"Wouldn’t we look good on a wedding cake together?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Yeah! Keep dreaming...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 10
                        Player(Flirty)::"I think there’s something wrong with my phone. Could you try calling it to see if it works?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            If Actor.isContactExchanged()
                                Actor(Wink)::"You're funny! I already have your phone number!"
                            Else
                                Actor(Wink)::"You're funny! Ok, let's exchange our numbers!"
                                exchangeContact(Actor)
                                "We exchanged numbers and continued talking."
                            EndIf
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"This is the cheapest sentence ever, go tell it to someone else...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                            If random(0, 100) > 50 && !Actor.isContactExchanged()
                                Actor(Wink)::"Ok, let's exchange our numbers!"
                                exchangeContact(Actor)
                                "We exchanged numbers and continued talking."
                            EndIf
                        EndIf
                    ElseIf ResultLot == 11
                        Player(Flirty)::"Even if there was no gravity on planet Earth, I would still fall for you."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Instead, come to my house and we will both fall into my bed and fuck like rabbits.!"
                            "What should I say?"
                            0::"Anytime"
                            1::"Ignore"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Maybe later! I have something else important to do!"
                                    Actor(Sad)::"Ok that's fine...!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Do as you like but I'm not interested in you at all! So don't waste your time!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 12
                        Player(Flirty)::"The sparkle in your eyes is so bright, the sun must be jealous."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"This is the cheapest sentence ever, go tell it to someone else...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 13
                        Player(Flirty)::"Well, here I am. What are your other two wishes?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Flirty)::"See you naked and fuck you!"
                            "What should I say?"
                            0::"Ok"
                            1::"Ignore"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Ok! But no! I was expecting something else!"
                                    Actor(Sad)::"Oh well...!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"My first one is: God come and get me I have an idiot in front of me! My second one is: get out of my sight!"
                             Actor(Annoyed)::"I'm not interested!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 14
                        Player(Flirty)::"Are you a magician? Because whenever I look at you, everyone else disappears!"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Yes if you continue I will call the police for harassment... and you will disappear too... !"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 15
                        Player(Flirty)::"If you could read my thoughts, you would marry me tomorrow."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Sorry I'm not a fortune teller so no luck!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot == 16
                        Player(Flirty)::"If being sexy was a crime, you’d be in prison for life!"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny! Thanks for the compliment!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"But also saying this kind of thing could take you to the police for harassment... take off!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    EndIf
                    NPCFlirt += 1
                ElseIf 1
                    If ResultLot2 == 1
                        Player(Flirty)::"You’re so pretty I forgot what I was going say..."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Ah, you are lovely!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Just don't think about me!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Ah, you are charming!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 2
                        Player(Flirty)::"I fall in love with you everytime I look into your beautiful eyes..."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Ah, you are so lovely too!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Damn, come on! Leave me alone!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Thanks, you are charming!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 3
                        Player(Flirty)::"I can forget my very existence in a deep kiss with you."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Well, maybe you will prefer a deep fuck!"
                            "What should I say?"
                            0::"Ok let's do it!"
                            1::"Ignore"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Not really!"
                                    Actor(Sad)::"That's fine!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Yeah! Keep dreaming...!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Charming!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 4
                        Player(Flirty)::"Before I met you, I never knew what it was like to smile for no reason."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"You're funny!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Stop doing drugs and it will stop!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're silly!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 5
                        Player(Flirty)::"You are so amazingly…wonderfully…beautifully…awesomely…most definitely the most precious of all precious things."
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Ok, I would say the same..."
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"When I look at you, it's not the same thing I see!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Sarcastic)::"Ah! Stop it, I'm embarrassed!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 6
                        Player(Flirty)::"Do you come here often ?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"No, but I will...!"
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"None of you're business!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Yeah! I like this place!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 7
                        Player(Flirty)::"Sorry but you look so familiar!"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Hmm, how do we know each other?"
                            Player(Flirty)::"Why don't we figure that out over drinks?"
                            Actor(Wink)::"Sure let's go!"
                            "So we went to have a few drinks in a bar not too far from here!"
                            Dest = findNearbyBuilding(bar)
                            moveTo(Dest)
                            setBackground(bar)
                            Player.animate(drinkglass)
                            Actor.animate(drinkglass)
                            If Actor.isContactExchanged()
                                "We stayed for a few hours and take times to know each other..."
                            Else
                                "We stayed for a few hours and took the opportunity to exchange phone numbers..."
                                exchangeContact(Actor)
                            EndIf
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Nice try! I'm not interested!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Lol, sorry but I don't know you!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 8
                        Player(Flirty)::"Excuse me. I know this is kind of weird and I’m super shy…but I was just wondering…what pick up lines work best with you?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"None, come to my place I will show you my best pick up line!"
                            "What should I say?"
                            0::"Sure"
                            1::"Refuse"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Not now! I want to stay here!"
                                    Actor(Sad)::"That's fine!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"None, move away!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            If !Actor.isContactExchanged()
                                Actor(Wink)::"Let's exchange our numbers?"
                                Player(Flirty)::"Yeah sure!"
                                exchangeContact(Actor)
                                "We exchanged numbers and continued talking."
                            Else
                                Actor(Wink)::"We already exchanged our numbers! Just call me!"
                                Player(Flirty)::"Yeah sure! I will!"
                            EndIf
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 9
                        Player(Flirty)::"Excuse me…I’m a bit lost. Can you give me directions to your heart?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Instead! I can show you the way to my bed!!"
                            "What should I say?"
                            0::"Sure"
                            1::"Refuse"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Not now! I want to stay here!"
                                    Actor(Sad)::"That's fine!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Hmmm, I have no heart! Don't waste your time!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"You're so nice!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 10
                        Player(Flirty)::"You look cool, so I thought I'd come say hi!"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            If !Actor.isContactExchanged()
                                Actor(Wink)::"Let's exchange phone numbers!"
                                Player(Flirty)::"Yeah sure!"
                                exchangeContact(Actor)
                                "We exchanged numbers and continued talking."
                            Else
                                Actor(Happy)::"Thank you for the compliment! Call me sometime, to go out somewhere!"
                            EndIf
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Thanks, that's nice but I'm not interested!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Thanks, that's nice! Hi!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 11
                        Player(Flirty)::"Hey I’ve seen you a few times, don’t you think it’s time we met. My name is <Player.name> and yours?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Hi, my name is <Actor.name>, nice to meet you!"
                            Actor(Wink)::"Do you want to come to my place?"
                            "What should I say?"
                            0::"Sure"
                            1::"Refuse"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Not now! I want to stay here!"
                                    Actor(Sad)::"That's fine!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"None of your business! Move away please!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Hi, my name is <Actor.name>, nice to meet you!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 12
                        Player(Flirty)::"I hope I’m not being too forward, but I saw you from across the room and wanted to introduce myself"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            If !Actor.isContactExchanged()
                                Actor(Wink)::"Not at all, let's exchange phone numbers!"
                                Player(Flirty)::"Yeah sure!"
                                exchangeContact(Actor)
                                "We exchanged numbers and continued talking."
                            Else
                                Actor(Happy)::"Hmm! You already introduce yourself before, but I'm happy to meet you again!"
                            EndIf
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"Hi but I'm not interested!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Thanks, that's nice! Hi!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 13
                        Player(Flirty)::"Hi, I saw you walking by, and I had to come to say hello. I just love your style, I’m <Player.name>!"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"Hi, my name is <Actor.name>, nice to meet you!"
                            Actor(Wink)::"Do you want to come to my place?"
                            "What should I say?"
                            0::"Sure"
                            1::"Refuse"
                            
                            If 0
                                wegonpcfuck = true
                            Else
                                If !Actor.isContactExchanged()
                                    Player(Wink)::"Not now! But let's exchange our numbers!"
                                    Actor(Flirty)::"Yeah sure!"
                                    weexchangNumb = true
                                Else
                                    Player(Sad)::"Not now! I want to stay here!"
                                    Actor(Sad)::"That's fine!"
                                EndIf
                            EndIf
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"None of your business! Move away please!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            Actor(Wink)::"Hi, my name is <Actor.name>, nice to meet you!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    ElseIf ResultLot2 == 14
                        Player(Flirty)::"Hmm sorry <Actor.sir_or_miss>, let me ask you something.. does size really matter?"
                        If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                            Actor(Wink)::"It depend what you're talking about? Anyway size doesn't matter, it's how you use it...why?"
                            If !Player.isMale()
                                Player(Flirty)::"Interesting, because a friend of mine got a huge, huge..."
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..car, and he gets none, while my other friend has a tiny, tiniest"
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..scooter and he gets all of the mens… what’s up with that?"
                            Else
                                Player(Flirty)::"Interesting, because a friend of mine has a huge, huge..."
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..truck, and he gets none, while my other friend has a tiny, tiniest"
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..scooter and he gets all of the girls… what’s up with that?"
                            EndIf
                            Actor(Happy)::"You're funny! I thought we were talking about sex!"
                            Actor:arousal += random(0, 1)
                            Actor:rapportwithplayer += random(2, 4)
                            Actor:attractiontoplayer += random(2, 4)
                        ElseIf !Actor.isInterestedIn(Player)
                            Actor(Annoyed)::"WTF! Get lost dude!"
                            Actor:rapportwithplayer -= random(0, 2)
                            Actor:attractiontoplayer -= random(0, 2)
                        Else
                            If !Player.isMale()
                                Player(Flirty)::"Interesting, because a friend of mine got a huge, huge..."
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..car, and he gets none, while my other friend has a tiny, tiniest"
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..scooter and he gets all of the mens… what’s up with that?"
                            Else
                                Player(Flirty)::"Interesting, because a friend of mine has a huge, huge..."
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..truck, and he gets none, while my other friend has a tiny, tiniest"
                                Actor(Happy)::"...(nervous pause)"
                                Player(Flirty)::"..scooter and he gets all of the girls… what’s up with that?"
                            EndIf
                            Actor(Shy)::"You're funny! I thought we were talking about sex!"
                            Actor:rapportwithplayer += random(0, 2)
                            Actor:attractiontoplayer += random(0, 2)
                        EndIf
                    EndIf
                    NPCFlirt1 += 1
                ElseIf 2
                    "I decided to tell <Actor.him_or_her> a joke... So I started with..."
                    Player.animate(socialize)
                    Random
                        Player(Grin)::"What did the toaster say to the slice of bread? I want you inside me."
                        Player(Grin)::"What do you call a cheap circumcision? A rip-off!"
                        Player(Grin)::"What's the difference between a tire and 365 used condoms? One's a Goodyear. The other's a great year."
                        Player(Grin)::"What do a penis and a Rubik's Cube have in common? The more you play with it, the harder it gets."
                        Player(Grin)::"What did one butt cheek say to the other? Together, we can stop this crap."
                    EndRandom
                    If Actor:rapportwithplayer > 25
                        "<Actor.He_or_She> enjoyed the joke, and said ..."
                        Actor.animate(socialize)
                        Actor(Happy)::"My turn now..."
                        Random
                            Actor(Grin)::"Who's the most popular guy at the nudist colony? The one who can carry a cup of coffee in each hand and a dozen doughnuts."
                            Actor(Grin)::"How do you make a pool table laugh? Tickle its balls."
                            Actor(Grin)::"What do you do when your cat's dead? Play with the neighbor's pussy instead."
                            Actor(Grin)::"What's long, green, and smells like bacon? Kermit The Frog's fingers!"
                            Actor(Grin)::"What do you call a person who doesn’t masturbate? A liar."
                        EndRandom
                        mood += 3
                        CountJokes = 0
                        JokeTelled = 0
                        JokeTelledA = 0
                        While CountJokes < 4
                            0::"One more"
                            1::"Stop joking"
                        
                            If 0
                                If JokeTelled == 0
                                    Player(Grin)::"What’s the best part about gardening? Getting down and dirty with your hoes."
                                    JokeTelled += 1
                                ElseIf JokeTelled == 1
                                    Player(Grin)::"What’s the difference between your wife and your job? After five years, your job will still suck."
                                    JokeTelled += 1
                                ElseIf JokeTelled == 2
                                    Player(Grin)::"Why did God give men penises? So they’d have at least one way to shut a woman up."
                                    JokeTelled += 1
                                ElseIf JokeTelled == 3
                                    Player(Grin)::"What did the leper say to the sex worker? Keep the tip."
                                    JokeTelled += 1
                                ElseIf JokeTelled == 4
                                    Player(Grin)::"Why does a mermaid wear seashells? Because she outgrew her B-shells!"
                                    JokeTelled = 1
                                EndIf
                                Actor(Happy)::"My turn now..."
                                If JokeTelledA == 0
                                    Actor(Grin)::"A naked man broke into a church. The police chased him around and finally caught him by the organ."
                                    JokeTelledA += 1
                                ElseIf JokeTelledA == 1
                                    Actor(Grin)::"What do boobs and toys have in common? They were both originally made for kids, but daddies end up playing with them."
                                    JokeTelledA += 1
                                ElseIf JokeTelledA == 2
                                    Actor(Grin)::"What’s the difference between a hooker and a drug dealer? A hooker can wash her crack and resell it."
                                    JokeTelledA += 1
                                ElseIf JokeTelledA == 3
                                    Actor(Grin)::"What do you call the lesbian version of a cock block? A beaver dam!"
                                    JokeTelledA += 1
                                ElseIf JokeTelledA == 4
                                    Actor(Grin)::"What is Moby Dick's dad's name? Papa Boner."
                                    JokeTelledA += 1
                                EndIf
                                CountJokes += 1
                            Else
                                CountJokes = 5
                                Joke = true
                            EndIf
                            Actor:rapportwithplayer += 0.5
                        EndWhile
                        Actor:attractiontoplayer += 4
                        Player.animate()
                        Actor.animate()
                    ElseIf Actor:rapportwithplayer <= 24.8
                        Actor:attractiontoplayer += 2
                        "<Actor.He_or_She> enjoyed the joke..."
                    Else
                        Actor:attractiontoplayer -= 1
                        "<Actor.He_or_She> looks annoyed by my joke..."
                    EndIf
                    NPCFlirt2 += 1
                    joke = true
                ElseIf 3
                    "I decide to give <Actor.him_or_her> a charming smile while winking at <Actor.him_or_her>"
                    If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 70
                        Actor(Wink)::"Hi, my name is <Actor.name>, nice to meet you!"
                        Actor(Wink)::"Do you want to come to my place?"
                        "What should I say?"
                        0::"Sure"
                        1::"Refuse"
                        
                        If 0
                            wegonpcfuck = true
                        Else
                            If !Actor.isContactExchanged()
                                Player(Wink)::"Not now! But let's exchange our numbers!"
                                Actor(Flirty)::"Yeah sure!"
                                weexchangNumb = true
                            Else
                                Player(Sad)::"Not now! I want to stay here!"
                                Actor(Sad)::"That's fine!"
                            EndIf
                        EndIf
                    ElseIf !Actor.isInterestedIn(Player)
                        "<Actor.He_or_She> completely ignores my advances..."
                        Actor:rapportwithplayer -= random(0, 2)
                        Actor:attractiontoplayer -= random(0, 2)
                    Else
                        "<Actor.He_or_She> winks back at me!"
                        Actor:rapportwithplayer += random(0, 2)
                        Actor:attractiontoplayer += random(0, 2)
                    EndIf
                ElseIf 4
                    "I decide to impress <Actor.him_or_her> with my knowledge..."
                    Random
                        txtPays = "France"
                        txtPays = "Iran"
                        txtPays = "Bazil"
                        txtPays = "Peru"
                        txtPays = "Egypt"
                        txtPays = "Germany"
                    EndRandom
                    Player(Serene)::"Did you know that in <txtPays> there is a really extraordinary statue..."
                    If Actor.isInterestedIn(Player) && intelligence > 70 && Actor:arousal > 25 && Actor:perversion > 70
                        "I think <Actor.he_or_she> was impressed by my knowledge! In any case, <Actor.he_or_she> stayed tuned until the end!"
                        Actor:arousal += random(1, 5)
                        Actor:rapportwithplayer += random(1, 5)
                        Actor:attractiontoplayer += random(1, 5)
                    ElseIf Actor.isInterestedIn(Player) && intelligence > 50
                        If random(0, 100) > 50
                            "I'm having a really hard time explaining things that I feel like I've been talking in a vacuum, but hey!"
                            Actor(Happy)::"Ah! If you say so!"
                            Actor:rapportwithplayer += random(0, 1)
                            Actor:attractiontoplayer += random(0, 1)
                        Else
                            "I think <Actor.he_or_she> was impressed by my knowledge! In any case, <Actor.he_or_she> stayed tuned until the end!"
                            Actor:arousal += random(1, 2)
                            Actor:rapportwithplayer += random(1, 2)
                            Actor:attractiontoplayer += random(1, 2)
                        EndIf
                    ElseIf !Actor.isInterestedIn(Player)
                        "<Actor.He_or_She> completely ignored me..."
                        Actor:rapportwithplayer -= random(0, 2)
                        Actor:attractiontoplayer -= random(0, 2)
                    Else
                        Actor(Happy)::"Ah! If you say so!"
                        Actor:rapportwithplayer += random(0, 1)
                        Actor:attractiontoplayer += random(0, 1)
                    EndIf
                    IntelKnow = true
                ElseIf 5
                    "I decide to impress <Actor.him_or_her> with some semi-erotic flexing..."
                    Player.animate(yoga)
                    If Actor.isInterestedIn(Player) && fitness > 70 && Actor:arousal > 25 && Actor:perversion > 70
                        Actor(Wink)::"Hi, my name is <Actor.name>, nice to meet you!"
                        Actor(Wink)::"Do you want to come to my place, we could do more sensual flexing?"
                        "What should I say?"
                        0::"Sure"
                        1::"Refuse"
                        
                        If 0
                            wegonpcfuck = true
                        Else
                            If !Actor.isContactExchanged()
                                Player(Wink)::"Not now! But let's exchange our numbers!"
                                Actor(Flirty)::"Yeah sure!"
                                weexchangNumb = true
                            Else
                                Player(Sad)::"Not now! I want to stay here!"
                                Actor(Sad)::"That's fine!"
                            EndIf
                        EndIf
                    ElseIf Actor.isInterestedIn(Player) && fitness > 50
                        If random(0, 100) > 50
                            "I don't know what happen but I'm having a really hard time with my flexing today, <Actor.he_or_she> doesn't look likes impressed!"
                            "<Actor.He_or_She> winks back at me!"
                            Actor:rapportwithplayer += random(0, 1)
                            Actor:attractiontoplayer += random(0, 1)
                        Else
                            "I think <Actor.he_or_she> was impressed by my flexing! In any case, <Actor.he_or_she> stayed tuned until the end!"
                            Actor:arousal += random(1, 2)
                            Actor:rapportwithplayer += random(1, 2)
                            Actor:attractiontoplayer += random(1, 2)
                        EndIf
                    ElseIf !Actor.isInterestedIn(Player)
                        "<Actor.He_or_She> completely ignores my flexing..."
                        Actor:rapportwithplayer -= random(0, 2)
                        Actor:attractiontoplayer -= random(0, 2)
                    Else
                        "<Actor.He_or_She> winks back at me!"
                        Actor:rapportwithplayer += random(0, 2)
                        Actor:attractiontoplayer += random(0, 2)
                    EndIf
                    flexing = true
                ElseIf 6
                    "I decide to impress <Actor.him_or_her> with a semi-erotic dance moves in front of <Actor.him_or_her>..."
                    Player.moveToPersonStand(Actor, 50)
                    Player.animate(dance)
                    If Actor.isInterestedIn(Player) && dance > 70 && Actor:arousal > 25 && Actor:perversion > 70
                        Actor(Wink)::"Nice moves!"
                        Actor(Wink)::"Do you want to come to my place, we could do more sensual moves?"
                        "What should I say?"
                        0::"Sure"
                        1::"Refuse"
                        
                        If 0
                            wegonpcfuck = true
                        Else
                            If !Actor.isContactExchanged()
                                Player(Wink)::"Not now! But let's exchange our numbers!"
                                Actor(Flirty)::"Yeah sure!"
                                weexchangNumb = true
                            Else
                                Player(Sad)::"Not now! I want to stay here!"
                                Actor(Sad)::"That's fine!"
                            EndIf
                        EndIf
                    ElseIf Actor.isInterestedIn(Player) && dance > 50
                        If random(0, 100) > 50
                            "I don't know what happen but I'm having a really hard time with my moves today, <Actor.he_or_she> doesn't look impressed!"
                            "But, <Actor.he_or_she> winks back at me!"
                            Actor:rapportwithplayer += random(0, 1)
                            Actor:attractiontoplayer += random(0, 1)
                        Else
                            "I think <Actor.he_or_she> was impressed by my moves!"
                            Actor.animate(dance)
                            "<Actor.He_or_She> winks back at me and started to dance with me!"
                            Actor:arousal += random(1, 2)
                            Actor:rapportwithplayer += random(1, 2)
                            Actor:attractiontoplayer += random(1, 2)
                        EndIf
                    ElseIf !Actor.isInterestedIn(Player)
                        "<Actor.He_or_She> completely ignores my moves..."
                        Actor:rapportwithplayer -= random(0, 2)
                        Actor:attractiontoplayer -= random(0, 2)
                    Else
                        "<Actor.He_or_She> winks back at me!"
                        Actor:arousal += random(0, 1)
                        Actor:rapportwithplayer += random(0, 2)
                        Actor:attractiontoplayer += random(0, 2)
                    EndIf
                    dancer = true
                Else
                    cruisingnpc = false
                    TNPCFlirt = 99
                EndIf
                If wegonpcfuck
                    Player(Wink)::"Okay, I'm open to any adventure!"
                    Actor(Flirty)::"So come on over to my place!"
                    setBackground(bedroom)
                    "Once we got to <Actor.his_or_her> house, we got undressed and started our little sex session."
                    Player.strip()
                    Actor.strip()
                    sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    Actor:rapportwithplayer += random(2, 5)
                    Actor:attractiontoplayer += random(2, 5)
                    If !Actor.isContactExchanged()
                        "Should I ask for <Actor.his_or_her> phone number?"
                        0::"Yes"
                        1::"No"
                        
                        If 0
                            exchangeContact(Actor)
                            "<Actor.He_or_She> happily accepted. We exchanged numbers and continued talking."
                            Actor:rapportwithplayer += random(0, 1)
                            Actor:attractiontoplayer += random(0, 1)
                        EndIf
                    EndIf
                    cruisingnpc = false
                    TNPCFlirt = 99
                EndIf
                If weexchangNumb
                    exchangeContact(Actor)
                    "We exchanged numbers and continued talking."
                    Actor:rapportwithplayer += random(1, 4)
                    Actor:attractiontoplayer += random(1, 4)
                EndIf
                TNPCFlirt += 1
                interpersonal += random(0, 0.5)
                Actor.animate()
                Player.animate()
            EndWhile
        EndIf
        If visit_neighbor == 1
            rk_VB_visit_neighbor.setGlobal(0)
            rk_VB_visit_neighbor2.setGlobal(1)
            passOn(Actor)
            followUp(rk_VB_visit_neighbor)
        EndIf
    SceneEnd()