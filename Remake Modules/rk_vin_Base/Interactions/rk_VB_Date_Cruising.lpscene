WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: Dating = getSpecific(Dating); Actor = getSpecific(Dating_Friend); If Dating:arousal > 25 && Dating:perversion > 50
OTHER: isAtHome()

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If Player.isNaked()
            If Actor:arousal > 25 && Actor:perversion < 50
                Random
                    Actor(Flirty)::"You're so cute when your naked!"
                    Actor(Flirty)::"I love the smell of your skin!"
                    Actor(Flirty)::"You're so delicious when your naked!"
                EndRandom
                Player(Kiss)::"..."
                animatePair(Actor, Player, Kissing)
                Actor(Kiss)::"..."
                arousal += 1
                perversion += 1
                Actor:arousal += 1
                Actor:perversion += 1
            
            If Actor:arousal > 25 && Actor:perversion > 51
            
                SexPositionChoice = Random(1, 6)
                SexPositionChoice = SexPositionChoice.round()
                arousal += 1
                perversion += 1
                Actor:arousal += 1
                Actor:perversion += 1
                Actor.strip()
                Actor(Flirty)::"You makes me horny, when your naked!"
                
                If SexPositionChoice == 1
                
                    Filter(Anal)
                    sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                Elseif SexPositionChoice == 2
                
                    Filter(Blowjob)
                    sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Elseif SexPositionChoice == 3
                
                    Filter(Handjob)
                    sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Elseif SexPositionChoice == 4
                
                    Filter(Licking)
                    sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Elseif SexPositionChoice == 5
                
                    Filter(vaginal)
                    sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                Elseif SexPositionChoice == 6
                
                    If Player.isFemale()
                        Filter(Dildo)
                        sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Elseif Player.isMale()
                        Filter(Fleshlight)
                        sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Else
                        Filter(Handjob)
                        sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Endif
                Endif
            Else
                Actor(Sad)::"Uh! Can you please cover yourself? I'm not interested to see you naked!"
                Actor:perversion -= 1
                Actor:arousal -= 1
                "What I should do?"
                0::"Dress"
                1::"Refuse"
                
                If 0
                    Player.dress()
                    Player.show()
                    "I decided to put some clothes ..."
                    Actor:rapportwithplayer += 1
                    Actor:attractiontoplayer += 1
                Else
                    Player.dress()
                    Player.show()
                    "I decided to stay naked ..."
                    Actor:rapportwithplayer -= 1
                    Actor:attractiontoplayer -= 1
                    Actor(Sad)::"Ok, in that case I'll come back another time, when you're less horny. Seeing you naked makes me uncomfortable."
                    "Just like that, <Actor.name> left to <Actor.his_or_her> house ..."
                Endif
            Endif
            If Dating:arousal > 25 && Dating:perversion < 50
                Random
                    Dating(Flirty)::"You're so cute when your naked!"
                    Dating(Flirty)::"I love the smell of your skin!"
                    Dating(Flirty)::"You're so delicious when your naked!"
                EndRandom
                Player(Kiss)::"..."
                animatePair(Dating, Player, Kissing)
                Dating(Kiss)::"..."
                arousal += 1
                perversion += 1
                Dating:arousal += 1
                Dating:perversion += 1
            If Dating:arousal > 25 && Dating:perversion > 51
                SexPositionChoice = Random(1, 6)
                SexPositionChoice = SexPositionChoice.round()
                arousal += 1
                perversion += 1
                Dating:arousal += 1
                Dating:perversion += 1
                Dating.strip()
                Dating(Flirty)::"You makes me horny, when your naked!"
                
                If SexPositionChoice == 1
                
                    Dating(Flirty)::"Let's do some anal sex!"
                    Filter(Anal)
                    sex(Dating, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                Elseif SexPositionChoice == 2
                
                    Filter(Blowjob)
                    sex(Dating, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Elseif SexPositionChoice == 3
                
                    Filter(Handjob)
                    sex(Dating, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Elseif SexPositionChoice == 4
                
                    Filter(Licking)
                    sex(Dating, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Elseif SexPositionChoice == 5
                
                    Filter(vaginal)
                    sex(Dating, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                Elseif SexPositionChoice == 6
                
                    If Player.isFemale()
                        Filter(Dildo)
                        sex(Dating, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Elseif Player.isMale()
                        Filter(Fleshlight)
                        sex(Dating, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Else
                        Filter(Handjob)
                        sex(Dating, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Endif
                Endif
            Else
                Dating(Sad)::"Uh! Can you please cover yourself? I'm not interested to see you naked!"
                Dating:perversion -= 1
                Dating:arousal -= 1
                "What I should do?"
                0::"Dress"
                1::"Refuse"
                
                If 0
                    Player.dress()
                    Player.show()
                    "I decided to put some clothes ..."
                    Dating:rapportwithplayer += 1
                    Dating:attractiontoplayer += 1
                Else
                
                    Player.dress()
                    Player.show()
                    "I decided to stay naked ..."
                    Dating:rapportwithplayer -= 1
                    Dating:attractiontoplayer -= 1
                    Dating(Sad)::"Ok, in that case I'll come back another time, when you're less horny. Seeing you naked makes me uncomfortable."
                    "Just like that, <Dating.name> left to <Dating.his_or_her> house ..."
                Endif
            Endif
        Elseif Actor:arousal > 25 && Actor:perversion < 50
            Actor(Flirty)::"I want to kiss to you, you're so delicious!"
            Player(Kiss)::"..."
            animatePair(Actor, Player, Kissing)
            Actor(Kiss)::"..."
            Actor:perversion += 1
            Actor:arousal += 1
            Actor:rapportwithplayer += 1
            Actor:attractiontoplayer += 1
        Elseif Dating:arousal > 15
            Dating(Flirty)::"I want to kiss to you, you're so delicious!"
            Player(Kiss)::"..."
            animatePair(Dating, Player, Kissing)
            Dating(Kiss)::"..."
            Dating:perversion += 1
            Dating:arousal += 1
            Dating:rapportwithplayer += 1
            Dating:attractiontoplayer += 1
        Endif
    SceneEnd() // Created by Raddeck
    timeout(250, rk_VB_Date_Cruising)