WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If Actor.isCreatureType(Dog) && random(0, 200) < Actor:interpersonal + Actor:fitness
            Actor.show(2)
            "<Actor.name> is wagging <Actor.his_or_her> tail. <Actor.He_or_She> seems to be in a mood to play fetch."
            0:: "Play catch with <Actor.him_or_her>"
            1:: "Not now"
            
            If 0
                Player(Happy):: "Good <Actor.boy_or_girl>! There, fetch that ..."
                Actor:rapportwithplayer += random(0, 1)
                Actor:attractiontoplayer += random(0, 1)
                Actor:interpersonal += random(0, 1)
                mood += random(0, 5)
                passTime(0.5, 1)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            Else
                Player(Annoyed):: "Not now big <Actor.boy_or_girl>!"
                Actor:rapportwithplayer -= random(0, 1)
                Actor:attractiontoplayer -= random(0, 1)
                Actor:interpersonal -= random(0, 1)
            EndIf
        ElseIf Actor.isCreatureType(Dog)
            "<Actor.name> don't want to play fetch!"
        ElseIf Actor.isCreatureType(Horse) && random(0, 200) < Actor:interpersonal + Actor:fitness
            Actor.show(2)
            "<Actor.name> is wagging <Actor.his_or_her> head. <Actor.He_or_She> seems to be in a mood to play fetch."
            0:: "Play catch with <Actor.him_or_her>"
            1:: "Take a little ride"
            99:: "Not now"
            
            If 0
                Player(Happy):: "Good <Actor.boy_or_girl>! There, fetch that ..."
                Actor:rapportwithplayer += random(0, 1)
                Actor:attractiontoplayer += random(0, 1)
                Actor:interpersonal += random(0, 1)
                mood += random(0, 5)
                passTime(0.5, 1)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            ElseIf 1
                Player(Happy):: "Good <Actor.boy_or_girl>!..."
                Actor:rapportwithplayer += random(0, 1)
                Actor:attractiontoplayer += random(0, 1)
                Actor:interpersonal += random(0, 1)
                mood += random(0, 5)
                passTime(0.5, 1)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            Else
                Player(Annoyed):: "Not now big <Actor.boy_or_girl>!"
                Actor:rapportwithplayer -= random(0, 1)
                Actor:attractiontoplayer -= random(0, 1)
                Actor:interpersonal -= random(0, 1)
            EndIf
        ElseIf Actor.isCreatureType(Horse)
            "<Actor.name> don't want to play fetch!"
        Else
            "You don't have any pets!"
        EndIf
    SceneEnd() // Created by Raddeck