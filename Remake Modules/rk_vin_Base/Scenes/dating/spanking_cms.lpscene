WHAT: all, -pickpocket, -go_to_the_bathroom, -sleep, -sleep_hotel, -nap, -nap_hotel, -rk_VB_small_sleep
WHERE: home
WHEN: 0-24
WHO: Actor = getSpecific(Dating); If Actor:martial - Actor:masochist + masochist - martial > Random(50, 200)
OTHER: none

    SceneStart()
    
        Actor(Evil):: "You've been naughty. Prepare for a spanking!"
        "It's no secret that <Actor.name> is a bit of a dom."
        0:: "Take it"
        1:: "Resist"
    
        If 0
    
            "I must admit I have become somewhat of a sub and have started enjoying it."
            "<Actor.name> spanked me many times before letting me off."
            masochist += 1
            mood += 1
        
        Else
    
            Player(Angry):: "This is going too far!"
            masochist -= 1
            mood -= 1
        
        Endif
    
    SceneEnd()
    
    timeout(300, spanking_cms) // Scene improved by Raddeck