WHAT: all
WHERE: all, -home
WHEN: 0-24
WHO: none
OTHER: Random(0, 100) < 1 && Random(0, 100) < sneak

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        PPReward = Random(50, 150)
        PPReward_loc = PPReward.convertToLocalCurrency()
        PPRewardTrue = Random(1, 2)
        PPRewardTrue = PPRewardTrue.round()
        "Hmm, what's that?"
        Player(Angry):: "Stop, what are you doing!"
        Actor = generatePersonTemporary()
        Actor.dress()
        Actor.show()
        "I caught a pickpocket red-handed."
        0:: "Let <Actor.him_or_her> go"
        1:: "Blackmail <Actor.him_or_her> for sex"
        2:: "Call the cops"
    
        If 0
        
            Actor(Happy):: "Oh really? You're so kind, thank you. I only did this out of desperation ..."
            karma += 2
            
        Elseif 1
        
            If Actor:arousal > 15 && Actor:perversion > 50
            
                Actor(Sad):: "Okay ... I guess I have no other choice. I don't want to go to prison."
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            Else
            
                Actor(Angry)::"Go fuck yourself !"
                Player(Serene)::"Ok fine I call the cops!"
                "I called the cops to take the pickpocket away. Justice has been served!"
                
                If PPRewardTrue == 2
                
                    "I got a reward of <PPReward_loc> for my action!"
                    mood += 1
                    money += PPReward
                    
                EndIf
            
            EndIf
            
        Else
        
            "I called the cops to take the pickpocket away. Justice has been served!"
            If PPRewardTrue == 2
                
                "I got a reward of <PPReward_loc> for my action!"
                mood += 1
                money += PPReward
                    
            EndIf
            
        Endif
        
    SceneEnd()
    timeout(200, catches_pickpocket) // Scene improved by Raddeck