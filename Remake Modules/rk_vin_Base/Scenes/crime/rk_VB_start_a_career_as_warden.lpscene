WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        
        Boss_Sucker = Player.getActorVar(rk_VB_Boss_Sucker) // Get Boss_Sucker value
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        setBackground(police)
        Actor = getPerson(tag_warden)
        If Player.isJob(Police)
                
            "You need to leave your current Police job, to be able to work as a prison warden."
            SceneEnd()
                
        Else
           
            WardenSalary = Random(2000, 5000)
            WardenSalary_loc = WardenSalary.convertToLocalCurrency(true)
            "Should I apply for a job as a prison warden?"
            0:: "Yes"
            1:: "No"
            
            If 0
            
                moveJobs()
                Player.SetJob(Police)
                WardenSalary.setSalary()
                Player.dressUniform(police)
                Player.setActorVar(rk_VB_Boss_Sucker, 50)
                Actor2 = generatePerson()
                Actor2.setActorVar(tag_wardenboss, 1)
                Actor2.dress()
                Actor2.show()
                Actor2(Serene):: "I'm quite impressed with you today. When can you start?"
                "The interview was pretty easy and I got the job, which pays per shift. After which I was introduced to my colleagues - fellow prison wardens."
                exchangeContact(Actor2)
                jobperformance => 50
                count = 0
                While count < 5
                    count += 1
                    Actor = generatePerson()
                    Actor.setActorVar(tag_warden, 1)
                    Actor.dress()
                    Actor.show()
                    Actor.moveToPerson(Player)
                    Actor(Happy):: "Hi, I'm Warden <Actor.name> <Actor.name_last>. Nice to you meet you."
                    Player(Happy):: "Hi, I'm <Player.name> <Player.name_last>"
                    Actor.SaveAndDelete()
                Endwhile
                
            Endif
            
        Endif
    
    SceneEnd() // Scene created by Raddeck