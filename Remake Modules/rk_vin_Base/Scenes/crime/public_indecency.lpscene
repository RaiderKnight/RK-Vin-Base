WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        Actor2 = generatePersonTemporary()
        Actor2.dress()
        Actor2.hide()
        Actor21 = generatePersonTemporary()
        Actor21.dress()
        Actor21.hide()
        SuisSauver = false
        Ripoux = false
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        PoliceMan = generatePersonTemporary()
        Actor1 = generatePersonTemporary()
        PublicIndCount = Player.getActorVar(rk_VB_PublicIndCount)
        TotalSentences = Player.getActorVar(rk_VB_TotalSentences)
        cestlecas = false
        consek = false
        VaenPris = false
        VaBaiser = false
        VaBaiser2 = false
        VaBaiser3 = false
        nonmerci = false
        GeuLoffre = false
        meilleureChance = false
        Actor2:intoxication => random(0, 50)
        Player.modifyActorVar(rk_VB_PublicIndCount, 1)
        "That was rather risky doing that in public. I hope there weren't any cops around or someone called them!"
        If Actor2.isInterestedIn(Player) && Actor2:arousal > 25 && Actor2:perversion > 50 && !isWithCompanion()
            Actor2.show()
            "I wasn't being discreet enough and a stranger saw me in the act ..."
            Actor2(Flirty)::"Hey, I saw you flashing that person, if you let me fuck you, you'll be safe!"
            Player(Curious)::"What??"
            Actor2(Flirty)::"The cops are coming to arrest you in a second, if you let me fuck you, you'll be safe!"
            "What should I do?"
            0:: "Accept <Actor2.his_or_her> offer"
            1:: "Refuse"
            
            If 0
                "So I followed <Actor2.him_or_her> and we went to hide in <Actor2.his_or_her> house!"
                setBackground3D(Modules/rk_vin_Base/World/YoungGRoom.lpworld)
                Actor2(Flirty)::"Now that I have saved you, can we get down to business!"
                Player(Happy)::"Yeah sure!"
                "We undressed and started fucking right away..."
                Player.strip()
                Actor2.strip()
                If Actor2:masochist < masochist && isModEnabled(vin_NonConsensual) && Actor2:perversion > 65
                    Actor2.moveToPersonStand(Player, 40)
                    "Without any reason, the stranger grabs me and starts to be really aggressive and tells me..."
                    Actor2(Evil)::"You like that, showing yourself in front of everybody, you're going to be spoiled, we're going to make a sex movie together and I'm going to post it on the web afterwards! Cool Hey!"
                    "Do I try to fight?"
                    0::"Yes"
                    1::"No"
                    
                    If 0
                        Actor2.moveToPersonStand(Player, 40)
                        Actor2.animate(martialart)
                        Player.animate(martialart)
                        "A fight started ..."
                        Actor2(Evil):: "Stop struggling, you <Player.manwhore_or_bitch> ... You were asking for this by doing it so publicly!"
                        Actor2.moveToPersonStand(Player, 40)
                        Actor2.animate(martialart)
                        Player.animate(martialart)
                        If martial - intoxication > Actor2:martial - Actor2:intoxication     
                            "Thankfully, I managed to escape from the pervert's grasp and landed a strong kick on my assailant."
                        Else
                            "Unfortunately, I've got ambushed by the stranger, I didn't stand a chance."
                            Actor2(Evil):: "This is only what you deserve, teasing me in public!"
                            Player(Scared):: "No! Please stop ..."
                            If !Actor2.isMale() && Player.isMale()
                                Filter(AggressiveFM)
                            Else
                                Filter(Aggressive)
                            EndIf
                            TalkNonConsensual()
                            Sex(Actor2, Player)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                        EndIf
                    Else
                        Actor2(Evil)::"Let's start..."
                        If !Actor2.isMale() && Player.isMale()
                            Filter(AggressiveFM)
                        Else
                            Filter(Aggressive)
                        EndIf
                        TalkNonConsensual()
                        Sex(Actor2, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -3)
                        EndIf
                        pornfame += random(0, 10)
                    EndIf
                Else
                    Actor2(Flirty)::"You are so beautiful!"
                    Sex(Actor2, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                EndIf
                Actor2.hide()
                GeuLoffre = true
            Else
                nonmerci = true
            EndIf
        ElseIf Actor2:masochist < -50 && isModEnabled(vin_NonConsensual) && !isWithCompanion()
            "The stranger didn't say a word but immediately tried to overwhelm me and force <Actor2.himself_or_herself> on me ..."
            "A fight started ..."
            Actor2(Evil):: "Stop struggling, you <Player.manwhore_or_whore> ... You were asking for this by doing it so publicly!"
            If martial - intoxication > Actor2:martial - Actor2:intoxication
                "Thankfully, I managed to escape from the pervert's grasp and landed a strong kick on my assailant."
            Else
                "Unfortunately, already tired from my earlier session and ambushed by a stranger, I didn't stand a chance."
                Actor2(Evil):: "This is only what you deserve, teasing me in public!"
                Player(Scared):: "No! Please stop ..."
                If !Actor2.isMale() && Player.isMale()
                    Filter(AggressiveFM)
                Else
                    Filter(Aggressive)
                EndIf
                TalkNonConsensual()
                Sex(Actor2, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
            EndIf
            GeuLoffre = true
        EndIf
        If !GeuLoffre && sneak <= random(0, 100) && random(0, 100) < 33
            PoliceMan.dressUniform(Police)
            PoliceMan.show(2)
            Actor1.dressUniform(Police)
            Actor1.show(4)
            If !nonmerci
                "I wasn't being discreet enough and some stranger saw me in the act ..."
            EndIf
            "Sure enough, the cops promptly arrived at the scene."
            PoliceMan(Furious)::"'Hands in the air, now! You're under arrest of public indecency'"
            0:: "Run"
            1:: "Surrender"
        
            If 0
                setBackground3D(Modules/rk_vin_Base/World/gl_street1.lpworld)
                If fitness > random(0, 30)
                    SuisSauver = true
                Else
                    PoliceMan.dressUniform(Police)
                    PoliceMan.show(2)
                    Actor1.dressUniform(Police)
                    Actor1.show(4)
                    PoliceMan(Furious)::"'Hands in the air, now! Hey you stop'"
                    Player(Angry):: "You're not taking me alive."
                    0:: "Run"
                    1:: "Surrender"
                
                    If 0
                        If fitness > random(0, 30)
                            SuisSauver = true
                        Else
                            setBackground(street)
                            Player(Angry):: "You're not taking me alive."
                            copfired = 0
                            While copfired < 5
                                If copfired > 0
                                    PoliceMan(Angry)::"HEY YOU! I ASKED YOU TO STOP!"
                                EndIf
                                "Pow!"
                                Player.hide()
                                "A cop fired <PoliceMan.his_or_her> gun..."
                                If fitness > random(0, 30)
                                    "I managed to dodge the shot. Phew, that was close."
                                    sneak += 1
                                    mood += 1
                                    copfired += 1
                                ElseIf copfired >= 5
                                    Actor1.hide()
                                    PoliceMan.hide()
                                    "I managed to lose the cops. Phew, that was close."
                                    sneak += 3
                                    mood += 2
                                    copfired = 6
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -10)
                                    EndIf
                                Else
                                    "The cop fired <PoliceMan.his_or_her> gun. It was a perfect shot ..."
                                    followUp(death)
                                EndIf
                            EndWhile
                        EndIf
                    Else
                        consek = true
                    EndIf
                EndIf
            Else
                consek = true
                setBackground3D(Modules/rk_vin_Base/World/gl_street1.lpworld)
            EndIf
        Else
            SuisSauver = true
        EndIf
        If consek && isModEnabled(rk_vin_Base)
            Player.animate(fightlost)
            Player(Scared):: "Please, don't shoot!"
            "I put my hands up and surrendered to the overwhelming number of cops."
            sneak -= 5
            mood -= 5
            If CurrentCompanion.isValid() && !CurrentCompanion.isCreature()
                BeauteTotal = CurrentCompanion:attractiveness + attractiveness
            EndIf
            "To try to get out of it, should I offer them ..."
            0::"No, I accept my fate!"
            1:: "Offer my body"
            2:: CurrentCompanion.isValid() && !CurrentCompanion.isCreature() && [CurrentCompanion:perversion > 35 || CurrentCompanion:masochist > 35]:: "Offer <CurrentCompanion.name>'s body"
            3:: CurrentCompanion.isValid() && !CurrentCompanion.isCreature():: "Offer both of our bodies"
            
            If 0
                meilleureChance = true
            ElseIf 1
                If attractiveness > 70 && PoliceMan.isInterestedIn(Player) && Actor1.isInterestedIn(Player) && PoliceMan:arousal > 25 && Actor1:arousal > 25
                    PoliceMan(Flirty)::"Well, we accept your offer, we can turn a blind eye this time!"
                    Actor1(Flirty)::"Come on in, the world will think we are arresting you but in fact we went to the hotel to take advantage of your offer!"
                    Player(Happy)::"Yeah sure!"
                    VaBaiser = true
                Else
                    Actor1(Sarcastic)::"Our job is to stop people, not to fuck them or accept their bogus offers... my lovely!"
                    PoliceMan(Sarcastic)::"Don't worry, in prison you will find many people who will accept your offer..."
                    "Damn stupid cops, I have no choice to accept my fates... I guess..."
                    meilleureChance = true
                EndIf
            ElseIf 2
                If CurrentCompanion:attractiveness > 70 && PoliceMan.isInterestedIn(CurrentCompanion) && Actor1.isInterestedIn(CurrentCompanion) && PoliceMan:arousal > 25 && Actor1:arousal > 25
                    PoliceMan(Flirty)::"Well, we accept your offer, we can turn a blind eye this time!"
                    Actor1(Flirty)::"Both, come in, the world will think we are arresting you but in fact we went to the hotel to take advantage of your offer!"
                    Player(Happy)::"Yeah sure!"
                    VaBaiser2 = true
                Else
                    Actor1(Sarcastic)::"Our job is to stop people, not to fuck them or accept their bogus offers... my lovely!"
                    PoliceMan(Sarcastic)::"Don't worry, in prison you will find many people who will accept your offer..."
                    "Damn stupid cops, I have no choice to accept my fates... I guess..."
                    meilleureChance = true
                EndIf
            Else
                If BeauteTotal > 140 && PoliceMan.isInterestedIn(Player) && Actor1.isInterestedIn(Player) && PoliceMan:arousal > 25 && Actor1:arousal > 25 && PoliceMan.isInterestedIn(CurrentCompanion) && Actor1.isInterestedIn(CurrentCompanion)
                    PoliceMan(Flirty)::"Well, we accept your offer, we can turn a blind eye this time!"
                    Actor1(Flirty)::"Both, come in, the world will think we are arresting you but in fact we went to the hotel to take advantage of your offer!"
                    Player(Happy)::"Yeah sure!"
                    VaBaiser3 = true
                Else
                    Actor1(Sarcastic)::"Our job is to stop people, not to fuck them or accept their bogus offers... my lovely!"
                    PoliceMan(Sarcastic)::"Don't worry, in prison you will find many people who will accept your offer..."
                    "Damn stupid cops, I have no choice to accept my fates... I guess..."
                    meilleureChance = true
                EndIf
            EndIf
        EndIf
        If meilleureChance
            If PoliceMan:arousal > 25 && PoliceMan:perversion > 50 && Actor1:arousal > 25 && Actor1:perversion > 50 && isModEnabled(rk_vin_Base) && [!VaBaiser || !VaBaiser2 || !VaBaiser3]
                If CurrentCompanion.isValid()
                    "If you are interested, we can turn a blind eye to this if you let us fuck you both!"
                Else
                    "If you are interested, we can turn a blind eye to this if you let us fuck you!"
                EndIf
                "What I should do?"
                0::"Accept"
                1::"Refuse"
                
                If 0
                    If CurrentCompanion.isValid()
                        VaBaiser3 = true
                    Else
                        VaBaiser = true
                    EndIf
                Else
                    cestlecas = true
                EndIf
            Else
                cestlecas = true
            EndIf
            If cestlecas
                sentence += random(10, 30)
                AddSent = Player:sentence
                Player.modifyActorVar(rk_VB_TotalSentences, AddSent)
                If PoliceMan:arousal > 25 && PoliceMan:perversion > 75 && Actor1:arousal > 25 && Actor1:perversion > 75 && PoliceMan:masochist < 0 && Actor1:masochist < 0
                    Ripoux = true
                Else
                    VaenPris = true
                EndIf
            EndIf
        EndIf
        If Ripoux && isModEnabled(rk_vin_Base)
            "As we drive towards the police station, one of the police officer tell me ..."
            PoliceMan(Grin)::"We have to make a little detour before taking you to the station."
            Player(Anxious)::"Oh it's good I guess!"
            "And they look at each other with a suspicious smile ..."
            setBackground3D(Modules/vin_Base/World/forest___vin01.lpworld)
            "After a while they take a dirt road and we enter the forest, after a few mile they stopped the car and asked me to get out, which I did."
            PoliceMan.show()
            Actor1.show()
            "I'm handcuffed, I think I know what their intentions are ... They undress ..."
            Player.strip()
            "One of the policemen approaches me and puts a stocking in my mouth, while the other takes off all my clothes."
            "I can scream, but with a stocking in my mouth, there is absolutely no point in resisting ... "
            "My scream for help went unheard in the dark forest and I had no choice but endure it all."
            PoliceMan.strip()
            Actor1.strip()
            roundtofuck = true
            roundtoGetfucked = 0
            While roundtofuck
                If roundtoGetfucked == 3
                    roundtofuck = false
                ElseIf roundtoGetfucked == 0
                    Filter(Aggressive)
                    TalkNonConsensual()
                    SexNoAffair(Actor1, Player)
                    roundtoGetfucked += 1
                ElseIf roundtoGetfucked == 1
                    Filter(Aggressive)
                    TalkNonConsensual()
                    SexNoAffair(PoliceMan, Player)
                    roundtoGetfucked += 1
                ElseIf roundtoGetfucked == 2
                    Filter(Aggressive)
                    TalkNonConsensual()
                    SexNoAffair(Actor1, Player, PoliceMan)
                    roundtoGetfucked += 1
                    roundtofuck = false
                EndIf
            EndWhile
            "After raping me, they told me to shut my mouth about that or it will happen again in prison .... "
            mood => 0
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -20)
            EndIf
            VaenPris = true
        EndIf
        If VaBaiser
            setBackground(hotel)
            "Once we arrived at the hotel, we undressed and I let them have fun with my body..."
            Player.strip()
            PoliceMan.strip()
            Actor1.strip()
            Sex(Actor1, Player, PoliceMan)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -3)
            EndIf
        EndIf
        If VaBaiser2
            setBackground(hotel)
            "Once we arrived at the hotel, they undressed and <CurrentCompanion.name> let them have fun with <CurrentCompanion.his_or_her> body..."
            CurrentCompanion.strip()
            PoliceMan.strip()
            Actor1.strip()
            Sex(Actor1, CurrentCompanion, PoliceMan)
        EndIf
        If VaBaiser3
            setBackground(hotel)
            "Once we arrived at the hotel, we undressed and we let them have fun with our bodies..."
            Player.strip()
            CurrentCompanion.strip()
            PoliceMan.strip()
            Actor1.strip()
            Sex(Actor1, Player, PoliceMan, CurrentCompanion)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -4)
            EndIf
        EndIf
        If VaBaiser || VaBaiser2 || VaBaiser3
            PoliceMan(Happy)::"You can leave now, hurry up before we change our minds..."
            Actor1(Happy)::"Yes and, next time we arrest you, we might not be so nice!"
            Player.dress()
            If CurrentCompanion.isValid()
                CurrentCompanion.dress()
                Player(Happy)::"Sure, thanks to let us go!"
            Else
                Player(Happy)::"Sure, thanks to let me go!"
            EndIf
        EndIf
        If VaenPris
            followUp(imprisoned)
        EndIf
        If SuisSauver && isModEnabled(rk_vin_Base)
            setBackground3D(Modules/rk_vin_Base/World/gl_street1.lpworld)
            Actor1.hide()
            PoliceMan.hide()
            "I managed to lose the cops. Phew, that was close."
            sneak += 1
            mood += 2
            energy -= 10
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -10)
            EndIf
            If Actor21.isInterestedIn(Player) && Actor21:interpersonal > 50
                "A stranger seemed to take my brazen public act as an invitation and confidently came up to me and suggested that I have some fun with <Actor21.him_or_her>!"
                0:: "Why not?"
                1:: "Turn <Actor21.him_or_her> down"
                
                If 0
                    "Pubic sex with a complete stranger! Always makes me horny!"
                    Sex(Player, Actor21)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    GeuLoffre = true
                Else
                    "I might have liked doing in public, it doesn't mean I would do it with a random stranger."
                    moveTo(home)
                EndIf
            ElseIf Actor21.isInterestedIn(Player) && Actor21:interpersonal < 50 && Actor21:perversion > 50
                "The stranger didn't have the confidence to proposition me. Instead, my peeping Tom simply jerked off."
                Filter(Solo)
                Sex(Actor21)
                
                "Maybe I should offer <Actor21.him_or_her> a hand?"
                0:: "Join the masturbating loner"
                1:: "Ignore <Actor21.him_or_her>"
                
                If 0
                    Player:: "Sorry I got you so excited. It's only fair that I help you relieve all this pressure."
                    Sex(Actor21, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                EndIf
            Else
                moveTo(home)
            EndIf
        EndIf
        Actor1.hide()
        PoliceMan.hide()
    SceneEnd() // Scene improved by Raddeck