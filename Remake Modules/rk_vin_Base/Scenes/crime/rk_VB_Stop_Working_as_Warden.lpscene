WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Boss_Sucker = Player.getActorVar(rk_VB_Boss_Sucker) // Get Boss_Sucker value
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        "Am I sure that I want to resign from my prison warden job?"
        0:: "Yes"
        1:: "No"
        
        If 0
            "This job just wasn't for me. I submitted a resignation letter to my boss."
            "Time for a new chapter in my career."
            loseJob()
        Else
            "On second thought, I can't afford to give up this job."
        Endif
    SceneEnd() // Scene edited by Raddeck