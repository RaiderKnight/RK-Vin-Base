WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    
    SceneStart(()

        Actor = getCompanion()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        personal_vids = Player.getActorVar(rk_VB_personal_vids)
        "I wonder if my <Actor.boyfriend_or_girlfriend> is willing to do a sexto..."
        "What if I asked <Actor.name> to do this for one of <Actor.his_or_her> friends?"
        0:: "Nah I shouldn't do this. That could turn out bad."
        1:: "Honestly that sounds pretty hot. I need to see this."

        If 0
            "I explain my desire to see <Actor.name> sending a sexto to one of <Actor.his_or_her> own friends, I will film all the show."
            If Actor:perversion > 50
                Actor(Flirty):: "Alright, anything I can do to make you happy! I hope it will be fun for you and me too!" 
                Player(Flirty):: "Having the privilege to zoom in on your private parts, Oh it will trust me! I love when you do dirty things!"
                arousal += 1
                perversion += 1
                Actor:perversion += 1
                Actor2 = generatePersonTemporary()
                Actor2.makeInterested(Actor)
                Actor2.hide()
                "<Actor.name> then decided to choose a random friend that <Actor.he_or_she> know, called <Actor2.name>."
                setBackground(home)
                Actor.strip()
                Filter(Solo)
                Sex(Actor)
                
                secondScreen()
                "While watching <Actor.name> masturbate I have a fantasy running through my head..." 
                0:: "Fantasize about <Actor.name> and <Actor2.name> having sex." 
                1:: "Fantasizing about the three of us having sex"
                2:: "Suddenly jump onto your lover and take <Actor.him_or_her> hard"

                If 0
                    Actor2.strip()
                    SexNoAffair(Actor2, Actor)  
                ElseIf 1
                    Actor2.strip()
                    Player.strip()
                    SexNoAffair(Player, Actor2, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -3)
                    EndIf
                Else
                    Player.strip()
                    Sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                EndIf
                Actor:attractiontoplayer += 1
                "Once I finish filming my <Actor.boyfriend_or_girlfriend>, we decide to send a copy to <Actor2.name>"
                Player.modifyActorVar(rk_VB_personal_vids, 1)
                secondScreen(Actor2)
                Actor2.show()
                "Now, let's see how <Actor2.name> responds ..."
                If Actor2:perversion > 40
                    "<Actor2.name> sent a nude back."
                    Filter(Solo)
                    Sex(Actor2)
                ElseIf Actor2:perversion > 60 && Actor2:masochist < -50 && choice == 1
                    "<Actor2.name> invites <Actor.name> over to have sex." 
                    0:: "Allow it"
                    1:: "Put a stop to it"
                    
                    If 0
                        secondScreen()
                        "I helpfully drove <Actor.name> over to <Actor2.name>'s place."
                        setBackground(home)
                        Actor2.dress()
                        Actor2.show()
                        "Once there, I waited outside while they had sex."
                        Actor2.strip()
                        Sex(Actor2, Actor)
                        Actor2:rapportwithplayer += 2
                    EndIf
                ElseIf Actor2:perversion > 40 && Actor2:masochist < -25
                    TempVids = personal_vids - 1
                    Actor2(Happy):: "Send another!"
                    0::TempVids > 0:: "Send another"
                    1:: "Make an another one"
                    99:: "Put a stop to it"
                    
                    If 0
                        "Another nude was sent to <Actor2.name>"
                    ElseIf 1
                        followUp(sr_fso_phonedare2_cms)
                    EndIf
                    Actor2:rapportwithplayer += 2
                Else
                    "No response from <Actor2.name>."
                EndIf
            Else
                Actor(Sad):: "No way, I'm not doing that."
                Actor:arousal -= 1
                Actor:perversion -= 1
            EndIf
        EndIf
    
    SceneEnd() // Scene improved by Raddeck
    //timeout(164, sr_fso_phonedare2_cms)
