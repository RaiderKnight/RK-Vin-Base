WHAT: masturbate, sleep, sleep_hotel, nap, nap_hotel
WHERE: all
WHEN: 0 - 24
WHO: none
OTHER: !Player.isMale() && Random(0, 200) < perversion && arousal > 25

    SceneStart()
        Dating = getSpecific(Dating)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Wetdream = false
        NightMares = Random(1, 5)
        NightMares = NightMares.round()
        If NightMares == 1
            followUp(tentacles_dark_alley)
        Elseif NightMares == 2
            followUp(tentacles_shower)
        Elseif NightMares == 3
            followUp(tentacles_disguise)
        Elseif NightMares == 4
            followUp(tentacles_cult)
        Elseif NightMares == 5
            followUp(tentacles_alien_deal)
        Endif
    SceneEnd() // Creted by Raddeck
    timeout(550, rk_VB_NightMaresF)