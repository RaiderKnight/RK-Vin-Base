WHAT: all
WHERE: work
WHEN: 14 - 24
WHO: Actor = getSpecific(Dating); Actor2 = getSpecific(Boss); If !Actor.isMale() && Actor.isColleague() && Actor2.isMale()
OTHER: Player.isMale() && random(50, 100) < perversion && isTimingOut(sr_fso_work_dare1_cms) && isModEnabled(vin_NTR)

    
    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Boss_Sucker = Player.getActorVar(rk_VB_Boss_Sucker)
        dressFormal()
        Actor.dress()
        Actor.show()
        Actor2.dress()
        Actor2.show()
        Flash = getActorVar(tag_sr_fso_work_flash)
        "My <Actor.boyfriend_or_girlfriend> <Actor.name> works in the same office as me."
        If Flash >= 1
            "Should I give my lovely SO a dare today?"
            0:: "Yes, of course!"
            1:: "No, I should focus on work today."

            If 0
                "Now who should give the dare?"
                0:: "Me obviously." 
                1:: "My boss who we made the deal with."

                If 0 
                    Actor.stripOne()
                    "I tell <Actor.name> to flash the boss."
                    Actor2:arousal += random(0, 5)
                    If Actor2:arousal > 25
                        "Considering our past history, things escalate fast."
                        Sex(Actor2, Player, Actor)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -3)
                        EndIf
                    EndIf
                Else
                    Player.modifyActorVar(rk_VB_Boss_Sucker, 2)
                    Actor.stripOne()
                    "So <Actor.name> is going to flash the employee who was made aware of the deal and who paid the boss to participate."
                    Actor3:arousal += random(0, 5)
                    Actor3 = getSpecific(Colleague)
                    While Actor3.isValid() && [!Actor3.isInterestedIn(Actor) || Actor3:perversion < 50]
                        Actor3 = getSpecific(Colleague)
                    EndWhile
                    Actor3.dress()
                    Actor3.show()
                    "the employee paid the boss to be let in."
                    Sex(Actor3, Actor)
                EndIf
            EndIf
        Else
            "This is certainly a morale booster, having someone so dear near me for the whole work day."
            "As a result, my stress level reduces ..."
            mood += 10    
        EndIf
    SceneEnd()
    timeout(160, sr_fso_workdare3_cms) // Scene improved by Raddeck