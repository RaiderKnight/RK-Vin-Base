WHAT: rk_VB_Visit_Internet, rk_VB_Use_Computer, -sleep, -nap
WHERE: home
WHEN: 8 - 22
WHO: Actor = getSpecific(Dating); If Actor:rapportwithplayer > 35
OTHER: perversion > 50 && isModEnabled(vin_NTR) && isWithCompanion()

    SceneStart()
        personal_vids = Player.getActorVar(rk_VB_personal_vids)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "I was browsing the web looking for sexual challenges I could give my love and I found a few that piqued my interest."
        Player(Curious)::"Hey babe! Do you want to play dare today?"
        If Actor:arousal > 15 && Actor:perversion > 50
            Actor(Flirty):: "Oh naughty! Well we are at home...Ok. What I have to do?"
            LetsPlay = true
        Else
            Actor(Sad)::"Not really feeling like playing that now!"
        Endif
        ActorLD = random(0, 100)
        If LetsPlay
            Actor.dress()
            Actor.show()
            Actor(Curious)::"What I will have to do?"
            Player(Flirty)::"I want to..."
            0:: "Film a swinger party including us and 2 of your best friends"
            1:: "Watch you to masturbate in front of the window"
            2:: "Watch you to masturbate in front of 2 of my friend without no reason"
            99:: "Nevermind!"
        
            If 0
                Player(Curious):: "Hey babe, I'd like to film us doing a couple swap with a friend couple that you know well? Basically filming a swinger session?"
                If ActorLD > 50
                    VCnt = 0
                    Actor:perversion += 1
                    Actor:arousal += 1
                    Actor(Curious):: "Oh! Yeah I'm willing to do it!"
                    Actor(Flirty)::"I can't believe how kinky you can get sometimes! But you have to promise me that if it starts to make you jealous, we'll stop."
                    Player(Flirty):: "Don't worry I'll let you know if I can't take it. Same on your side, tell if you have enough!"
                    Actor(Flirty):: "Just remember that I'm doing this just for you!"
                    Actor(Flirty):: "Alright then, I'll call a new friend couple that I met few days ago!"
                    Actor1 = generatePersonTemporary()
                    While !Actor1.isSameGender(Actor)
                        Actor1 = generatePersonTemporary()
                    EndWhile
                    Actor2 = generatePersonTemporary()
                    While !Actor2.isSameGender(Player)
                        Actor2 = generatePersonTemporary()
                    EndWhile
                    Actor1:age => Actor:age + Random(-3, 3)
                    If Actor1:age < 18
                        Actor1:age => 18
                    Endif
                    Actor1.randomizeFace()
                    Actor1.randomizeHairs()
                    Actor2:age => Actor:age + Random(-3, 3)
                    If Actor2:age < 18
                        Actor2:age => 18
                    Endif
                    Actor2.randomizeFace()
                    Actor2.randomizeHairs()
                    Actor1.makeInterested(Player)
                    Actor2.makeInterested(Player)
                    "Two of <Actor.his_or_her> best friends immediately accepted our invitation and came over after <Actor.he_or_she> hung up."
                    Actor1.makeInterested(Actor)
                    Actor2.makeInterested(Actor)
                    setBackgroundCustom(livingroom)
                    "After a few minutes, <Actor.his_or_her> friends finally arrived..."
                    Actor1.dress()
                    Actor1.show()
                    Actor2.dress()
                    Actor2.show()
                    Player.animate(socialize)
                    Actor.animate(socialize)
                    Actor1.animate(socialize)
                    Actor2.animate(socialize)
                    Actor1:arousal += 10
                    Actor2:arousal += 10
                    "We chatted for a while all together, just to loosen up a bit...."
                    Player.animate(drinkglass)
                    Actor.animate(drinkglass)
                    Actor1.animate(drinkglass)
                    Actor2.animate(drinkglass)
                    "Then we started to drink a few glasses of alcohol..."
                    "Once we were all relaxed and our shyness was gone, our conversations turned to sex, which finally made us want to start our sex session..."
                    Actor.strip()
                    Actor1.strip()
                    Actor2.strip()
                    Player.strip()
                    sexNoAffair(Actor1, Actor)
                    VCnt += 2
                    sexNoAffair(Player, Actor2)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -3)
                    EndIf
                    VCnt += 2
                    "Should we do a second round with a different partner?"
                    0::"Yes"
                    1::"No that's enough!"
                    
                    If 0
                        ContiNextS = true
                        Actor.strip()
                        Actor1.strip()
                        Actor2.strip()
                        Player.strip()
                        sexNoAffair(Actor2, Actor)
                        VCnt += 2
                        Player(Wink)::"Now our turn..."
                        sexNoAffair(Player, Actor1)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -3)
                        EndIf
                        VCnt += 2
                    Endif
                    If ContiNextS
                        "Should we do a third round and do a foursome?"
                        0::"Yes"
                        1::"No that's enough!"
                        
                        If 0
                            Actor.strip()
                            Actor1.strip()
                            Actor2.strip()
                            Player.strip()
                            sexNoAffair(Actor2, Actor, Player, Actor1)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -4)
                            EndIf
                            VCnt += 4
                        Endif
                    Endif
                    If VCnt >= 1
                        Player.modifyActorVar(rk_VB_personal_vids, VCnt)
                        "<VCnt> videos added to my personal video collection!"
                    Endif
                Else
                    Actor(Sad):: "Oh god! Not today, maybe another time!"
                    Actor:mood -= 5
                    Actor:perversion += 1
                EndIf
                Actor1.hide()
                Actor2.hide()
            Elseif 1
                Player(Curious):: "Hey baby, I'd like you to masturbate in front of the window while there are passers-by."
                If ActorLD > 50
                    Actor:arousal += 1
                    Actor:perversion += 1
                    Actor(Flirty)::"It's a little risky for the police, but okay!"
                    Actor(Curious):: "I don't know where you got your ideas sometimes!"
                    Player(Flirty):: "Bah! You know me..."
                    Actor(Flirty):: "Just remember that I'm doing this just for you!"
                    Actor.strip()
                    Actor.animate(masturbate)
                    "<Actor.He_or_She> takes the time to position <Actor.himself_or_herself> in front of the window and starts to masturbate in front of the passers-by ...."
                    count = 0
                    Amasturbating = true
                    While Amasturbating && count < 3
                        Actor.strip()
                        Actor.animate(masturbate)
                        If Random(0, 100) > 85
                            "That was rather risky doing that in front of the window."
                            "Sure enough, the cops promptly arrived at the scene and arrest my <Actor.boyfriend_or_girlfriend>."
                            Actor:mood -= 5
                            ActID = Actor.getID()
                            Actor.setActorVar(rk_VB_ActArrestTagD, ActID)
                            Actor.setActorVar(tag_DPrison, 1)
                            Datingsentence = Random(2, 6)
                            Actor.setActorVar(rk_VB_Sentence, Datingsentence)
                            Actor.setActorVar(rk_VB_MeToD, ActID)
                            DayGetImprison = Actor:age
                            Actor.setActorVar(rk_VB_ActSentence, DayGetImprison)
                            count += 15
                        Elseif Random(0, 100) > 65
                            Actor.hide()
                            Actor4 = generatePersonTemporary()
                            Actor5 = generatePersonTemporary()
                            Actor4.strip()
                            Actor4.show()
                            Actor5.strip()
                            Actor5.show()
                            "A horny couple stops in front of the window and starts to fuck at the same time!"
                            sex(Actor4, Actor5)
                            Actor4.hide()
                            Actor5.hide()
                            Actor.show()
                            "And <Actor.name> still masturbating vigorously..."
                            count += 1
                        Else
                            Actor3 = generatePersonTemporary()
                            secondScreen(Actor3)
                            Actor3.strip()
                            Actor3.show()
                            Actor3.animate(masturbate)
                            "Someone stops in front of the window and starts to masturbate at the same time!"
                            secondScreen()
                            "And <Actor.name> still masturbating vigorously..."
                            count += 1
                            Actor3.hide()
                        Endif
                    EndWhile
                Else
                    Actor(Sad):: "Are you crazy! No way, maybe another time!"
                    Actor:mood -= 5
                    Actor:perversion += 1
                EndIf
            Elseif 2
                If ActorLD > 50
                    Player(Flirty)::"I'd like to invite two friends over to watch the soccer game tonight and all of a sudden, for no reason, you start masturbating."
                    Actor(Curious)::"Ok, but you know that could get hardcore!"
                    Player(Flirty)::"Ok, so if it happen, are you willing for a bukkake?"
                    Actor(Flirty)::"Oh, hell yeah! Bring me the juice!"
                    Player(Happy)::"Ok, so I'll call some friends..."
                    firstAc = true
                    SeconAc = true
                    searchFF = true
                    While searchFF
                        If firstAc
                            "Who's the first guy I'm going to invite?"
                            selectNPC()
                            Actor6 = getSelectedNPC()
                            Actor6.dress()
                            Actor6.hide()
                            firstAc = false
                        EndIf
                        If !Actor6.isValid() || !Actor6.isMale()
                            "Either you haven't found any friends or you have chosen a female. You need to find two males who are friends with you."
                            Actor6.Delete()
                        EndIf
                        If !firstAc && SeconAc
                            "And the second guy?"
                            selectNPC()
                            Actor7 = getSelectedNPC()
                            Actor7.dress()
                            Actor7.hide()
                            SeconAc = false
                            searchFF = false
                        EndIf
                        If !Actor7.isValid() || !Actor7.isMale()
                            "Either you haven't found any friends or you have chosen a female. You need to find two males who are friends with you."
                            Actor7.Delete()
                        EndIf
                    EndWhile
                    If Actor6.isValid() && Actor7.isValid()
                        "After a while... My friends finally arrived!"
                        allFreund = true
                    Elseif Actor6.isValid() || Actor7.isValid()
                        Player(Sad):: "I could only find one friend I know who would be interested, so I cancelled the challenge...."
                        Actor(Wink):: "That's also mean that we need to find more active friends!"
                    Else
                        Player(Sad):: "I couldn't find any friend I know that would be interested, so I canceled the dare..."
                        Actor(Wink):: "That's also mean that we need to find more active friends!"
                    EndIf
                    If allFreund
                        Ask7 = false
                        GSHap = false
                        fuckedA = false
                        fuckedB = false
                        setBackgroundCustom(livingroom)
                        Actor6.show()
                        Actor7.show()
                        "They greet my <Actor.boyfriend_or_girlfriend> and we invite them in the living room, we sit down and we watch the soccer match which has already started since a few minutes..."
                        Player.animate(drinkbottle)
                        Actor.animate(drinkbottle)
                        Actor6.animate(drinkbottle)
                        Actor7.animate(drinkbottle)
                        "During this time, we also drink several beers one after the other.... and we also start to be tipsy!"
                        "My <Actor.boyfriend_or_girlfriend> winks at me and starts acting like a <Actor.fool_or_bimbo>, pretending to be a little bit too drunk..."
                        Actor.strip()
                        Actor.dressExcept(Outerwear, Top, Top_Under)
                        "Suddenly, my <Actor.boyfriend_or_girlfriend> takes off <Actor.his_or_her> <Actor.shirt_or_top> and asks one of my friends to write 'Go Team! on <Actor.his_or_her> <Actor.chest_or_breasts>."
                        Actor.moveToPersonStand(Actor6, 40)
                        If Actor6.isInterestedIn(Actor)
                            Actor6(Happy)::"Sure..."
                            Actor6:arousal += Random(0, 1)
                            GSHap = true
                        Else
                            Actor6(Annoyed)::"Ask <Actor7.name> instead..."
                            Ask7 = true
                        Endif
                        If Ask7 && Actor7.isInterestedIn(Actor)
                            Actor7(Happy)::"Sure..."
                            Actor7:arousal += Random(0, 1)
                            GSHap = true
                        Else
                            Actor7(Annoyed)::"Ask your <Player.boyfriend_or_girlfriend> instead..."
                            // bad start
                        Endif
                        If GSHap
                            "After while..."
                            "The moment the soccer team scores a goal, we stands up and screams with joy..."
                            "But in the meantime, my <Actor.boyfriend_or_girlfriend> strips completely naked and starts to masturbate as a sign of support for the team..."
                            Filter(Solo)
                            Sex(Actor)
                            Actor6:arousal += Random(0, 5)
                            Actor7:arousal += Random(0, 5)
                            If Actor6:arousal > 15
                                Actor6(Flirty)::"Hey man! I'll give money to fuck your <Actor.boyfriend_or_girlfriend>, can I?"
                                Actor(Flirty)::"No need to ask, just do it, babe! Show me your thick dick!"
                                Actor6.strip()
                                fuckedA = true
                                sexNoAffair(Actor6, Actor)
                                money += Random(50, 100)
                            Else
                                "Hmmm! yo, I think your <Actor.boyfriend_or_girlfriend> is a little too drunk... take <Actor.him_or_her> to your room, do something."
                                Actor6:arousal += Random(0, 5)
                                Actor7:arousal += Random(0, 5)
                                Actor6:perversion += Random(0, 5)
                            Endif
                            Actor.strip()
                            Actor.animate(masturbate)
                            If Actor7:arousal > 15
                                Actor7(Flirty)::"Hey <Player.name>! Same for me, I'll give money to fuck your <Actor.boyfriend_or_girlfriend>, can I too?"
                                Actor(Flirty)::"No need to ask, just do it, babe! Show me your thick dick!"
                                Actor7.strip()
                                fuckedB = true
                                sexNoAffair(Actor7, Actor)
                                money += Random(50, 100)
                            Else
                                "WTF, hey <Player.name>, your <Actor.boyfriend_or_girlfriend> is so drunk <Actor.he_or_she>'s masturbating...."
                                Actor6:arousal += Random(0, 5)
                                Actor7:arousal += Random(0, 5)
                                Actor7:perversion += Random(0, 5)
                            Endif
                            arousal += Random(5, 10)
                        EndIf
                        If fuckedA && fuckedB
                            Tfucked = true
                        EndIf
                        If Tfucked
                            "Let's extend the party and do a foursome..."
                            Sex(Actor6, Player, Actor, Actor7)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -4)
                            EndIf
                        Elseif fuckedA
                            "Let's extend the party and do a threesome..."
                            Sex(Actor6, Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                        Elseif fuckedB
                            "Let's extend the party and do a threesome..."
                            Sex(Actor7, Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                        EndIf
                    EndIf
                Else
                    Actor(Sad):: "Are you crazy! No way, maybe another time!"
                    Actor:mood -= 5
                    Actor:perversion += 1
                EndIf
                Actor6.hide()
                Actor7.hide()
            EndIf
        EndIf
    SceneEnd() // Scene improved by Raddeck
    timeout(180, sr_fso_hangoutdare3_cms)