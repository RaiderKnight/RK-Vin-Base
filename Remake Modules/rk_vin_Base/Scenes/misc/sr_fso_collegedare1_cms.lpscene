WHAT: all
WHERE: university:school, fraternity
WHEN: 0-24
WHO: Actor = getSpecific(Dating)
OTHER: perversion > 50 && arousal > 15 && isModEnabled(vin_NTR)

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.show()
        "While at college I thought about a terrible idea. What if I dared my <Actor.boyfriend_or_girlfriend> <Actor.name> to go into one of the empty college rooms nude and masturbate there?"
        0:: "Yeah this will be great idea!"
        1:: "Hmm, <Actor.he_or_she> might get caught...lets not."

        If 0
            If Actor:perversion > 50
                Actor.strip()
                "<Actor.name> agreed to my dare."
                Filter(Solo)
                Sex(Actor)
                If random(0, 100) < 33 && isModEnabled(vin_NonConsensual)
                    Actor2 = generatePersonTemporary()
                    While !Actor.isInterestedIn(Actor2)
                        Actor2 = generatePersonTemporary()
                    EndWhile
                    Actor2.show()
                    Actor2.strip()
                    "Unfortunately for <Actor.him_or_her>, <Actor.he_or_she> was caught and rapped by a student who passed by."
                    If Actor2.isMale() && !Actor.isMale()
                        Filter(AggressiveFM)
                    Else
                        Filter(Aggressive)
                    EndIf
                    TalkNonConsensual()
                    Sex(Actor2, Actor)
                    Actor:arousal -= random(20, 50)
                    Actor:mood -= random(20, 50)
                    Actor:perversion -= random(20, 50)
                ElseIf random(0, 100) < 50
                    "No-one saw what <Actor.he_or_she> was doing but eventually <Actor.he_or_she> said <Actor.he_or_she> enjoyed that but we should leave fast just in case."
                    Actor:perversion += random(2, 5)
                Else
                    Price = random(50, 200)
                    Price_loc = Price.convertToLocalCurrency()
                    "Unfortunately, <Actor.he_or_she> was caught by a university staff member and I have to pay <Price_loc> to shut the university staff up because <Actor.he_or_she> was threatened with expulsion."
                    money -= Price
                    Actor:mood -= random(2, 5)
                EndIf
            Else
                "<Actor.He_or_She> turned my idea down and actually got a bit angry at me for even suggesting it."
                Actor:rapportwithplayer -= random(0, 1)
                Actor:mood -= random(2, 5)
            EndIf
        EndIf
    SceneEnd()
    timeout(200, sr_fso_collegedare1_cms) // Scene improved by Raddeck