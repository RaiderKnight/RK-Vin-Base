WHAT: all, -pickpocket, -go_to_the_bathroom, -sleep, -sleep_hotel, -nap, -nap_hotel, -rk_VB_small_sleep
WHERE: home
WHEN: 0 - 24
WHO: Actor = getPerson(tag_incalling); If Actor.isDating()
OTHER: isModEnabled(vin_NTR)

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        GoldDigger = Actor.getActorVar(rk_VB_GoldDigger)
        Actor.dress()
        Actor.show()
        "My <Actor.boyfriend_or_girlfriend> is getting ready for an evening of escorting a high-paying client."
        "You're feeling horny, and watching <Actor.him_or_her> prepare has you really want to fuck <Actor.him_or_her>, but I know <Actor.he_or_she> don't want to be late."
        0:: "Try to propose sex"
        1:: "Leave <Actor.him_or_her> go to 'work'"
        
        If 0
            If masochist - Actor:masochist < -30
                Player(Evil)::"This other person is just a normal client, I'm a VIP client, and insist on a fuck before you go!"
                Player.strip()
                Actor(Annoyed)::"Ah okay, you're a real pain in the ass sometimes when you want to! But since I love you it's ok, but we have to be quick!"
                Actor.strip()
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                masochist -= Random(1, 3)
            ElseIf Actor:masochist - masochist < -30
                Price = 200
                Price_loc = Price.convertToLocalCurrency()
                Player(Flirty)"Let's have a quick one!"
                Actor(Annoyed)::"I'm gonna be late!"
                Player(Flirty)::"Stop it, you're an hour early!"
                Actor(Grin):: "You know what, fine, but you're paying for it. Same rate as other client (<Price_loc>), 4-hour minimum even though I'm still leaving. Is a quickie worth that to you?"
                0:: "Pay up"
                1:: "Turn down this bullshit deal"
                
                If 0
                    money -= Price
                    Player.strip()
                    Actor.strip()
                    Sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    Actor.modifyActorVar(rk_VB_GoldDigger, 1)
                Else
                    Player(Annoyed)::"Ok never mind, you're a real pain in the ass sometimes when you want to!"
                    arousal -= 1
                    Actor(Wink):: "Hey, I was just offering. Anyway, I have to go work!"
                    Actor:masochist -= 1
                EndIf
            ElseIf Actor:attractiontoplayer > 35 && Actor:rapportwithplayer > 35
                Actor(Flirty)::"Ok, but just a quicky!"
                Player(Happy)::"Cool!"
                Actor.strip()
                Player.strip()
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                Actor(Sad):: "Sorry babe, that was good, but I absolutely have no more time for you right now. I really need to go."
            Else
                Actor(Sad):: "Sorry, babe, but I just absolutely don't have time right now. I really need to go."
            EndIf
            Actor.hide()
        EndIf
    SceneEnd() // Scene improved by Raddeck
    timeout(300, ctf_jan02_cms)