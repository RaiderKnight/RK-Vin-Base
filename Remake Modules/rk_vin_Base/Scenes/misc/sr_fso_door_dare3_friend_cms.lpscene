WHAT: all, -sleep, -nap
WHERE: all, -prison
WHEN: 8 - 22
WHO: Actor = getSpecific(Dating); Actor1 = getSpecific(Dating_Friend); If Actor1.isInterestedIn(Player) && Actor:perversion > 50 && Actor1:perversion > 50
OTHER: isModEnabled(vin_NTR)

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.hide()
        If !Actor1.isValid()
            Actor1 = generatePersonTemporary()
            Actor1.addDatingFriend()
        EndIf
        "I just got a text from my <Actor.boyfriend_or_girlfriend>'s friend?"
        secondScreen(Actor1)
        Actor1.dress()
        Actor1.show()
        "<Actor1.name> just requested that I go to <Actor1.his_or_her> house. I have no idea why but <Actor1.he_or_she> said it'll be fun."
        secondScreen()
        Actor1.hide()
        "What should I do?"
        0:: "Let's visit <Actor1.him_or_her>"
        1:: "Don't go!"
        
        If 0
            setBackgroundCustom(livingroom)
            "When I arrived, I was received with a very interesting show."
            Actor1.show()
            Actor1.strip()
            Filter(Solo)
            Sex(Actor1)
            "<Actor1.name>, <Actor.name>'s friend, was alone in <Actor1.his_or_her> hallway, in the middle of an intense masturbation session and when <Actor1.he_or_she> saw me, <Actor1.he_or_she> said..."
            Actor1(Flirty)::"Stop looking and come play with my body dirty <Player.boy_or_girl>..."
            arousal += random(0, 3)
            "What should I say?"
            0:: "I'm sorry I can't do this to my <Actor.boyfriend_or_girlfriend>"
            1:: "Don't worry, I was waiting your invitation!"

            If 1
                perversion += random(0, 1)
                Player.strip()
                Sex(Player, Actor1)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                "After the fuck, we exchanged numbers and parted ways."
                If !Actor1.isContactExchanged()
                    exchangeContact(Actor1)
                EndIf
                Actor1:rapportwithplayer += random(0, 3)
                Actor1:attractiontoplayer += random(0, 3)
            EndIf
        Else
            karma += random(0, 1)
            "I told <Actor1.him_or_her> I had something more important to do and I couldn't go!"
            Actor1:mood -= 5
        EndIf
    SceneEnd() // Scene improved by Raddeck
    timeout(240, sr_fso_door_dare3_friend_cms)
