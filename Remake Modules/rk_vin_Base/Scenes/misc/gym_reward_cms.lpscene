WHAT: all
WHERE: home
WHEN: 0 - 24
WHO: Actor = getSpecific(Dating); If Actor:rapportwithplayer > 35
OTHER: Random(0, 100) > 75

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        moveTo(home)
        setBackground(home)
        Actor.dress()
        Actor.show()
        fitness += Random(1, 5)
        muscle += Random(1, 5)
        energy -= Random(19, 15)
        "After a hard workout I come home from the gym, greeted with a warm smile by my loving <Actor.boyfriend_or_girlfriend>."
        Actor(Flirty):: "Ohh baby, you are so beautiful when you are all sweaty! Have you been working out hard at the gym? "
        Player(Flirty)::"Yeah, that was a tough workout... my muscles hurt a bit!"
        Actor(Curious):: "You must be tired right now..."
        Player(Curious):: "Not that much, why?"
        Actor(Flirty):: "Mmm... you know what?..."
        Actor(Flirty):: "Why don't you.. just lay down on the bed for me ...?"
        Player.animate(na_sleep)
        Actor(Flirty)::"There's still one muscle you haven't trained and I'll take care of it!"
        Actor.strip()
        Player.strip()
        Actor(Flirty):: "Relax babe... let me just undress yourself... and give you a little reward for staying motivated..."
        Player(Flirty):: "Nice??"
        Actor(Flirty):: "What do you prefer?"
        0:: "A Blowjob"
        1:: "A Handjob"
        2:: energy > 25:: "A complete fuck"
        
        If 0
            If !Player.isMale()
                Filter(LickPussy)
            Else
                Filter(Blowjob)
            Endif
            Sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
        Elseif 1
            If !Player.isMale()
                Filter(Fingering)
            Else
                Filter(Handjob)
            Endif
            Sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
        Else
            Sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
        Endif
        Actor(Flirty):: "I'm so proud of you baby... I know it can be hard sometimes ... Just think about me then! I always got your back.. and I'll motivate you again... Any time you need it!"
        "With those words my <Actor.boyfriend_or_girlfriend> gives me another intimate kiss, before leaving me in my room"
        "<Actor.He_or_She> is really the best motivator ever!"
        Actor:attractiontoplayer += Random(0, 5)
        Actor:rapportwithplayer += Random(0, 5)
    SceneEnd() // Scene improved by Raddeck
    timeout(300, gym_reward_cms)