WHAT: all, -go_to_the_bathroom
WHERE: art 
WHEN: 12-20
WHO: none
OTHER: Player:Pornfame > 75 && Player:attractiveness > 75

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        SexAssure = false
        SexAssure1 = false
        SexAppend = false
        Actor1 = generatePersonTemporary()
        Argent = Random(150, 500)
        ArgentLoc = Argent.convertToLocalCurrency()
        Actor1:arousal += 5
        Actor1:perversion += 3
        Actor1.makeInterested(Player)
        "While looking at the artworks, a person approaches me and asks me..."
        Actor1(Happy)::"Hello, sorry to bother you like this but are you the famous <Player.name>?"
        Player(Curious)::"Yes, why?"
        Actor1(Happy)::"I am an artist sculptor and my passion is to make scultures of the most beautiful celebrities of sex and I would be very interested to make a sculture of your genitals."
        Actor1(Happy)::"I am willing to give you <ArgentLoc> in exchange for sculpting you!"
        Player(Curious)::"Okay??"
        "What I should say?"
        0::"Yeah sure!"
        1::"No way, your offer is too low!"
        2::"I'll do it for free!"
        3::"Are you crazy!"
        
        If 0
            SexAssure = true
        Elseif 1
            Actor1(Sad)::"I understand, maybe another time then, I wish you a good day!"
        Elseif 2
            SexAssure1 = true
        Else
            Player(Disgusted)::"You are crazy, I would never accept such an offer from a stranger! You are simply a retarded pervert..."
            Actor1(Sad)::"Sorry I can assure you I'm not an amateur and you're not the first person I've done a sculture to...."
            Player(Angry)::"Who cares!"
            Actor1(Sad)::"Anyway, maybe another time then, I wish you a good day!"
            Player(Angry)::"That's right, go away!"
        Endif

        If SexAssure || SexAssure1
            Actor1(Happy)::"Nice, let's go to my place for more privacy..."
            Player(Happy)::"Yeah sure!"
            passTime(0.10, 1)
            Random
                setBackground3D(Modules/rk_vin_Base/World/Vim_RoofOfficeBedroom.lpworld)
                setBackground3D(Modules/rk_vin_Base/World/DoctorBedroom.lpworld)
                setBackground3D(Modules/rk_vin_Base/World/girlroom.lpworld)
                setBackground3D(Modules/rk_vin_Base/World/rk_VB_Bedroom1.lpworld)
                setBackground3D(Modules/rk_vin_Base/World/Vim_RoofApartment.lpworld)
                setBackground3D(Modules/rk_vin_Base/World/YoungGRoom.lpworld)
            EndRandom
            "Once we arrived at <Actor1.his_or_her> place, we chatted for a while to relax a bit, and also to explain me how <Actor1.he_or_she> will proceed....."
            Actor1(Happy)::"You just have to undress completely so that I can start..."
            Player(Happy)::"Yeah sure!"
            Player.strip()
            Player.setNoRedress(true)
            Actor1(Happy)::"Nice, nice..."
            If !Player.isMale()
                Actor1(Happy)::"Now sit down and spread your legs wide so that I can see all the details of your vagina, your labia and your wonderful clitoris."
            Elseif Player.isMale()
                Actor1(Happy)::"Now sit down and spread your legs wide so that I can see all the details of your balls and your wonderful cock."
            Endif
            Player(Happy)::"Yeah sure!"
            Actor1:arousal += 5
            If Actor1:arousal > 15
                Actor1:perversion += 5
                Actor1(Flirty)::"Can you stroke yourself to show me what your <Player.cock_or_pussy> looks like when it is excited..."
                "What I should do?"
                0::"Accept"
                1::"Ask <Actor1.him_or_her> to help you"
                2::"Ask if <Actor1.he_or_she> want to taste it instead"
                99::"Refuse"
                
                If 99
                    Player(Sad)::"Sorry but you're going too far my friend!"
                    Actor1(Sad):: "That's ok! I'm almost done anyway..."
                    Actor1:arousal -= random(0, 5)
                Elseif 0
                    Filter(solo)
                    sex(Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                    Actor1(Happy)::"Nice, nice..."
                    Actor1:arousal += 5
                    SexAppend = true
                Elseif 1
                    Actor1(Happy)::"Sure..."
                    If !Player.isMale()
                        Filter(Fingering)
                        sex(Actor1, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf 
                    Elseif
                        Filter(Handjob)
                        sex(Actor1, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf 
                    Endif
                    SexAppend = true
                    Actor1:arousal += 5
                Else
                    Actor1(Happy)::"Are you kidding me? Sure..."
                    If !Player.isMale()
                        Filter(LickPussy)
                        sex(Actor1, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Elseif
                        Filter(Blowjob)
                        sex(Actor1, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Endif
                    SexAppend = true
                    Actor1:arousal += 5
                Endif
            Endif
            If Actor1:arousal > 25 && Actor1:perversion > 50
                Actor1(Flirty)::"Actually, do you want to fuck? I'm in love with your body!"
                "What I should do?"
                0::"Accept"
                99::"Refuse"
                
                If 0
                    Player(Happy)::"I'm always open for sex!"
                    Actor1.strip()
                    Actor1(Flirty)::"I'm ready to fuck ... or get fucked!"
                    sex(Actor1, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    SexAppend = true
                Else
                    Player(Sad)::"Sorry but that's going too far my friend!"
                    Actor1(Sad):: "That's ok! I'm almost done anyway..."
                    Actor1:arousal -= random(0, 5)
                Endif
            Endif
            Actor1(Happy)::"Well, that's it! A beautiful statue representing your penis in all its splendor!"
            Player(Happy)::"Wow, you are a great artist!"
            Actor1(Happy)::"It will be presented at my next exhibition..."
            Player(Happy)::"Cool!"
        Endif
        
        If SexAssure1
            Actor1(Happy)::"Thanks for everything, here is your money as promised. I really enjoyed the session, I wish you success."
            Player(Happy)::"Thank you! That was fun!"
            money += Argent
        Endif
        
        If SexAppend
            Argent2 = Random(20, 150)
            Argent2Loc = Argent2.convertToLocalCurrency()
            Actor1(Happy)::"Before leaving, here is a little extra for our sex session..."
            money += Argent2
            Player(Happy)::"Thanks!"
            Actor1(Curious)::"Would it be possible to leave your number, for other sex sessions between friends?"
            "What I should do?"
            0::"Accept"
            1::"Refuse"
            
            If 0
                Player(Happy):: "Of course, I'd love to exchange numbers."
                Actor1:attractiontoplayer += Random(0, 3)
                Actor1:rapportwithplayer += Random(0, 3)
                Actor1.makePermanent()
                exchangeContact(Actor1)
            Else
                Player(Sad):: "Sorry, but I don't normally exchange numbers with someone I just met."
                Actor:attractiontoplayer -= Random(0, 2)
                Actor:rapportwithplayer -= Random(0, 2)
            Endif
            Player.setNoRedress(false)
            Player.dress()
        Endif
        Actor1.hide()
    SceneEnd() // Created by Raddeck
    timeout(500, rk_VB_artsculting)