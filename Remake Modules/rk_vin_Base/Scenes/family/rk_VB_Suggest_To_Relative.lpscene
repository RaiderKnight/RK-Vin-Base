WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        
        If Actor.isInterestedIn(Player)
            "What I should suggest to <Actor.name>?"
            
            5:: "Propose to hired an escort for <Actor.him_or_her>"
            99::"I changed my mind"
    
            If 5
                passOn(Actor)
                followUp(call_a_hooker)
            EndIf
        Else
            "..."
        EndIf
    SceneEnd() // Scene improved by Raddeck