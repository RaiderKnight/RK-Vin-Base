WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        setBackground(bedroom)
        "As soon as I open the door of my room, I see the maid going through my stuff..."
        Actor = generatePersonTemporary()
        Actor.dress()
        Actor.show(2)
        Player(Angry):: "What on earth do you think you're doing? Thief!"
        "I can see that I had also forgotten my wallet on the table in the room."
        "I don't remember how much money I had inside, I hope she didn't steal anything..."
        continueStoryMS = false
        If Random(0, 100) < Actor:sneak*2
            Actor:karma -= 2.5
            Actor:sneak += Random(0, 0.5)
            CashT = Random(0, 200)
            CashTloc = CashT.convertToLocalCurrency()
            money -= CashT
            continueStoryMS = true
        EndIf
        If continueStoryMS
            If Random(0, 125) < Actor:interpersonal + Actor:attractiveness
                "<Actor.He_or_She> managed to bluff <Actor.his_or_her> way out of trouble in the end with <Actor.his_or_her> slick tongues. I'm still confused ..."
            Else
                "It's clear <Actor.he_or_she> stole from me but I don't know what..."
                "What I should do?"
                0::"Report to the manager"
                1::"Demand sex in exchange"
                2:: isModEnabled(vin_BDSM) || isModEnabled(vin_NonConsensual)::"Show who is the boss"
                99::"Do Nothing!"
                
                If 0
                    "Nothing <Actor.he_or_she> was telling me impressed me. I went straight to the hotel manager. <Actor.He_or_She> was fined heavily."
                    "Finally, <Actor.he_or_she> was lucky because the manager decided not to fire <Actor.him_or_her> because of this incident."
                    Actor.hide()
                    Actor.delete()
                Elseif 1 
                    Player(Flirty):: "Oh well ... if you undress and let me fucked you then I can consider not telling your manager all about this."
                    If !Actor.isInterestedIn(Player)
                        "<Actor.He_or_She> stormed off from the room. Calling creepy pervert ..."
                    Else
                        Actor(Flirty):: "I'm kinda short on money right now ... so I guess it's your lucky day. But don't think I'm the type that does this to all guests."
                        Actor.strip()
                        Player.strip()
                        Sex(Player, Actor)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        perversion += Random(0, 0.5)
                    EndIf
                    Actor.hide()
                    Actor.delete()
                Elseif 2
                    "Ok, big <Actor.guy_or_girl> today you made a big mistake and you're going to have to pay it back..."
                    "I lock the door and move to <Actor.him_or_her>..."
                    Player.moveToPerson(Actor)
                    Player(Evil):: "Now undress! I want to see your little <Actor.dick_or_cunt>..."
                    Actor.strip()
                    Player.strip()
                    Player(Evil):: "Good, now I will show you how I kill loser like you with my <Player.dick_or_pussy>!"
                    If Player.isMale() && !Actor.isMale()
                        Filter(AggressiveFM)
                    Else
                        Filter(Aggressive)
                    EndIf
                    TalkNonConsensual()
                    SexNoAffair(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -3)
                    EndIf
                    Actor.hide()
                    Actor.delete()
                EndIf
            EndIf
        EndIf
    SceneEnd() // Created by Raddeck