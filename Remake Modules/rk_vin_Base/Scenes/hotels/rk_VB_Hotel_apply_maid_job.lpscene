WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        PTypeClient = rk_VB_PTypeClient.getGlobal()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        MaidJob = rk_VB_setHotelMaidJob.getGlobal()
        tag_maidHotel = Player.getActorVar(rk_VB_tag_maidHotel)
        Hotescort = Player.getActorVar(rk_VB_tag_Hotescort)
        Iapplied = false
        MaPC = Player:age*365
        FirtsDJob = Player.getActorVar(rk_VB_FirtsDJobHotel)
        If MaidJob == 1
            "You already work at an hotel, no need to re-apply!"
        ElseIf Player.isMale()
            Actor = generatePersonTemporary()
            If !Actor.isMale()
                Actor.dress(French-Maid-Skirt_1_F, French-Maid-Stockings_1_F)
            Else
                Actor.dress()
            Endif
            Actor.show()
            Actor(Happy)::"Sorry, but we only accept female candidate for the moment!"
        Else
            "Payroll works by quotas, so the more rooms I clean, the bigger my paycheck will be at the end of the day."
            "Do I want to apply?"
            0::"Yes"
            1::"No"
            
            If 0
                If Player:attractiveness < 50
                    "I don't know why but they turned me down as a candidate!"
                Else
                    rk_VB_setHotelMaidJob.setGlobal(1)
                    Player.setActorVar(rk_VB_FirtsDJobHotel, MaPC)
                    Iapplied = true
                    If !Player.isMale()
                        Player.dress(French-Maid-Skirt_1_F, French-Maid-Stockings_1_F)
                    Else
                        Player.dress()
                    Endif
                    Player.setActorVar(rk_VB_tag_maidHotel, 1)
                    "They accepted my application right away, really happy! I can start anytime I want!"
                EndIf
            EndIf
            If Iapplied && Player:attractiveness > 55
                Actor = generatePersonTemporary()
                If !Actor.isMale()
                    Actor.dress(French-Maid-Skirt_1_F, French-Maid-Stockings_1_F)
                Else
                    Actor.dress()
                Endif
                Actor.show()
                Actor(Happy)::"If your interested to make even more money per day, we also have a position for a room escort!"
                "Do I want to apply?"
                0::"Yes"
                1::"No"
                
                If 0
                    // PTypeClient = rk_VB_PTypeClient.getGlobal()
                    Player.setActorVar(rk_VB_tag_Hotescort, 1)
                    Player(Excited)::"Sure! I love sex!"
                    "What kind of customer do I want?"
                    0::"Male only"
                    1::"Female only"
                    2::"Both"
                    3::"Trans"
                    
                    If 0
                        rk_VB_PTypeClient.setGlobal(1)
                    ElseIf 1
                        rk_VB_PTypeClient.setGlobal(2)
                    ElseIf 2
                        rk_VB_PTypeClient.setGlobal(3)
                    Else
                        rk_VB_PTypeClient.setGlobal(4)
                    EndIf
                Else
                    Player(Sad)::"Sorry, but that's not my type!"
                EndIf
            EndIf
        EndIf
    SceneEnd()