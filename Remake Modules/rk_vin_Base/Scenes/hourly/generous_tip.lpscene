WHAT: work_hourly
WHERE: bar, pub, nightclub, restaurant, cafe, coffee, hotel
WHEN: 0 - 24
WHO: none
OTHER: Random(0, 1000) < interpersonal

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        "A guest left me a very generous tip. It's good people like this that makes this job bearable."
        money += Random(5, 50)
        mood += 20
        
    SceneEnd() // Improved by Raddeck
    timeout(224, generous_tip)