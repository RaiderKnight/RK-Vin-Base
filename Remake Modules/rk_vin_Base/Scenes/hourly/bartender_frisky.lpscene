WHAT: work_hourly
WHERE: bar, nightclub, pub, biergarten
WHEN: 0 - 24
WHO: none
OTHER:

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor = generatePersonTemporary()
        While !Actor.isInterestedIn(Player)
            Actor = generatePersonTemporary()
        EndWhile
        Actor.dress()
        Actor.show()
        btip = Player:attractiveness/10
        tip = 5 + btip
        "One of the customers I serve seem clearly attracted to me and is really trying to make a conversation."
        If random(0, 100) < Actor:perversion
            Actor(Flirty):: "You make me so horny, darling. How about coming back home with me after work? I'll make it worth your while"
            0:: "Fuck the customer"
            1:: "Report to the bouncer"
            
            If 0
                setBackground(home)
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                money += 200
            Else
                "I am not a prostitute! I immediately went to the bouncer, who forcefully asked the frisky customer to leave."
            EndIf
        Else
            "Thankfully, the customer was well behaved and never harassed me in any way even when intoxicated, just some harmless flirting here and there. <Actor.He_or_She> even left me a big tip."
            money += tip 
        EndIf
    SceneEnd() // Improved by Raddeck
    timeout(150, bartender_frisky)