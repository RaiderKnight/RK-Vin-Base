WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        BoutikP = random(14, 16)
        BouP = false
        Doct = random(20, 40)
        Docteur = false
        Serv = random(17, 30)
        Services = false
        Atip = random(0, 5)
        BarP = false
        BarPub = random(14, 15) + Atip
        BoutikPL = BoutikP.convertToLocalCurrency()
        DoctL = Doct.convertToLocalCurrency()
        ServL = Serv.convertToLocalCurrency()
        AtipL = Atip.convertToLocalCurrency()
        BarPubL = BarPub.convertToLocalCurrency()
        If WHERE == art
            money += BoutikP
            BouP = true
        ElseIf WHERE == bag
            money += BoutikP
            BouP = true
        ElseIf WHERE == bakery
            money += BoutikP
            BouP = true
        ElseIf WHERE == bank
            money += Serv
            Services = true
        ElseIf WHERE == bar
            money += BarPub
            BarP = true
        ElseIf WHERE == beauty
            money += Serv
            Services = true
        ElseIf WHERE == beverages
            money += BarPub
            BarP = true
        ElseIf WHERE == bicycle
            money += BoutikP
            BouP = true
        ElseIf WHERE == biergarten
            money += BarPub
            BarP = true
        ElseIf WHERE == books
            money += BoutikP
            BouP = true
        ElseIf WHERE == brothel
            money += Serv
            Services = true
        ElseIf WHERE == butcher
            money += BoutikP
            BouP = true
        ElseIf WHERE == cafe
            money += BarPub
            BarP = true
        ElseIf WHERE == car
            money += Serv
            Services = true
        ElseIf WHERE == chemist
            money += Doct
            Docteur = true
        ElseIf WHERE == cinema
            money += BoutikP
            BouP = true
        ElseIf WHERE == clothes
            money += BoutikP
            BouP = true
        ElseIf WHERE == coffee
            money += BarPub
            BarP = true
        ElseIf WHERE == computer
            money += BoutikP
            BouP = true
        ElseIf WHERE == confectionery
            money += BoutikP
            BouP = true
        ElseIf WHERE == convenience
            money += BoutikP
            BouP = true
        ElseIf WHERE == copyshop
            money += BoutikP
            BouP = true
        ElseIf WHERE == cosmestics
            money += BoutikP
            BouP = true
        ElseIf WHERE == deli
            money += BarPub
            BarP = true
        ElseIf WHERE == department_store
            money += BoutikP
            BouP = true
        ElseIf WHERE == doityourself
            money += BoutikP
            BouP = true
        ElseIf WHERE == electronics
            money += BoutikP
            BouP = true
        ElseIf WHERE == fast_food
            money += BarPub
            BarP = true
        ElseIf WHERE == florist
            money += BoutikP
            BouP = true
        ElseIf WHERE == furniture
            money += BoutikP
            BouP = true
        ElseIf WHERE == garden_centre
            money += BoutikP
            BouP = true
        ElseIf WHERE == gift
            money += BoutikP
            BouP = true
        ElseIf WHERE == greengrocer
            money += BoutikP
            BouP = true
        ElseIf WHERE == hairdresser
            money += Serv
            Services = true
        ElseIf WHERE == hotel
            money += Serv
            Services = true
        ElseIf WHERE == ice_cream
            money += BoutikP
            BouP = true
        ElseIf WHERE == jewellery
            money += BoutikP
            BouP = true
        ElseIf WHERE == laundry
            money += BoutikP
            BouP = true
        ElseIf WHERE == marketplace
            money += BoutikP
            BouP = true
        ElseIf WHERE == mobile_phone
            money += BoutikP
            BouP = true
        ElseIf WHERE == motorcycle
            money += Serv
            Services = true
        ElseIf WHERE == musical_instrument
            money += BoutikP
            BouP = true
        ElseIf WHERE == newsagent
            money += Serv
            Services = true
        ElseIf WHERE == nightclub
            money += BarPub
            BarP = true
        ElseIf WHERE == optician
            money += Doct
            Docteur = true
        ElseIf WHERE == outdoor
            money += BoutikP
            BouP = true
        ElseIf WHERE == pet
            money += BoutikP
            BouP = true
        ElseIf WHERE == pharmacy
            money += Doct
            Docteur = true
        ElseIf WHERE == post_office
            money += Serv
            Services = true
        ElseIf WHERE == photo
            money += BoutikP
            BouP = true
        ElseIf WHERE == pub
            money += BarPub
            BarP = true
        ElseIf WHERE == restaurant
            money += BarPub
            BarP = true
        ElseIf WHERE == seafood
            money += BoutikP
            BouP = true
        ElseIf WHERE == shoes
            money += BoutikP
            BouP = true
        ElseIf WHERE == sports
            money += BoutikP
            BouP = true
        ElseIf WHERE == sports_centre
            money += Serv
            Services = true
        ElseIf WHERE == stationery
            money += BoutikP
            BouP = true
        ElseIf WHERE == stripclub
            money += BarPub
            BarP = true
        ElseIf WHERE == supermarket
            money += BoutikP
            BouP = true
        ElseIf WHERE == tea
            money += BarPub
            BarP = true
        ElseIf WHERE == theatre
            money += Serv
            Services = true
        ElseIf WHERE == tobacco
            money += BoutikP
            BouP = true
        ElseIf WHERE == toys
            money += BoutikP
            BouP = true
        ElseIf WHERE == travel_agency
            money += Serv
            Services = true
        ElseIf WHERE == variety_store
            money += BoutikP
            BouP = true
        ElseIf WHERE == veterinary
            money += Doct
            Docteur = true
        EndIf
        If isModEnabled(rk_vin_Base)
            If BouP
                "I received <BoutikPL> for the hour I worked."
            ElseIf BarP
                "I received <BarPubL> for the hour I worked and a <AtipL> tip."
            ElseIf Services
                "I received <ServL> for the hour I worked."
            ElseIf Docteur
                "I received <DoctL> for the hour I worked."
            EndIf
        EndIf
    SceneEnd() // created by Raddeck