WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        rk_VB_setCallerPics = rk_VB_setCallerPics.getGlobal()
        tag_RKCPIC = Actor.getActorVar(tag_RKCPIC)
        If HungerEnable > 0 && Hunger < 9
            "You need to eat something... You don't have enough energy!"
        ElseIf energy < 9
            "You don't have enough energy...."
        Else
            "Let's check my nude contact's pictures..."
            SearChA  = true
            While SearChA
                selectNPC(tag_RKCPIC)
                Actor = getSelectedNPC()
                //chooseContact()
                //Actor = getSpecific(chooseContact)
                RKCPIC = Actor.getActorVar(tag_RKCPIC)
                watchingA = true
                While watchingA
                    If RKCPIC > 1
                        "Let's take a look!"
                        Player.hide()
                        Actor.strip()
                        Actor.show()
                        99::"Done watching"
                        
                        If 99 
                            watchingA = false
                        EndIf
                        If watchingA
                            "Should I masturbate?"
                            0::"Yes"
                            1::"No"
                            
                            If 0
                                Player.show()
                                "So I started to masturbate slowly while watching <Actor.his_or_her> video..."
                                Filter(Solo)
                                sex(Player)
                                perversion += random(0, 0.25)
                                arousal += random(0, 1)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                            EndIf
                        EndIf
                    Else
                        "I can't find any pictures of <Actor.him_or_her>...."
                        watchingA = false
                    EndIf
                EndWhile
                "Should I check another contact's video?"
                0::"Yes"
                1::"No"
                
                If 1
                    SearChA  = false
                EndIf
            EndWhile
        EndIf
    SceneEnd()