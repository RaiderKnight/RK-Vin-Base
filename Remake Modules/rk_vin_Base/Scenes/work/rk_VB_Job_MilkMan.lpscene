WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Job_MilkMan = rk_VB_Job_MilkMan.getGlobal()
        MilkManSalary = Player.getActorVar(rk_VB_MilkManSalary)
        MilkManXP = Player.getActorVar(rk_VB_MilkManXP)
        ChoixduJour1 = false
        ChoixduJour2 = false
        ChoixduJour3 = false
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        JourTranq = false
        JourMieux = false
        colis = random(20, 100)
        Boni = random(0.25, 1)
        PourB = colis*Boni
        If random(0, 100) > 50
            JourMieux = true
        Else
            JourTranq = true
        EndIf
        echange = false
        Salaire = PourB + MilkManSalary + MilkManXP
        If JourMieux
            FaitJob = true
            While FaitJob
                clientMyst = generatePersonTemporary()
                clientMyst:arousal => random(20, 100)
                clientMyst:perversion => random(20, 100)
                clientMyst.dress()
                clientMyst.hide()
                If !ChoixduJour1 && random(0, 100) > 30
                    clientMyst.strip()
                    "I arrive at a customer's house and put the milk bottles next to his door... the door opened and a completely naked <clientMyst.man_or_woman> told me:"
                    clientMyst.show()
                    If !Player.isMale()
                        clientMyst(Flirty)::"Hi! I would like to taste your breast and pussy milk, if it is possible?"
                    Else
                        clientMyst(Flirty)::"Hi! I would like to have milk more concentrated, if it is possible?"
                    EndIf
                    Player(Curious)::"Sorry, what did you say?"
                    If !Player.isMale()
                        clientMyst(Flirty)::"I want to suck your tits and lick your pussy, I need fresh milk!"
                    Else
                        clientMyst(Flirty)::"I want to suck your dick, I need fresh milk!"
                    EndIf
                    "What should I say?"
                    0::"Accept the offer"
                    1::"Refuse the offer"
                    2::"Scold <clientMyst.him_or_her>"
                    
                    If 0
                        Player(Flirty)::"Of course! I'm always open for that kind of work break!"
                        perversion += 1
                        arousal += random(1, 3)
                        If !Player.isMale()
                            "As soon as I entered <clientMyst.his_or_her> house, I took off my clothes and <clientMyst.he_or_she> started sucking my breasts and my clit!"
                            Player.strip()
                        Else
                            "As soon as I entered <clientMyst.his_or_her> house, I pulled down my pants and <clientMyst.he_or_she> immediately started to suck my cock!"
                            Player.dressExcept(Bottom, Bottom_Under)
                        EndIf
                        "A real thirsty <clientMyst.guy_or_girl>!"
                        Filter(Oral)
                        Sex(clientMyst, Player)
                        clientMyst:attractiontoplayer += random(0, 3)
                        clientMyst:rapportwithplayer += random(0, 3)
                        Player.dress()
                        Player(Flirty)::"Thank you so much for this wonderful break but I have to get back to work!"
                        clientMyst(Flirty)"I understand!"
                        If !clientMyst.isContactExchanged()
                            echange = true
                        EndIf
                    ElseIf 1
                        Player(Serene)::"Sorry <clientMyst.sir_or_miss>, but I care about my job and I can't afford this while I'm working."
                        If !clientMyst.isContactExchanged()
                            clientMyst(Flirty)::"Okay, so leave me your number so you can come by later!"
                            "What should I do?"
                            0::"Accept"
                            1::"Refuse"
                            
                            If 0
                                clientMyst:attractiontoplayer += random(3, 10)
                                clientMyst:rapportwithplayer += random(1, 5)
                                "We exchanged phone numbers and I continued my rounds!"
                                clientMyst.makePermanent()
                                exchangeContact(clientMyst)
                            Else
                                Player(Sad)::"Sorry, but I'm not allowed to flirt with the customers. I can't accept your phone numbers!"
                                clientMyst(Flirty)::"Pff! Little sissy!"
                            EndIf
                        EndIf
                    Else
                        Player(Angry)::"Sorry <clientMyst.sir_or_miss>, but I care about my job and I will never fuck with a pervert like you!"
                        clientMyst(Annoyed)::"Pff! Little sissy!"
                    EndIf
                    ChoixduJour1 = true
                ElseIf !ChoixduJour2 && random(0, 100) > 30
                    clientMyst.strip()
                    "I arrive at a customer's house and put the milk bottles next to his door... the door opened and a completely naked <clientMyst.man_or_woman> told me:"
                    clientMyst.show()
                    clientMyst(Flirty)::"Hi baby, looks like you should take a break, in between I will give you a quick massage. Are you interested?"
                    "What should I say?"
                    0::"Accept the offer"
                    1::"Refuse the offer"
                    2::"Scold <clientMyst.him_or_her>"
                    
                    If 0
                        Player(Flirty)::"Of course! I'm always open for that kind of work break!"
                        perversion += 1
                        arousal += random(1, 3)
                        If !Player.isMale()
                            "As soon as I entered <clientMyst.his_or_her> house, I took off my clothes and <clientMyst.he_or_she> started fingering my pussy!"
                            Player.strip()
                            Filter(Fingering)
                        Else
                            "As soon as I entered <clientMyst.his_or_her> house, I took off my clothes and <clientMyst.he_or_she> immediately started to stroke my cock!"
                            Player.strip()
                            Filter(Handjob)
                        EndIf
                        sex(clientMyst, Player)
                        clientMyst:attractiontoplayer += random(0, 3)
                        clientMyst:rapportwithplayer += random(0, 3)
                        Player.dress()
                        Player(Flirty)::"Thank you so much for this wonderful break but I have to get back to work!"
                        clientMyst(Flirty)"I understand!"
                        If !clientMyst.isContactExchanged()
                            echange = true
                        EndIf
                    ElseIf 1
                        Player(Serene)::"Sorry <clientMyst.sir_or_miss>, but I care about my job and I can't afford this while I'm working."
                        If !clientMyst.isContactExchanged()
                            clientMyst(Flirty)::"Okay, so leave me your number so you can come by later!"
                            "What should I do?"
                            0::"Accept"
                            1::"Refuse"
                            
                            If 0
                                clientMyst:attractiontoplayer += random(3, 10)
                                clientMyst:rapportwithplayer += random(1, 5)
                                "We exchanged phone numbers and I continued my rounds!"
                                clientMyst.makePermanent()
                                exchangeContact(clientMyst)
                            Else
                                Player(Sad)::"Sorry, but I'm not allowed to flirt with the customers. I can't accept your phone numbers!"
                                clientMyst(Flirty)::"Pff! Little sissy!"
                            EndIf
                        EndIf
                    Else
                        Player(Angry)::"Sorry <clientMyst.sir_or_miss>, but I care about my job and I will never fuck with a pervert like you!"
                        clientMyst(Annoyed)::"Pff! Little sissy!"
                    EndIf
                    ChoixduJour2 = true
                ElseIf !ChoixduJour3 && random(0, 100) > 30
                    clientMyst.strip()
                    "I arrive at a customer's house and put the milk bottles next to his door... the door opened and a completely naked <clientMyst.man_or_woman> told me:"
                    clientMyst.show()
                    clientMyst(Flirty)::"Hi baby, looks like you should take a break, in between let's have some fun together. Are you interested?"
                    "What should I say?"
                    0::"Accept the offer"
                    1::"Refuse the offer"
                    2::"Scold <clientMyst.him_or_her>"
                    
                    If 0
                        Player(Flirty)::"Of course! I'm always open for that kind of work break!"
                        perversion += 1
                        arousal += random(1, 3)
                        "As soon as I entered <clientMyst.his_or_her> house, I took off my clothes and we started fucking!"
                        Player.strip()
                        sex(clientMyst, Player)
                        clientMyst:attractiontoplayer += random(0, 3)
                        clientMyst:rapportwithplayer += random(0, 3)
                        Player.dress()
                        Player(Flirty)::"Thank you so much for this wonderful break but I have to get back to work!"
                        clientMyst(Flirty)"I understand!"
                        If !clientMyst.isContactExchanged()
                            echange = true
                        EndIf
                    ElseIf 1
                        Player(Serene)::"Sorry <clientMyst.sir_or_miss>, but I care about my job and I can't afford this while I'm working."
                        If !clientMyst.isContactExchanged()
                            clientMyst(Flirty)::"Okay, so leave me your number so you can come by later!"
                            "What should I do?"
                            0::"Accept"
                            1::"Refuse"
                            
                            If 0
                                clientMyst:attractiontoplayer += random(3, 10)
                                clientMyst:rapportwithplayer += random(1, 5)
                                "We exchanged phone numbers and I continued my rounds!"
                                clientMyst.makePermanent()
                                exchangeContact(clientMyst)
                            Else
                                Player(Sad)::"Sorry, but I'm not allowed to flirt with the customers. I can't accept your phone numbers!"
                                clientMyst(Flirty)::"Pff! Little sissy!"
                            EndIf
                        EndIf
                    Else
                        Player(Angry)::"Sorry <clientMyst.sir_or_miss>, but I care about my job and I will never fuck with a pervert like you!"
                        clientMyst(Annoyed)::"Pff! Little sissy!"
                    EndIf
                    ChoixduJour3 = true
                ElseIf random(0, 100) > 60
                    FaitJob = false
                Else
                    FaitJob = false
                EndIf
                If echange
                    "Should I ask <clientMyst.him_or_her> to exchange our numbers?"
                    0::"Yes"
                    1::"No"
                    
                    If 0
                        clientMyst(Happy)::"Of course, here's my numbers..."
                        clientMyst:attractiontoplayer += random(3, 10)
                        clientMyst:rapportwithplayer += random(1, 5)
                        "We exchanged phone numbers and I continued my rounds!"
                        clientMyst.makePermanent()
                        exchangeContact(clientMyst)
                    EndIf
                EndIf
            EndWhile
        EndIf
        passTime(6, 8)
        If JourTranq
            "I finally finished my rounds, all the milk bottles were delivered, my customers were delighted to see me. It was a good day!"
            mood += random(0, 3)
            energy -= random(10, 15)
            money += Salaire
        EndIf
        If JourMieux || JourTranq
            Player.modifyActorVar(rk_VB_MilkManXP, 1)
        EndIf
    SceneEnd()