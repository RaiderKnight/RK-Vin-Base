WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        tagPolice = Policier.getActorVar(tag_Police)
        Boss_Sucker = Player.getActorVar(rk_VB_Boss_Sucker)
        SleepingWithBoss = SleepingWithCopBoss.getGlobal()
        Job_police = rk_VB_Job_police.getGlobal()
        PoliceXP = Player.getActorVar(rk_VB_PoliceXP)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        PSalaire = Player.getActorVar(rk_VB_PoliceSalary)
        PPID = rk_VB_PPatronID.getGlobal()
        PPatron = getSpecific(PPID)
        PPatron.dressUniform(police)
        PPatron.hide()
        If isModEnabled(rk_vin_Base)
            "Although the police station has a procedure in place to keep track of accumulated tickets, no one really checks."
            "It didn't take me long to realize that there were many holes to exploit in this procedure and that I could perhaps take advantage of them to make some quick money."
            "I suspect some of my colleagues are already doing it. Should I take this risk?"
            0:: "Yes"
            1:: "No"
            
            If 0
                "How can I turn down on money? What's life without risks?"
                "My clever feat earned me quite a bit of money, I failed to submit the tickets where people paid in cash!"
                karma -= 10
                If intelligence > random(0, 100)*random(0, 1)
                    money += random(200, 2000)
                    "However, I didn't go overboard in order to avoid suspicions and got away with it."
                    sneak += random(0, 2)
                Else
                    sneak -= 1
                    PPatron.show(2)
                    Player(Happy):: "<PPatron.Mr_or_Ms> <PPatron.name_last>, you called for me? Are there any issues? You look ... angry."
                    PPatron(Angry):: "I'll keep this short ... You're a thief and you're fired!"
                    PPatron.hide()
                    Player.setActorVar(rk_VB_PoliceSalary, 0)
                    Player.setActorVar(rk_VB_PoliceXP, 0)
                    rk_VB_PoliceXP.setGlobal(0)
                    setBackground(street)
                    "Not only did I lose my job, my reputation as a thief was passed from one police station to another. It's harder to get a decent job now as a police officer ..."
                    jobexperience -= random(0, 20)
                    mood -= 100
                    BossReallyPissed = random(0, 100)
                    If BossReallyPissed > 50
                        followUp(rk_VB_Steal_Arrest)
                    EndIf
                EndIf
            Else
                "I think I like my job and my integrity too much to steal."
                intelligence += 1
                "I endured the temptation and decide to works instead..."
                "Despite the pile of files I had to check and correct, I still had a great day!"
                energy -= random(15, 30)
                Player.modifyActorVar(rk_VB_PoliceXP, 1)
                intelligence += random(0.25, 2)
                jobperformance += random(0.25, 1)
                jobexperience += random(0.25, 1)
            EndIf
        EndIf
    SceneEnd()