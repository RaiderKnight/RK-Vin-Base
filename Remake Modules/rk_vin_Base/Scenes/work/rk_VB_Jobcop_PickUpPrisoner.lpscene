WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        cestBeau = false
        Job_police = rk_VB_Job_police.getGlobal()
        PoliceXP = Player.getActorVar(rk_VB_PoliceXP)
        PSalaire = Player.getActorVar(rk_VB_PoliceSalary)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        gfaitmajobcop = Player.getActorVar(rk_VB_gfaitmajobcop)
        PPID = rk_VB_PPatronID.getGlobal()
        Pris = generatePersonTemporary()
        Pris.randomizeFace()
        Pris.randomizeRace()
        Pris.randomizeHairs()
        Pris.dressUniform(prison)
        PPatron = getSpecific(PPID)
        PPatron.dressUniform(police)
        Pris.dressBondage(Handcuffs)
        setBackground(police)
        Pris.moveToPerson(PPatron)
        PPatron.show()
        Pris.show()
        "Once arrived at the police station, my boss is waiting for me with the prisoner at <PPatron.his_or_her> side."
        PPatron(Happy)::"So here you are at last! Take this prisoner to the nearest jail!"
        Player(Serene)::"10-4, boss!"
        PPatron.hide()
        Pris.moveToPerson(Player)
        "I take the prisoner in my car and head for the nearest prison..."
        If isModEnabled(rk_vin_Base)
            If Pris:perversion > 35 && Pris:arousal > 15 && Pris.isInterestedIn(Player)
                "While I am heading towards the prison, the prisoner calls out to me..."
                Pris(Flirty)::"Hey officer, I have a small favor to ask you before I go to jail!"
                "Do I listen to <Pris.his_or_her> suggestion?"
                0::"Let's see what <Pris.he_or_she> wants"
                1::"I tell <Pris.him_or_her> to shut the fuck up"
                
                If 0
                    "Okay, I'm listening and we'll see if it's possible!"
                    Pris(Flirty)::"Look, I'm going to be locked up for 10 years, so I'd like to get laid one last time before I go to jail!"
                    "Should I accept <Pris.his_or_her> offer?"
                    0::"Why not!"
                    1::"Hell no!"
                    
                    If 0
                        "I think you have a good idea, I think I have a few minutes free for a little break for sex!"
                        Pris(Sad)::"Oh yeah, you're a nice cop!"
                        perversion += 1
                        setBackground3D(Modules/rk_vin_Base/World/DarkAlley.lpworld)
                        "I park the car in an alley and make sure there is no one around."
                        "We got out of the car and started our sex session..."
                        Pris.strip()
                        Player.strip()
                        Sex(Player, Pris)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        Pris.dressUniform(prison)
                        Player.dressUniform(police)
                        Player(Happy)::"Ok, time to leave!"
                        Pris(Sad)::"Fine!"
                        cestBeau = true
                    Else
                        Player(Annoyed)::"Are you sick! I'll never fuck a prisoner in my life!"
                        Player(Grin)::"Now shut the fuck up, where I'm taking you, you'll make plenty of friends to fuck!"
                        Pris(Sad)::"Fine!"
                        cestBeau = true
                    EndIf
                Else
                    Player(Annoyed)::"Please remain silent, I am not interested in your stories!"
                    Pris(Sad)::"Fine!"
                    cestBeau = true
                EndIf
            Else 
                cestBeau = true
            EndIf
            If cestBeau
                "Everything went well, I had no problems during my route, the prisoner behaved well!"
                setBackground(prison)
                "Once arrived at the prison, the guards escorted <Pris.him_or_her> inside and then I went back to the station to make my report!"
                Pris.hide()
                Player.modifyActorVar(rk_VB_PoliceXP, 1)
                jobperformance += 1
                jobexperience += 1
            EndIf
            passTime(1, 3)
            Player.modifyActorVar(rk_VB_gfaitmajobcop, 1)
        EndIf
    SceneEnd()