WHAT: all
WHERE: work
WHEN: 0 - 24
WHO: none
OTHER: jobexperience > 50

    // IF your jobexperience is at least 50, your boss might occasionally ask you to interview potential new junior hires, who might try to bribe you with money, their number, their body or their SO's body to get hired
    
    SceneStart()
        teteux = false
        pasTeteux = false
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        AsuckMe = false
        IFckA = false
        IFckAF = false
        AHandJ = false
        ABlowJ = false
        AFckP = false
        AfuckOff = false
        "My boss is away on an office trip and since I'm relatively senior now, the boss has asked me to interview a recent college graduate for a low-paid and unimportant junior position."
        "So unimportant that the boss can't be bothered and I'm allowed the final say whether to hire the candidate as well."
        "I think I'm going to have some fun..."
        0:: "Interview the candidate"
        1:: "Let another senior colleague do the job"
        
        If 0
            Actor = generatePersonTemporary()
            Actor:age => random(21, 25)
            Actor.dress()
            secondScreen(Actor)
            Actor.show()
            "The candidate is called <Actor.name>."
            secondScreen()
            Actor2 = generatePersonTemporary()
            Actor2:age => random(21, 25)
            secondScreen(Actor2)
            Actor2.show()
            "As part of the interview process, I had to check <Actor.name>'s social media history for controversial posts. Nothing there, except finding out <Actor.he_or_she> has a friend sweetheart called <Actor2.name>."
            secondScreen()
            Actor.show()
            "Here is the candidate. Let's begin the interview."
            Actor(Happy)::"Hi!"
            Player(Serene)::"Please close the door behind you!"
            If Actor:intelligence > 80
                "It only took me a few minutes to figure out that <Actor.he_or_she> was a top candidate. There are not plenty of candidates like <Actor.him_or_her>, so let's give the kid a chance to learn even more."
                pasTeteux = true
            ElseIf Actor:intelligence < 79.8
                "It only took me a few minutes to figure out that <Actor.he_or_she> was a medium candidate. There are probably plenty of better candidates, still this is a junior position so it's not going to do much harm to give the kid a chance to learn."
                pasTeteux = true
            ElseIf Actor:intelligence < 50
                "It only took me a few minutes to figure out that <Actor.he_or_she> was a mediocre candidate at best. There are plenty of better candidates, still this is a junior position so it's not going to do much harm to give the kid a chance to learn."
                "One thing about <Actor.name> is that <Actor.he_or_she> seemed really desperate for this job, must have been rejected in all the other interviews, I reckon."
                Player(Sad):: "To be completely honest with you, <Actor.name>, I don't think you quite have what's required for this position."
                Actor(Sad):: "Please ... I really need this job ... It's terrible for my future to remain unemployed for long after graduation ... I'll do anything ..."
                teteux = true
            EndIf
            If pasTeteux
                "What I should do?"
                0:: "Reject <Actor.name>"
                2:: "Demand a bribe"
                3:: "Let me fuck you!"
                4:: "Let me fuck that hot college <Actor2.boyfriend_or_girlfriend> of yours!"
                
            ElseIf teteux
                "What I should do?"
                0:: "Reject <Actor.name>"
                1:: "Give <Actor.him_or_her> the job out of kindness"
                2:: "Demand a bribe"
                3:: "Let me fuck you!"
                4:: "Let me fuck that hot college <Actor2.boyfriend_or_girlfriend> of yours!"
                
            EndIf
            
            If 0
                Player(Sad):: "Sorry, but I've been entrusted to find the best candidate ... We'll call you if you get the job!"
                Actor(Happy)::"Cool! Thanks for the interview, I wish you gonna call me!"
                "You better dreaming..."
                Hired = false
            ElseIf 1
                Player(Serene):: "You know what ... I remember being in your shoes. Fine, it's only a junior position. There's no reason why you can't improve by learning on the job."
                Actor(Happy)::"Cool! Thanks for the job, I'm hurry to begin!"
                Actor:rapportwithplayer += 50
                Actor:attractiontoplayer += 25
                karma += 20
                "I felt like doing a good deed today ... Besides, there's no harm having a junior colleague who will be forever grateful for my help early on in their career. A useful ally in the workplace in the future perhaps."
                Hired = true
            ElseIf 2
                Player(Serene)::"Well, your resume is very good but there is one thing missing in it..."
                Actor(Curious)::"Oh yes, and what is that?"
                If !Actor.isMale() || Player.isMale()
                    Player(Serene)::"There's nothing that says if you handle a dick well..."
                ElseIf Actor.isMale() || !Player.isMale()
                    Player(Serene)::"There's nothing that says if you handle a pussy well..."
                EndIf
                Actor(Shocked)::"What??"
                Player(Grin)::"You heard right! ... If you want the job, you're going to have to get down on your knees and <Player.suck_or_lick> my beautiful fresh <Player.penis_or_pussy>!"
                If Actor.isInterestedIn(Player)
                    Actor(Embarrassed):: "I can't believe I'm doing this ... I guess I'm really desperate for this job..."
                    If Player.isMale()
                        Filter(Blowjob)
                    Else
                        Filter(LickPussy)
                    EndIf
                    Player.strip()
                    Sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    Player.dress()
                    Hired = true
                    Player(Grin)::"Congratulations, you are now hired!"
                    AsuckMe = true
                Else
                    Actor(Angry)::"Are you crazy! Never in my life! Keep your fucking job pervert!"
                    Actor.deletePerson()
                EndIf
            ElseIf 3
                Player(Serene)::"Well, your resume is very good but there is one thing missing in it..."
                Actor(Curious)::"Oh yes, and what is that?"
                If !Actor.isMale() || Player.isMale()
                    Player(Serene)::"There's nothing that says if you handle a dick well..."
                ElseIf Actor.isMale() || !Player.isMale()
                    Player(Serene)::"There's nothing that says if you handle a pussy well..."
                EndIf
                Actor(Shocked)::"What??"
                Player(Grin)::"You heard right! ... If you want the job, you're going to have to fuck with me!"
                If Actor.isInterestedIn(Player)
                    Actor(Embarrassed):: "I can't believe I'm doing this ... I guess I'm really desperate for this job..."
                    Actor.strip()
                    Player.strip()
                    Sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    Player.dress()
                    Hired = true
                    Player(Grin)::"Congratulations, you are now hired!"
                    IFckA = true
                Else
                    Actor(Angry)::"Are you crazy! Never in my life! Keep your fucking job pervert!"
                    Actor.deletePerson()
                EndIf
            ElseIf 4
                Player(Serene)::"Well, your resume is very good but there is one thing missing in it..."
                Actor(Curious)::"Oh yes, and what is that?"
                Player(Serene)::"Well, nothing tells me if your <Actor2.boyfriend_or_girlfriend> know how to fuck??"
                If Actor.isInterestedIn(Player)
                    Actor(Embarrassed):: "WTF! Sorry! Ok I think I got it, I'm going to try to ask my <Actor2.boyfriend_or_girlfriend>... I guess I'm really desperate for this job."
                    "After a while..."
                    CRandChoice = random(0, 100)
                    If CRandChoice > 50
                        Actor(Happy)::"My <Actor2.boyfriend_or_girlfriend> accepted your offer!"
                        Player(Happy)::"Good, hurry to see your <Actor2.boyfriend_or_girlfriend>!"
                        "I meet with the candidate <Actor2.boyfriend_or_girlfriend> in a hotel room later on."
                        setBackground(home)
                        Actor2.strip()
                        Player.strip()
                        Sex(Player, Actor2)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        IFckAF = true
                    Else
                        Actor(Sad)::"My <Actor2.boyfriend_or_girlfriend> didn't accepted your offer!"
                        Player(Grin)::"No good! So, what do you offer?"
                        CRandChoice1 = random(1, 3)
                        CRandChoice1 = CRandChoice1.round()
                        continuSto = true
                        If CRandChoice1 == 1
                            Actor(Curious)::"A handjob?"
                            AHandJ = true
                        ElseIf CRandChoice1 == 2
                            Actor(Curious)::"A blowjob?"
                            ABlowJ = true
                        ElseIf CRandChoice1 == 3
                            Actor(Curious)::"A fuck?"
                            AFckP = true
                        Else
                            AfuckOff = true
                            continuSto = false
                        EndIf
                        If continuSto
                            "What should I do?"
                            0::"Accept"
                            1::"Refuse"
                            
                            If 0
                                If AHandJ
                                    Player.strip()
                                    Filter()
                                    sex(Actor, Player)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    IFckAF = true
                                ElseIf ABlowJ
                                    Player.strip()
                                    Filter()
                                    sex(Actor, Player)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    IFckAF = true
                                ElseIf AFckP
                                    Actor.strip()
                                    Player.strip()
                                    sex(Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -2)
                                    EndIf
                                    IFckAF = true
                                EndIf
                            Else
                                Player(Sad):: "Sorry, but I've been entrusted to find the best candidate ... We'll call you if you get the job!"
                                Actor(Sad)::"Not cool! Ok fine!"
                                Player(Grin):: "Have a nice day!"
                                Hired = false
                            EndIf
                        EndIf
                        If AfuckOff
                            Actor(Sad)::"Forget it! I'll find another job somewhere else, goodbye!"
                            Player(Grin):: "Have a nice day!"
                            Hired = false
                        EndIf
                    EndIf
                Else
                    Actor(Angry)::"Are you crazy! Never in my life I will share my <Actor2.boyfriend_or_girlfriend>! Keep your fucking job pervert!"
                    Actor.deletePerson()
                EndIf
            EndIf
            If Hired
                "I recommended that <Actor.name> get hired for the position. HR sorted out the contract swiftly and soon <Actor.he_or_she> joined the company as my latest junior colleague."
                Actor.makePermanent()
                Actor.addColleague()
                Actor:jobexperience => 5
                If !Actor.isContactExchanged()
                    exchangeContact(Actor)
                EndIf
                If random(0, 100) < 50
                    "As with many college relationships, <Actor.name>'s relationship with <Actor.his_or_her> college sweetheart ended only a month into the new job."
                    If IFckA || AsuckMe || IFckAF
                        "Maybe I'm partly responsible for the breakup with that indecent arrangement earlier."
                    EndIf
                Else
                    "<Actor.name>'s relationship with <Actor.his_or_her> college sweetheart remained strong as they both found jobs in the same city."
                    Actor2.makePermanent()
                    addNpcRelationship(Dating, Actor, Actor2)
                    If IFckA || AsuckMe || IFckAF
                        "They must really be in love for the relationship to survive after the indecent arrangement with me earlier."
                    EndIf                
                EndIf
            EndIf
            If !hired
                "Should I keep her number for a future 'date'?"
                0::"Yes"
                1::"No"
                
                If 0
                    If !Actor.isContactExchanged()
                        exchangeContact(Actor)
                    EndIf
                EndIf
            EndIf
        Else
            "I let another colleague do the interview, who ended up rejecting the candidate anyway. I'm glad I didn't waste my time then."
        EndIf
        Actor.hide()
        Actor2.hide()
    SceneEnd()
    timeout(200, interview_bribe)