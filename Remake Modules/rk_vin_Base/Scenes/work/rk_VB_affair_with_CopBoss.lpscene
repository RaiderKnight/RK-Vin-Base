WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Boss_Sucker = Player.getActorVar(rk_VB_Boss_Sucker)
        SleepingWithBoss = SleepingWithCopBoss.getGlobal()
        Job_police = rk_VB_Job_police.getGlobal()
        PoliceXP = Player.getActorVar(rk_VB_PoliceXP)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        PPID = rk_VB_PPatronID.getGlobal()
        Boss = getSpecific(PPID)
        Boss.dressUniform(police)
        Boss.show()
        PColl = generatePersonTemporary() //getting caught
        PColl.randomizeFace()
        PColl.randomizeRace()
        PColl.randomizeHairs()
        PColl.hide()
        setBackground(police)
        If isModEnabled(rk_vin_Base)
            If SleepingWithBoss == 0
                "It's obvious from the way my boss looks at me that <Boss.he_or_she> likes me a lot. I've heard stories of ambitious employees willing to sleep with their bosses to gain a professional advantage."
                "But is this the road that I want to go down?"
                0:: "Yes"
                1:: "No"
                
                If 0
                    perversion += random(0, 2)
                    karma -= 5
                    "It's a competitive job market, I'll do whatever it takes to get ahead."
                    "So I decide to go directly to my boss to suggest my private services..."
                    If Boss:perversion > 50 && Boss:arousal > 15 && Boss.isInterestedIn(Player)
                        Boss(Flirty):: "You have no idea how much I have been waiting for you to offer me that since the day you started here!"
                        Player(Flirty):: "I'm glad to be of service ..."
                        jobperformance += random(0, 10)
                        Sex(Boss, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        Boss.show(2)
                        Boss(Flirty):: "That was amazing, babe. We must do it again soon, but we must wait until everyone else has left."
                        SleepingWithCopBoss.setGlobal(1)
                        Player.modifyActorVar(rk_VB_Boss_Sucker, 1)
                        Boss:attractiontoplayer += random(1, 2)
                        Boss:rapportwithplayer += random(1, 2)
                    ElseIf Boss.isInterestedIn(Player)
                        Boss(Flirty):: "I am flattered that you are interested in me but you are not my type and never sleep with my employees!"
                        Player(Sad):: "Oh, ok then ..."
                        mood -= 1
                        Boss:attractiontoplayer += random(-1, 2)
                        Boss:rapportwithplayer += random(-1, 2)
                    Else
                        Boss(Furious):: "Excuse me, have you lost your mind? First of all, you are not my type and I never sleep with my employees!"
                        Player(Sad):: "Sorry boss, I didn't mean to offend you!"
                        Boss(Furious):: "Get out of my office!"
                        Player(Sad):: "Oh, ok then ..."
                        Boss:attractiontoplayer -= random(0, 20)
                        Boss:rapportwithplayer -= random(0, 20)
                        mood -= 5
                    EndIf
                Else
                    "No, I will achieve my ambitions based on merits alone."
                    Boss:attractiontoplayer -= random(0, 20)
                    Player.modifyActorVar(rk_VB_Boss_Sucker, -1)
                EndIf
            Else
                "My boss is calling for me and we're the only two left in the office. It's obvious again what <Boss.he_or_she> wants ..."
                0:: "Yes"
                1:: "No"
                
                If 0
                    karma -= 1
                    "I can't risk jeopardizing my career by breaking our secret arrangement ... Of course, I'll give <Boss.him_or_her> what <Boss.he_or_she> wants again."
                    perversion += random(0, 0.2)
                    jobperformance += random(0, 4)
                    Sex(Boss, Player)
                    Boss.show(2)
                    Boss(Flirty)::"You were great, as usual. Keep doing what you've been doing and you'll be promoted in no time!"
                    Player.modifyActorVar(rk_VB_Boss_Sucker, 1)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                Else
                    "I just don't feel like it today. I excused myself out of my indecent duty. The boss was visibly upset."
                    jobperformance -= random(0, 2)
                    Player.modifyActorVar(rk_VB_Boss_Sucker, -0.25)
                EndIf
            EndIf
            Player.modifyActorVar(rk_VB_gfaitmajobcop, 1)
        EndIf
    SceneEnd()