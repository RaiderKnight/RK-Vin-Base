WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        SlaveofActor = rk_MN_SlaveofActor.getGlobal()
        "I decided to go back to my house!"
        moveTo(home)
    SceneEnd()