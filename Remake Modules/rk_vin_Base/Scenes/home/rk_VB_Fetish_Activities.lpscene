WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        likes_watersports = Player.getActorVar(rk_VB_likes_watersports)
        Alikes_watersports = Actor.getActorVar(rk_VB_likes_watersports)
        Alikes_watersports2 = Actor2.getActorVar(rk_VB_likes_watersports)
        WeGay = false
        ContiSto = true
        datingS = false
        WeLesbian = false
        LoveP = random(0, 5)
        LoveP2 = random(0, 1)
        Actor = getSpecific(Dating)
        If Actor.isValid()
            datingS = true
            TFetish = 0
            TFetish += Actor:likes_anal
            TFetish += Actor:likes_bondage
            TFetish += Actor:likes_oral
            TFetish += Actor:likes_older
            TFetish += Actor:likes_rough
            TFetish += Actor:likes_toys
            TFetish += Alikes_watersports
        EndIf
        "I have a crazy urge to make a fetish. Who do I have in mind to fuck with?"
        0:: datingS ::"Ask Dating"
        1::"Check in contact"
        99::"I changed my mind!"
        
        If 0
            If TFetish >= 150
                "I managed to convince <Actor.name> to join my fetish fantasy."
                Actor.show()
                Actor(Flirty)::"Sure, what do you want to do?"
                Actor.modifyActorVar(rk_VB_likes_watersports, LoveP)
            Else
                "I couldn't convince <Actor.name> to be part of my fetish fantasy."
                ContiSto = false
            EndIf
        ElseIf 1
            perversion += random(0, 1)
            selectNpc()
            Actor2 = getSelectedNpc()
            If Actor2.isValid()
                TFetish1 = 0
                TFetish1 += Actor2:likes_anal
                TFetish1 += Actor2:likes_bondage
                TFetish1 += Actor2:likes_oral
                TFetish1 += Actor2:likes_older
                TFetish1 += Actor2:likes_rough
                TFetish1 += Actor2:likes_toys
                TFetish1 += Alikes_watersports2
            EndIf
            If !Actor2.isValid()
                "I couldn't find someone to be part of my fetish fantasy."
                ContiSto = false
            ElseIf TFetish1 < 175
                "I couldn't convince <Actor2.name> to be part of my fetish fantasy."
                ContiSto = false
            ElseIf Actor2.isInterestedIn(Player)
                "I managed to convince <Actor2.name> to join my fetish fantasy."
                Actor2.show()
                Actor2(Flirty)::"Sure, what do you want to do?"
                Actor2.modifyActorVar(rk_VB_likes_watersports, LoveP)
            EndIf
        Else
            "Finally, it will be for later!"
            ContiSto = false
        EndIf
        If ContiSto
            Actor.modifyActorVar(rk_VB_likes_watersports, LoveP2)
            Actor2.modifyActorVar(rk_VB_likes_watersports, LoveP2)
            If datingS
                Guest = Actor
            Else
                Guest = Actor2
            EndIf
            "How should I dress?"
            0::"Fully naked"
            1::"Let me choose"
            99::"I'm ok like that!"
            
            If 0
                Player.setNoRedress(true)
            ElseIf 1
                openMenu(Inventory)
            EndIf
            If Guest.isFemale() && Player.isFemale()
                WeLesbian = true
            EndIf
            If Guest.isMale() && Player.isMale()
                WeGay = true
            EndIf
            "What I want to do?"
            0::"I want pee on my face"
            1::"I want to pee on someone face"
            2:: !WeLesbian ::"I want a dick sunday"
            3:: !WeLesbian || !WeGay ::"I want a pussy cream pie"
            4::Player.isMale()::"I want a footjob"
            5::Guest.isMale()::"I want to give a footjob to someone"
            99::"I changed my mind!"
            
            If 0
                Filter(Watersports)LoveP2
                sex(Guest, Player)
                Player.modifyActorVar(rk_VB_likes_watersports, LoveP2)
            ElseIf 1
                Filter(Watersports)
                sex(Player, Guest)
                Player.modifyActorVar(rk_VB_likes_watersports, LoveP2)
                If UrgeToPeeEnable
                    Player.modifyActorVar(rk_VB_UrgeToPee, -50)
                EndIf
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            ElseIf 2
                If Player.isFemale()
                    "I licks <Guest.his_or_her> cock until <Guest.he_or_she> cum and lets the cum flow out while teasing the tip of <Guest.his_or_her> penis with my tongue."
                    Filter(Licking)
                    sex(Player, Guest)
                    "Once the flow is finished, I starts sucking <Guest.his_or_her> flaccid, cum-coated penis until it becomes hard again and ejaculates in my mouth..."
                    Filter(Blowjob)
                    sex(Player, Guest)
                Else
                    "<Guest.name> licks my cock until I cum and lets the cum flow out while teasing the tip of my penis with <Guest.his_or_her> tongue."
                    Filter(Licking)
                    sex(Guest, Player)
                    "Once the flow is finished, <Guest.he_or_she> starts sucking my flaccid, cum-coated penis until it becomes hard again and ejaculates in <Guest.his_or_her> mouth..."
                    Filter(Blowjob)
                    sex(Guest, Player)
                EndIf
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
            ElseIf 3
                If !Player.isFemale() && !Guest.isMale()
                    "I start slowly to push my dick in <Guest.his_or_her> wet vagina and as I go along I increase the rhythm until I finally come inside <Guest.him_or_her>..."
                    sex(Player, Guest)
                    "Then I start to eat <Guest.his_or_her> slimy pussy until <Guest.he_or_she> ejaculates in my face..."
                    Filter(LickPussy)
                    sex(Player, Guest)
                ElseIf !Player.isMale() && Guest.isMale()
                    "<Guest.He_or_She> start slowly to push <Guest.his_or_her> dick in my wet vagina and as <Guest.he_or_she> go along <Guest.he_or_she> increase the rhythm until <Guest.he_or_she> finally come inside me..."
                    sex(Guest, Player)
                    "Then <Guest.he_or_she> start to eat my slimy pussy until I ejaculates in <Guest.his_or_her> face..."
                    Filter(LickPussy)
                    sex(Guest, Player)
                EndIf
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
            ElseIf 4
                Filter(Footjob)
                sex(Guest, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
            ElseIf 5
                Filter(Footjob)
                sex(Player, Guest)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
            Else
                "Finally, it will be later!"
            EndIf
        EndIf
        If Player.isNaked()
            Player.setNoRedress(false)
            Player.dress()
        EndIf
    SceneEnd()