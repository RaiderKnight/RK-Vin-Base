WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        personal_pics = Player.getActorVar(rk_VB_personal_pics)
        IBuyaPhone = Player.getActorVar(rk_VB_IBuyaPhone)
        If IBuyaPhone <= 0
            "You don't have any device to take a picture ... Go buy a phone at any shop!"
        Else
            Player.show()
            Player.animate(camera)
            mood += 0.01
            "What type of picture it was?"
            0::"Normal"
            1::"XXX"
            
            If 0
                stock_photo += 1
                "1 photo added to my personal collection"
            Else
                Player.modifyActorVar(rk_VB_personal_pics, 1)
                "1 photo added to my personal nude pictures collection"
            EndIf
        EndIf
    SceneEnd()