WHAT: none
WHERE: none
WHEN: 0-18
WHO: none
OTHER: none


    SceneStart()
        FriendF = getPerson(true)
        While FriendF.isMale()
            FriendF = getPerson(true)
        EndWhile
        FriendM = getPerson(true)
        While !FriendM.isMale()
            FriendM = getPerson(true)
        EndWhile
        FriendF.dress()
        FriendM.dress()
        FriendF.hide()
        FriendM.hide()
        Wparty = false
        Mparty = false
        Tparty = false
        Wparty1 = false
        Mparty1 = false
        Tparty1 = false
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        lafete = false
        If Player.isMale() && Player.isInterestedInWomen() && FriendM.isInterestedInWomen()
            Wparty = true
        EndIf
        If Player.isMale() && Player.isInterestedInMen() && FriendM.isInterestedInMen()
            Mparty = true
        EndIf
        If Player.isMale() && Player.isInterestedInTrans() && FriendM.isInterestedInTrans()
            Tparty = true
        EndIf
        If !Player.isMale() && Player.isInterestedInWomen() && FriendF.isInterestedInWomen()
            Wparty1 = true
        EndIf
        If !Player.isMale() && Player.isInterestedInMen() && FriendF.isInterestedInMen()
            Mparty1 = true
        EndIf
        If !Player.isMale() && Player.isInterestedInTrans() && FriendF.isInterestedInTrans()
            Tparty1 = true
        EndIf
        If !FriendF.isValid() && !FriendM.isValid()
            "You don't have any friends, go walk around the city and make some friends before you start this mini quest"
        Else
            lafete = true
        EndIf
        displayidea = false
        If lafete
            //male
            If Wparty || Mparty || Tparty 
                If isModEnabled(rk_vin_Base)
                    "While I'm in my room fooling around... I get a call from one of my friends <FriendM.name>."
                    Player.animate(call)
                    Player(Happy)::"Hey hi <FriendM.name>, how are you!"
                    secondScreen(FriendM)
                    FriendM.show()
                    FriendM(Happy)::"Yeah great, hey are you doing anything tonight?"
                    "What I should answer?"
                    0::"Nothing why?"
                    1::"I am busy, why?"
                    
                    If 0
                        FriendM(Happy)::"Are you in the mood to organize a party special with me?"
                        "What I should answer?"
                        0::"Sure why not!"
                        1::"Not interested!"
                        
                        If 0
                            FriendM(Happy)::"I had an idea, I'm coming to your place!"
                            Player(Happy)::"Ok, I'm waiting for you!"
                            displayidea = true
                        EndIf
                    EndIf
                    FriendM.hide()
                    secondScreen()
                EndIf
            EndIf
            //female
            If Wparty1 || Mparty1 || Tparty1
                If isModEnabled(rk_vin_Base)
                    "While I'm in my room fooling around... I get a call from one of my friends <FriendF.name>."
                    Player.animate(call)
                    Player(Happy)::"Hey hi <FriendF.name>, how are you!"
                    secondScreen(FriendF)
                    FriendF.show()
                    FriendF(Happy)::"Yeah great, hey are you doing anything tonight?"
                    "What I should answer?"
                    0::"Nothing why?"
                    1::"I am busy, why?"
         
                    If 0
                        FriendF(Happy)::"Are you in the mood to organize a special party with me?"
                        "What I should answer?"
                        0::"Sure why not!"
                        1::"Not interested!"
                        
                        If 0
                            FriendF(Happy)::"I had an idea, I'm coming to your place!"
                            Player(Happy)::"Ok, I'm waiting for you!"
                            displayidea = true
                        EndIf
                    EndIf
                    FriendF.hide()
                    secondScreen()
                EndIf
            EndIf
        EndIf
        If displayidea
            passTime(0.5, 1)
            "After a while, my friend finally arrived... !"
            If Wparty || Mparty || Tparty
                Amis = FriendM
                txta = "girls"
                txtb = "guys"
            Else
                Amis = FriendF
                txta = "guys"
                txtb = "girls"
            EndIf
            Amis.show()
            Amis(Happy)::"Hey, we could have a party and only invite <txta>, and make them think we don't understand why no <txtb> showed up."
            Player(Happy)::"That's an idea!"
            Amis(Happy)::"Yes, and then we could get them drunk and then fuck them easier, at least touch them!"
            "What I should do?"
            0::"Organize the party"
            1::"Not interested!"   
            
            If 0
                arousal += random(1, 2)
                perversion += random(1, 2)
                Amis:rapportwithplayer += random(1, 2)
                Amis:attractiontoplayer += random(1, 2)
                
                
            Else
                arousal -= random(1, 2)
                perversion -= random(1, 2)
                Player(Sad)::"Finally, I'm not convinced that this is a good idea or I'm just in the mood for this! I'll pass for now!"
                Amis(Sad)::"Ah, you are annoying, I was counting on you!"
                Player(Serene)::"Don't be offended!"
                Amis(Happy)::"No, it's okay, I'll call someone else!"
                Amis:rapportwithplayer += random(-1, 2)
                Amis:attractiontoplayer += random(-1, 2)
                Amis.hide()
                "I may have missed an opportunity to fuck some girls, but ..., I have other things to do!"
            EndIf
        EndIf
    SceneEnd()