WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        personal_vids = Player.getActorVar(rk_VB_personal_vids)
        normal_vids = Player.getActorVar(rk_VB_normal_vids)
        IBuyaComputer = Player.getActorVar(rk_VB_IBuyaComputer)
        IBuyaPhone = Player.getActorVar(rk_VB_IBuyaPhone)
        If IBuyaComputer <= 0 && IBuyaPhone <= 0
            "You don't have any device to make a video... Go buy a phone at any shop!"
        ElseIf IBuyaComputer >= 1 && IBuyaPhone <= 0 && WHERE != home
            "You can't use your computer in this location... Go buy a phone at any shop!"
        Else
            Player.show()
            Player.animate(camera)
            mood += 0.05
            "What type of video it was?"
            0::"Normal"
            1::"XXX"
            
            If 0
                Player.modifyActorVar(rk_VB_normal_vids, 1)
                "1 video added to my personal collection"
            Else
                Player.modifyActorVar(rk_VB_personal_vids, 1)
                "1 video added to my personal nude video collection"
            EndIf
        EndIf
    SceneEnd()