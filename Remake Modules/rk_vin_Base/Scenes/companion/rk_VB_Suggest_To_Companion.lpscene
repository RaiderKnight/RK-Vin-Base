WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Dating = getSpecific(Dating)
        Actor3 = getCompanion()
        "What I should suggest?"
        50:: isModEnabled(sbrk_BeautyExpansion)::"Offer <Actor3.him_or_her> a beauty treatment"
        0:: isModEnabled(vin_NTR) ::"A special trip to a swinger club"
        1:: isModEnabled(vin_Netori) ::"Go to a swinger club and pretend to be a married couple"
        5:: Player.isDating() && isModEnabled(vin_NTR) && isModEnabled(vin_Pregnancy) ::"Suggest Breeding Party to Companion"
        29:: isModEnabled(vin_NTR) ::"Send a XXX picture of <Actor3.him_or_her> to one of <Actor3.his_or_her> friends"
        30:: isModEnabled(vin_NTR) ::"Send a XXX video of <Actor3.him_or_her> to one of <Actor3.his_or_her> friends"
        99::"I changed my mind"

        If 0
            followUp(swinger_club)
        ElseIf 1
            followUp(swinger_club_cheat)
        ElseIf 5
            followUp(breeding_party_companion_cms)
        ElseIf 29
            followUp(sr_fso_phone_dare1_cms)
        ElseIf 30
            followUp(sr_fso_phonedare2_cms)
        ElseIf 50
            "What I should propose?"
            0::"Get beauty treatment"
            1::"Get fake eyelashes"
            2::"Get a haircut"
            3::"Get a new haircolor"
            4::"Get a new hairstyle"
            5::"Get fake nails"
            6::"Get plastic surgery"
            7::"Get a waxing"
            8::"Whiten teeth"
            99::"I changed my mind"
            
            If 0
                "So I decided to take <Actor3.name> to the beauty salon to get <Actor3.him_or_her> beauty treatment..."
                followUp(sb_companion_beauty_treatment)
            ElseIf 1
                "So I decided to take <Actor3.name> to the beauty salon to get <Actor3.him_or_her> fake eyelashes..."
                followUp(sb_companion_eyelashes)
            ElseIf 2
                "So I decided to take <Actor3.name> to the hairdresser for a new haircut..."
                followUp(sb_companion_haircut)
            ElseIf 3
                "So I decided to take <Actor3.name> to the hairdresser for a new haircolor..."
                followUp(sb_companion_haircolor)
            ElseIf 4
                "So I decided to take <Actor3.name> to the hairdresser for a new hairstyle..."
                followUp(sb_companion_hairstyle)
            ElseIf 5
                "So I decided to take <Actor3.name> to the beauty salon to get <Actor3.him_or_her> fake nails..."
                followUp(sb_companion_nails)
            ElseIf 6
                "So I decided to take <Actor3.name> to the hospital for a plastic surgery..."
                followUp(sb_companion_plastic_surgery)
            ElseIf 7
                "So I decided to take <Actor3.name> to the beauty salon for a waxing..."
                followUp(sb_companion_waxing)
            ElseIf 8
                "So I decided to take <Actor3.name> to the dentist to get <Actor3.his_or_her> teeth whitened"
                followUp(sb_companion_whiten_teeth)
            EndIf
        EndIf
    SceneEnd() // Scene created by Raddeck