WHAT: all, -pickpocket, -go_to_the_bathroom, -sleep, -sleep_hotel, -nap, -nap_hotel, -rk_VB_small_sleep
WHERE: all
WHEN: 0 - 24
WHO: Actor = getCompanion(); If !Player.isInterestedIn(Actor) && !Player.isDating()
OTHER:  

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "While hanging out with <Actor.name>, knowing that I'm single, <Actor.he_or_she> suggests that I go on a blind date with one of <Actor.his_or_her> single friends."
        0:: "Yes"
        1:: "No"
        
        If 0
            Actor.hide()
            setBackground(restaurant)
            Actor:rapportwithplayer += random(1, 2)
            Actor2 = generatePersonTemporary()
            While !Player.isInterestedIn(Actor2)
                Actor2 = generatePersonTemporary()
            endWhile
            Actor2.makeInterested(Player)
            Actor2.dress()
            Actor2.show()
            "Looks like my date's name is <Actor2.name> ..."
            0:: "Be rough"
            1:: "Be nice and flirt"
            2:: "Run away"
            
            If 0
                "Ew, this is why blind dates are such a bad idea. <Actor2.name> is absolutely terrible-looking and not my type at all."
                "And I have no troubles telling <Actor2.him_or_her> that ..."
                "I even decided to end the date early and go home ..."
                Actor2.hide()
                
                If random(20, 50) < Actor2:attractiontoplayer
                    setBackgroundCustom(livingroom)
                    "When I arrived home though, I noticed that the front door lock has been picked open ...?"
                    Actor2.strip()
                    "As I carefully open the door, my blind date, now completely naked, was waiting for me there."
                    Actor2.show()
                    Actor2(Flirty):: "I'm sorry if I break into your apartment..."
                    Actor2(Flirty):: "I can't stop thinking about you... I think I fell in love with you at first sight..."
                    Actor2(Flirty):: "Come and fuck me!"
                    "What should I do?"
                    0:: "Kick this crazy <Actor2.man_or_woman> out"
                    1:: "Well, why not!"
                    
                    If 0
                        Player:: "Get the fuck out, right now, or I'll call the cops!"
                        Actor2(Flirty):: "Ok, Ok! I'm leaving!"
                        Actor2:rapportwithplayer -= random(0, 3)
                        Actor2.hide()
                    Else
                        "As much as I don't find <Actor2.name> even a tiny bit attractive ... I can't turn down such easy sex."
                        Player.strip()
                        Sex(Player, Actor2)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                    EndIf
                EndIf
            ElseIf 1
                "<Actor2.name> is quite hot actually and I tried my best to show <Actor2.him_or_her>. We got along very well and things started to flirt."
                setBackground(home)
                "Our blind date went so well that we eventually ended up in <Actor2.his_or_her> bedroom."
                Player.strip()
                Actor2.strip()
                Sex(Player, Actor2)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            Else
                "WTF, this is why blind dates are such a bad idea. <Actor2.name> is absolutely terrible-looking and not my type at all."
                Actor2:attractiontoplayer -= random(0, 1)
                Actor2:rapportwithplayer -= random(0, 3)
            EndIf
            Actor.show()
        Else
            Player(Sad)::"Sorry but I'm not interested!"
            Actor(Happy)::"That's fine, maybe another time!"
            Actor:rapportwithplayer -= random(0, 1)
        EndIf
    SceneEnd() // Improved by Raddeck
    timeout(100, blind_date_cms)