WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        
        "To us orcs, having a lot of children to maintain the Orc bloodline is an important duty. As such, unlike other races who go through the whole dating and marriage ordeal, whenever an Orc male and an Orc female meet, we skip straight to the breeding."
        "Of course, since making Orc babies is the number one priority, we don't bother with condoms, pills and other crap that the humans came up with."
        "<Actor.name> here is a fellow Orc who looks like <Actor.he_or_she> could be a suitable mate to produce an Orc baby. Should we fuck?"
        0:: "Yes"
        1:: "No"
    
        If 0
            Actor.Strip()
            Player.Strip()
            Player(flirty):: "Let get started then."
            Actor:perversion += 45
            Actor:likes_vaginal += 100
            Actor:attractiontoplayer += 10
            Filter(vaginal)
            Sex(Player, Actor)
            // Start edit by Raddeck
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
            
            // End edit by Raddeck
            Player(flirty):: "Want to keep going?"

        Endif
        
    SceneEnd() // Improved by Raddeck