WHAT: all
WHERE: university:school
WHEN: 8 - 20
WHO: none
OTHER: random(0, 100) < fitness && EnrollDegree >= 1

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "There's a flyer asking for volunteers to take part in a clinical trial for a university research. I will be paid some pocket money for my troubles."
        0:: "Yes"
        1:: "No"
        
        If 0
            Actor = generatePersonTemporary()
            Actor.dressUniform(doctor)
            Actor.show()
            Actor(Happy)::"Thanks for your service, that's really appreciated!"
            money += random(15, 75)
            fitness -= random(0, 0.5)
            If Actor.isInterestedIn(Player)
                Actor(Flirty)::"I'm wondering if you, because I'm little impress by your body... I'm mean your stamina!"
                Actor(Flirty)::"Is it possible for you to strip and do some stretching for me? I would like to check your muscles when your moving."
                Actor(Flirty)::"I'll give you an extra for my personal experience!"
                "What should I do?"
                0:: "Accept"
                1:: "Refuse"
                
                If 0
                    Player(Flirty)::"Why not!"
                    Player.strip()
                    Player.setNoRedress(true)
                    Actor(Flirty)::"Can you make some moves?"
                    Player(Flirty)::"Sure!"
                    Player.animate(dance)
                    Actor(Flirty)::"Nice, really nice!"
                    Actor.strip()
                    Actor.moveToPersonStand(Player, 40)
                    Actor(Flirty)::"You're so fucking nice that I'm gonna fuck you right here little slut! I'll give a good bonus!"
                    Player.animate()
                    If Player.isFemale()
                        Filter(Twerking)
                        Sex(Player)
                    EndIf
                    If Actor.isMale()
                        Filter(Standing)
                    EndIf
                    Sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf 
                    Actor(Grin)::"I think it's enough for today!"
                    Actor(Evil)::"Here's your money and don't even think to talk about what happen here or you will see!"
                    GBonus = 500
                    GBonusloc = GBonus.convertToLocalCurrency()
                    money += GBonus
                    Player.setNoRedress(false)
                Else
                    Player(Annoyed)::"I think it's enough for today!"
                    Actor(Sad)::"Fine! Have a nice day!"
                EndIf
            EndIf
        EndIf
    SceneEnd() // Improved by Raddeck
    timeout(500, clinical_trial)