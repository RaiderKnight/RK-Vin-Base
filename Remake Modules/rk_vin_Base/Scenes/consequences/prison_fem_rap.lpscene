WHAT: all
WHERE: prison
WHEN: 0 - 24
WHO: none
OTHER: random(0, 100) < perversion

    SceneStart()
        perdu = true
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor = generatePersonTemporary()
        While !Actor.isSameGender(Player)
            Actor = generatePersonTemporary()
        EndWhile
        Actor.show(2)
        Actor.dressUniform(Prison)
        Player(Scared)::"WTF, you came from where, I never heard you entered?"
        Actor(Evil):: "Shut up! I'm horny, let's fuck, bitch!"
        Player(Scared)::"What??"
        "Then <Actor.he_or_she> grabbed me from behind, <Actor.his_or_her> <Actor.cock_or_tits> was already hard ..."
        Actor(Evil):: "I said Shut up! Spread your legs, I want to <Actor.suck_or_lick> your <Actor.cock_or_pussy>!"
        0:: "Struggle"
        1:: "Accept your fate"
        
        If 0 && Actor:martial < martial
            Actor(Pain):: "Ah!"
            Player(Angry):: "That should teach you!"
            perdu = false
        Else
            perdu = true
        EndIf
        If perdu
            "The element of surprise was too great. I was overwhelmed before I could even react to the assault."
            Actor.strip()
            Player(Crying):: "No! Please stop ... I beg you ... Somebody help ..."
            "My cries for help went unheard in the dark prison cell and I had no choice but endure it all."
            Player.strip()
            Filter(Aggressive)
            TalkNonConsensual()
            SexNoAffair(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -3)
            EndIf
            Actor.setActorVar(tag_Rapist, 1)
            Actor.hide()
            "Despite the horrible incident that happened, I didn't have the guts to report the crime to the warden."
            "I figure they'll just laugh at my face. Here in a prison, rape is the language of power."
        EndIf
    SceneEnd()
    timeout(300, prison_fem_rap)