WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        preferentialtreatment = Player.getActorVar(rk_VB_preferentialtreatment)
        BadSexInPrison = Player.getActorVar(rk_VB_BadSexInPrison)
        TotalSentences = Player.getActorVar(rk_VB_TotalSentences)
        DateofSentences = Player.getActorVar(rk_VB_DateofSentences)
        Actor = generatePersonTemporary()
        Actor.dressUniform(Police)
        Actor.show(2)
        Actor1 = generatePersonTemporary()
        Actor1.dressUniform(Police)
        Actor1.show(4)
        "I served my sentence. Today is my release day. It's about time...!"
        sentence => 0
        Player.setActorVar(rk_VB_TotalSentences, 0)
        Player.setActorVar(rk_VB_DateofSentences, 0)
        If perversion > 50
            "First thing I'm gonna do is to eat something fat somewhere in a restaurant, get some booz and finish the day with a fuck or something!"
            arousal += 20
        Else
            "First thing I'm gonna do is to visit my family, get a good meal with glasses of wine and maybe hang out somewhere after!"
        EndIf
        If HungerEnable > 0
            Player.modifyActorVar(rk_VB_Hunger, -15)
        EndIf
        "I look at the guards and tell them..."
        If BadSexInPrison > 0
            Player(Happy)::"So..., bye, I hope not to come back here again, without wanting to offend you!"
        Else
            Player(Sarcastic)::"See you soon, I had a great time ..."
        EndIf
        Actor1.moveToPersonStand(Player, 50)
        Actor.moveToPersonStand(Player, 100)
        Actor(Serene)::"Ok, ok big <Player.boy_or_girl>, MOVE ON!"
        Actor1.hide()
        Actor.hide()
        setBackground(street)
        "That's it. I'm a free <Player.man_or_woman> again!"
        mood += 10
        Player.setActorVar(rk_VB_BadSexInPrison, 0)
        Player.setActorVar(rk_VB_preferentialtreatment, 0)
        endPrison()
        
    SceneEnd() // scene improved by Raddeck