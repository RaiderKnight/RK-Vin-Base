WHAT: all
WHERE: university:work
WHEN: 8 - 18
WHO: Actor = getPerson(rk_VB_tag_Prof); If Actor:rapportwithplayer > 10
OTHER: none

    SceneStart()
        argents = random(20, 50)
        argentsloc = argents.convertToLocalCurrency()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If isModEnabled(rk_vin_Base)
            dressFormal()
            Actor.dress()
            Actor.show(2)
            Actor.moveToPersonStand(Player, 50)
            Actor(Happy):: "<Player.name>, could you perhaps cover for me for the <Actor.Major> class later today? I've got to run. Something urgent popped up!"
            0:: "Yes"
            1:: "No"
            2::"Deal"
            
            If 0
                Player(Happy):: "Of course, I'll be happy to help."
                Actor:rapportwithplayer += random(0, 2.5)
                karma += 2.5
            ElseIf 2
                Player(Flirty)::"Only if you are willing to..."
                0::"To give me <argentsloc>"
                1::"<Player.Suck_or_Lick> my <Player.penis_or_vulva> in exchange"
                2::"Caress my <Player.cock_or_clit> in exchange"

                If 0
                    Actor(Annoyed)::"Ok then here the money!"
                    money += argents
                    Actor(Annoyed)::"You are really a crook, you know that!"
                    Player(Grin)::"It helps not to abuse my services my dear, and I have to live well after all!"
                    Actor:rapportwithplayer += random(-2, 1)
                    Actor:attractiontoplayer += random(-2, 1)
                ElseIf 1
                    Actor(Annoyed)::"Ok then! Quickly, show me your <Player.cock_or_pussy>, so I can <Player.suck_or_lick> it! Because I have to leave right away."
                    Actor(Annoyed)::"You are really a crook, you know that!"
                    Player(Grin)::"It helps not to abuse my services my dear, and I need to have fun sometime after all!"
                    Actor:rapportwithplayer += random(-1, 2)
                    Actor:attractiontoplayer += random(-1, 2)
                    Actor:arousal += random(0, 1)
                    Actor:perversion += random(0, 1)
                    Player.dressExcept(Bottom, Bottom_Under)
                    If !Player.isMale()
                        Filter(LickPussy)
                    Else
                        Filter(Blowjob)
                    EndIf
                    Sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Else
                    Actor(Annoyed)::"Ok then! Quickly, show me your <Player.cock_or_pussy>, so I can <Player.suck_or_lick> it! Because I have to leave right away."
                    Actor(Annoyed)::"You are really a crook, you know that!"
                    Player(Grin)::"It helps not to abuse my services my dear, and I need to have fun sometime after all!"
                    Actor:rapportwithplayer += random(-1, 2)
                    Actor:attractiontoplayer += random(-1, 2)
                    Actor:arousal += random(0, 1)
                    Actor:perversion += random(0, 1)
                    Player.dressExcept(Bottom, Bottom_Under)
                    If !Player.isMale()
                        Filter(FingerPussy)
                    Else
                        Filter(Handjob)
                    EndIf
                    Sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                EndIf
                If Actor.isInterestedIn(Player) && Actor:arousal > 15 && Actor:perversion > 70
                    Actor(Flirty)::"Then quickly, show me your <Player.cock_or_pussy>, so I can <Player.suck_or_lick> it! Because I have to leave right away."
                    Player.dressExcept(Bottom, Bottom_Under)
                    If !Player.isMale()
                        Filter(LickPussy)
                    Else
                        Filter(Blowjob)
                    EndIf
                    Sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                    Actor:rapportwithplayer += random(0, 2)
                    Actor:attractiontoplayer += random(0, 2)
                ElseIf Actor.isInterestedIn(Player)
                    Actor(Annoyed)::"Come on, you're not funny!"
                    Player(Sad):: "Sorry, I really have to go to my another class right now. Thanks for the offer but I can't, ask another lecturer perhaps?"
                    Actor:rapportwithplayer -= random(0, 2)
                    Actor(Annoyed)::"Ok fine!"
                    Actor:attractiontoplayer -= random(0, 2)
                Else
                    Actor(Annoyed)::"You're not funny and disgusting!"
                    Player(Grin):: "Sorry, I really have to go to my another class right now. Ask another lecturer perhaps?"
                    Actor:rapportwithplayer -= random(0, 5)
                    Actor(Annoyed)::"Ok fine!"
                    Actor:attractiontoplayer -= random(0, 5)
                    Actor:arousal -= random(0, 2)
                    Actor:perversion -= random(0, 2)
                EndIf
            Else
                Player(Sad):: "Sorry, I have another class at that time. You should ask another lecturer perhaps?"
                If Actor.isInterestedIn(Player) && Actor:arousal > 15 && Actor:perversion > 70
                    Actor(Annoyed)::"Come on, you're not funny! If I <Player.suck_or_lick> your <Player.cock_or_pussy> now, will you agree to help me?"
                    "What should I do?"
                    0::"Accept"
                    1::"Refuse"
                    
                    If 0
                        Player(Happy)::"When sex is for free, I'm always in!"
                        Actor(Flirty)::"Then quickly, show me your <Player.cock_or_pussy>, so I can <Player.suck_or_lick> it! Because I have to leave right away."
                        Player.dressExcept(Bottom, Bottom_Under)
                        If !Player.isMale()
                            Filter(LickPussy)
                        Else
                            Filter(Blowjob)
                        EndIf
                        Sex(Actor, Player)
                        Player.dress()
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        Actor:rapportwithplayer += random(0, 2)
                        Actor:attractiontoplayer += random(0, 2)
                    Else
                        Player(Sad):: "Sorry, I really have to go to my another class right now. Thanks for the offer but I can't, ask another lecturer perhaps?"
                        Actor:rapportwithplayer -= random(0, 2)
                        Actor(Annoyed)::"Ok fine!"
                        Actor:attractiontoplayer -= random(0, 2)
                    EndIf
                Else
                    Actor(Annoyed)::"Ok fine!"
                    Actor:rapportwithplayer -= random(0, 2)
                EndIf
            EndIf
        EndIf
    SceneEnd() // Improved by Raddeck
    timeout(350, substitute_lecturer)