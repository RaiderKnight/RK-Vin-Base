WHAT: all
WHERE: all
WHEN: 0-24
WHO: none
OTHER: Player.isRace(Orc) || Player.isRace(Elf) || Player.isRace(Vampire)

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        fitnessL = Player.getActorVar(fitnessL)
        muscleL = Player.getActorVar(muscleL)
        intelligenceL = Player.getActorVar(intelligenceL)
        martialArtL = Player.getActorVar(rk_VB_martialArtL)
        BonusTemp = intelligenceL/100
        BonusTempM = muscleL/100
        BonusTempF = fitnessL/100
        BonusTempMA = martialArtL/100
        Bonus = intelligenceL*BonusTemp
        BonusM = muscleL*BonusTemp
        BonusF = fitnessL*BonusTemp
        BonusMA = martialArtL*BonusTemp
        If Player.isRace(Orc)
            "As an Orc, I have a higher level of fitness and muscle mass than humans. Exercise and weight lifting make me gain weight much faster than the average human."
            If fitnessL == 0 || muscleL == 0
                "I haven't exercised since then, I should think about spending more time working out. It's definitely worth it."
            ElseIf fitnessL < 2 && muscleL < 2
                "Unfortunately, I have not worked on improving my fitness or muscle mass in any significant way over the past week." 
            Else
                If fitnessL >= 2.2
                    "I worked a lot on my fitness last week and became much more fit and agile."
                EndIf
                If muscleL >= 2.2
                    "I've been working a lot on my muscle mass over the past week and have gained more muscle and mass."
                EndIf
            EndIf
            fitness += BonusF
            muscle += BonusM
            Player.setActorVar(fitnessL, 0)
            Player.setActorVar(muscleL, 0)
        ElseIf Player.isRace(Vampire)
            "As a Vampire, I have a higher level of fitness and martial art skills than humans. Exercise and practicing make me gain new skills much faster than the average human."
            If fitnessL == 0 || martialArtL == 0
                "I haven't exercised since then, I should think about spending more time practicing my skills. It's definitely worth it."
            ElseIf fitnessL < 2 && martialArtL < 2
                "Unfortunately, I have not worked on improving my fitness or martial art in any significant way over the past week." 
            Else
                If fitnessL >= 2.2
                    "I worked a lot on my fitness last week and became much more fit and agile."
                EndIf
                If martialArtL >= 2.2
                    "I've been working a lot on my martial art skills over the past week and have gained more trick for fighting."
                EndIf            
            EndIf
            fitness += BonusF
            martial += BonusMA
            Player.setActorVar(fitnessL, 0)
            Player.setActorVar(martialArtL, 0)
        Else
            "As an Elf, I have higher intelligence compared to humans. I can obtain knowledge much faster than the average human."
            If intelligenceL == 0
                "I didn't learned anything since, I should remember to spend more time reading and researching. It's totally worth it."
            ElseIf intelligenceL < 2
                "Unfortunately, I've not worked on improving my knowledge significantly in the last week." 
            Else
                "I have worked on my intelligence over the past week and gained more knowledge. I have been reading a lot, doing a lot of research and experiments."
            EndIf
            intelligence += Bonus
            Player.setActorVar(intelligenceL, 0)
        EndIf
    SceneEnd() // Improved by Raddeck
    timeoutPrecise(168, fantasy_stat_bonus)