WHAT: all
WHERE: all
WHEN: 0 - 24
WHO: Actor = getCompanion(); If Actor.isRace(Vampire)
OTHER: !Player.isRace(Vampire)
    
    SceneStart()
        
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        "I know <Actor.name> is a vampire, and it's not hard to feel that <Actor.he_or_she> is currently thristy for blood, the way is licking raw meet from the fridge..."
        If masochist - Actor:masochist > 50
            "And for some reason, I would almost like to become a vampire... Just one bite from <Actor.his_or_her> and poof I become a bloodsucker..."
            Sucked = true
        Else
        
            "Should I ask <Actor.him_or_her> to turn me into vampire?"
            0:: "Yes"
            1:: "No"
            
            If 0
                Sucked = true
            Else
                Sucked = false
            Endif
        Endif
        
        If Sucked
            
            masochist += 2
            animatePair(Actor, Player, Kissing)
            "Sensing my receptiveness to the whole situation, <Actor.name> didn't hesistate anymore for the blood <Actor.he_or_she> has been craving ..."
            
            // Start Edit Raddeck
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -15)
            EndIf
            // End Edit Raddeck
        Endif
        
    SceneEnd() // Improved by Raddeck
    timeoutActor(200, sucked_by_vampire_cms, Actor)
    timeout(100, sucked_by_vampire_cms)