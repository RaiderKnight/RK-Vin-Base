WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.show()
        Player.moveToPersonStand(Actor, 50)
        If Actor.isDating() || Actor.hasRelationship(Spouses, Dating)
            Player(Curious)::"Hey, baby! Can I touch your belly? I want to feel our baby!"
            If Actor:mood > 15
                Actor(Happy)::"Sure??"
                Actor:attractiontoplayer += Random(0, 1)
                Actor:rapportwithplayer += Random(0, 1)
                Actor:mood += Random(0, 1)
                animatePair(Player, Actor, Hugging)
                If perversion > 50
                    "While I'm touching her, doesn't it turn me so on that I want want to..."
                    0::"Ask if I can listen from her vagina hole"
                    1::"Fingering her pussy"
                    2::"Suck on her breasts"
                    3::"Licking her pussy"
                    99::"Do nothing"
                    
                    If 0
                        arousal += 1
                        perversion += 1
                        Player(Curious)::"Hey, baby! Can I listen from your vagina hole? I'm curious!"
                        animatePair(Player, Actor, LickPussy)
                        If Actor:arousal > 25
                            Actor(Happy)::"You're funny, you could try!"
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of looking my pussy, put your cock inside!"
                            Else
                                Actor(Flirty)::"Instead of looking my pussy, lick it!"
                                Filter(LickPussy)
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Else
                            Actor(Annoyed)::"Not now!"
                        Endif
                    Elseif 1
                        arousal += 1
                        perversion += 1
                        "I take advantage of this moment to slip a finger between her legs and start to caress her clit..."
                        animatePair(Player, Actor, FingerPussy)
                        If Actor:arousal > 25
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of using your fingers, use your cock!"
                            Else
                                Actor(Flirty)::"Instead of using your fingers, use your tongue!"
                                Filter(LickPussy)
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Else
                            Actor(Annoyed)::"Not now!"
                        Endif
                    Elseif 2
                        arousal += 1
                        perversion += 1
                        "I take advantage of this moment to grab her tits and start to suck them..."
                        animatePair(Player, Actor, Kissing)
                        If Actor:arousal > 25
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of grabbing and sucking my tits, put your cock between!"
                                Filter(Boobjob)
                            Else
                                Actor(Flirty)::"Instead of grabbing and sucking my tits, let's fuck!"
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            Filter(Boobjob)
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Else
                            Actor(Annoyed)::"Not now!"
                        Endif
                    Elseif 3
                        arousal += 1
                        perversion += 1
                        "I take advantage of this moment to take off her panties and start to lick her pussy..."
                        animatePair(Player, Actor, LickPussy)
                        If Actor:arousal > 25
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of licking my pussy, put your cock inside!"
                            Else
                                Actor(Flirty)::"Instead of licking my pussy, let's fuck!"
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Else
                            Actor(Annoyed)::"Not now!"
                        Endif
                    Endif
                Endif
            Else
                Actor(Sad)::"I'm not in the mood right now, my belly is hurting me! So don't touch!"
            Endif
        Else
            ARefuseP = false
            arousal += 1
            perversion += 1
            Player(Curious)::"Excuse me ma'am! Can I touch your tummy? I want to feel your baby!"
            If Actor.isInterestedIn(Player) && Actor:arousal > 25 && Actor:perversion > 50
                Actor(Happy)::"Sure ???"
                Actor:attractiontoplayer += Random(0, 1)
                Actor:rapportwithplayer += Random(0, 1)
                Actor:mood += Random(0, 1)
                animatePair(Player, Actor, Hugging)
                Actor:arousal += Random(0, 5)
                Actor(Flirty)::"Do you want to come to my house, I'll let you touch me everywhere?"
                "What I should do?"
                0::"Accept"
                1::"Refuse"
                
                If 0
                    setBackground(bedroom)
                    "Soon enough I followed <Actor.name> back to <Actor.his_or_her> luxury home"
                    "As agreed, <Actor.name> and I got down to business right away."
                    Player.strip()
                    Actor.strip()
                    Sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    If !Actor.isContactExchanged()
                        "After our sex session, <Actor.name> suggested that we exchange contacts. Shall I?"
                        0:: "Yes"
                        1:: "No"
                        
                        If 0
                            exchangeContact(Actor)
                            "We exchanged numbers and parted ways."
                        Else
                            "After our sex session, we got dressed and separated."
                        Endif
                    Else
                        "After our sex session, we got dressed and separated."
                    EndIf
                Else
                    Player(Sad)::"Sorry but I was only interested in your tummy so..."
                    Actor(Happy)::"That's fine! Don't be sorry!"
                    Actor:attractiontoplayer += Random(0, 1)
                    Actor:rapportwithplayer += Random(0, 1)
                Endif
            Elseif Actor.isInterestedIn(Player)
                Actor(Happy)::"Sure ??"
                Actor:attractiontoplayer += Random(0, 1)
                Actor:rapportwithplayer += Random(0, 1)
                Actor:mood += Random(0, 1)
                animatePair(Player, Actor, Hugging)
                Actor:arousal += Random(0, 5)
                If perversion > 50
                    "While I'm touching her, doesn't it turn me so on that I want want to..."
                    0::"Ask if I can listen from her vagina hole"
                    1::"Fingering her pussy"
                    2::"Suck on her breasts"
                    3::"Licking her pussy"
                    99::"Do nothing"
                    
                    If 0
                        arousal += 1
                        perversion += 1
                        Player(Curious)::"Can I listen from your vagina hole? I'm curious!"
                        If Actor:arousal > 25 && Actor:perversion > 50
                            Actor(Happy)::"You're funny, you could try!"
                            animatePair(Player, Actor, LickPussy)
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of looking my pussy, put your cock inside!"
                            Else
                                Actor(Flirty)::"Instead of looking my pussy, lick it!"
                                Filter(LickPussy)
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Elseif Actor:arousal > 25
                            Actor(Happy)::"You're funny, you could try!"
                            animatePair(Player, Actor, LickPussy)
                            Actor(Annoyed)::"What are you doing? Not here!"
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                            Actor:arousal += 1
                            If !Actor.isContactExchanged()
                                "<Actor.name> suggested that we exchange contacts. Shall I?"
                                0:: "Yes"
                                1:: "No"
                                
                                If 0
                                    exchangeContact(Actor)
                                    "We exchanged numbers and parted ways."
                                    Actor(Happy)::"Sorry but I have to go!"
                                Else
                                    Actor(Annoyed)::"Sorry but I have to go!"
                                Endif
                            EndIf
                        Else
                            Actor(Angry)::"Hey, you fucking pervert! WTF are you doing?"
                            If !isAtHome()
                                Actor(Angry)::"Somebody call the police, <Player.he_or_she>'s stalking me!"
                                Actor:attractiontoplayer += Random(0, 1)
                                Actor:rapportwithplayer += Random(0, 1)
                                followUp(rk_VB_SexAssault_Arrest)
                            Else
                                Actor(Angry)::"That's not cool! I'm leaving!"
                            Endif
                            Actor.hide()
                        Endif
                    Elseif 1
                        arousal += 1
                        perversion += 1
                        "I take advantage of this moment to slip a finger between her legs and start to caress her clit..."
                        animatePair(Player, Actor, FingerPussy)
                        If Actor:arousal > 25 && Actor:perversion > 50
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of using your fingers, use your cock!"
                            Else
                                Actor(Flirty)::"Instead of using your fingers, use your tongue!"
                                Filter(LickPussy)
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Elseif Actor:arousal > 25
                            Actor(Annoyed)::"What are you doing? Not here!"
                            Actor(Annoyed)::"Sorry but I have to go!"
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                            Actor:arousal += 1
                            If !Actor.isContactExchanged()
                                "<Actor.name> suggested that we exchange contacts. Shall I?"
                                0:: "Yes"
                                1:: "No"
                                
                                If 0
                                    exchangeContact(Actor)
                                    "We exchanged numbers and parted ways."
                                    Actor(Happy)::"Sorry but I have to go!"
                                Else
                                    Actor(Annoyed)::"Sorry but I have to go!"
                                Endif
                            EndIf
                        Else
                            Actor(Angry)::"Hey, you fucking pervert! WTF are you doing?"
                            If !isAtHome()
                                Actor(Angry)::"Somebody call the police, <Player.he_or_she> tried to touch my vagina!"
                                Actor:attractiontoplayer += Random(0, 1)
                                Actor:rapportwithplayer += Random(0, 1)
                                followUp(rk_VB_SexAssault_Arrest)
                            Else
                                Actor(Angry)::"That's not cool! I'm leaving!"
                            Endif
                            Actor.hide()
                        Endif
                    Elseif 2
                        arousal += 1
                        perversion += 1
                        "I take advantage of this moment to grab her tits and start to suck them..."
                        animatePair(Player, Actor, Kissing)
                        If Actor:arousal > 25 && Actor:perversion > 50
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of grabbing and sucking my tits, put your cock between!"
                                Filter(Boobjob)
                            Else
                                Actor(Flirty)::"Instead of grabbing and sucking my tits, let's fuck!"
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Elseif Actor:arousal > 25
                            Actor(Annoyed)::"What are you doing? Not here!"
                            Actor(Annoyed)::"Sorry but I have to go!"
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                            Actor:arousal += 1
                            If !Actor.isContactExchanged()
                                "<Actor.name> suggested that we exchange contacts. Shall I?"
                                0:: "Yes"
                                1:: "No"
                                
                                If 0
                                    exchangeContact(Actor)
                                    "We exchanged numbers and parted ways."
                                    Actor(Happy)::"Sorry but I have to go!"
                                Else
                                    Actor(Annoyed)::"Sorry but I have to go!"
                                Endif
                            EndIf
                        Else
                            Actor(Angry)::"Hey, you fucking pervert! WTF are you doing?"
                            If !isAtHome()
                                Actor(Angry)::"Somebody call the police, <Player.he_or_she> touched my tits!"
                                Actor:attractiontoplayer += Random(0, 1)
                                Actor:rapportwithplayer += Random(0, 1)
                                followUp(rk_VB_SexAssault_Arrest)
                            Else
                                Actor(Angry)::"That's not cool! I'm leaving!"
                            Endif
                            Actor.hide()
                        Endif
                    Elseif 3
                        arousal += 1
                        perversion += 1
                        "I take advantage of this moment to take off her panties and start to lick her pussy..."
                        animatePair(Player, Actor, LickPussy)
                        If Actor:arousal > 25 && Actor:perversion > 50
                            If !Player.isFemale()
                                Actor(Flirty)::"Instead of licking my pussy, put your cock inside!"
                            Else
                                Actor(Flirty)::"Instead of licking my pussy, let's fuck!"
                            EndIf
                            Player(Happy)::"Sure!"
                            Player.strip()
                            Actor.strip()
                            sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                        Elseif Actor:arousal > 25
                            Actor(Annoyed)::"What are you doing? Not here!"
                            Actor(Annoyed)::"Sorry but I have to go!"
                            Actor:attractiontoplayer += Random(0, 1)
                            Actor:rapportwithplayer += Random(0, 1)
                            Actor:arousal += 1
                            If !Actor.isContactExchanged()
                                "<Actor.name> suggested that we exchange contacts. Shall I?"
                                0:: "Yes"
                                1:: "No"
                                
                                If 0
                                    exchangeContact(Actor)
                                    "We exchanged numbers and parted ways."
                                    Actor(Happy)::"Sorry but I have to go!"
                                Else
                                    Actor(Annoyed)::"Sorry but I have to go!"
                                Endif
                            EndIf
                        Else
                            Actor(Angry)::"Hey, you fucking pervert! WTF are you doing?"
                            If !isAtHome()
                                Actor(Angry)::"Somebody call the police, <Player.he_or_she>'s trying to rape me!"
                                Actor:attractiontoplayer += Random(0, 1)
                                Actor:rapportwithplayer += Random(0, 1)
                                followUp(rk_VB_SexAssault_Arrest)
                            Else
                                Actor(Angry)::"That's not cool! I'm leaving!"
                            Endif
                            Actor.hide()
                        Endif
                    Endif
                Endif
            Else
                Actor(Shocked)::"No? I don't know you? You're weird, move away!"
                Actor:attractiontoplayer -= Random(0, 1)
                Actor:rapportwithplayer -= Random(0, 1)
                Player(Scared):: "Sorry. I didn't mean any offence ..."
            Endif
        Endif
    SceneEnd()