WHAT: all, watch_tv, watch_a_movie_in, rk_VB_House_Entertainment, -sleep, -nap
WHERE: home
WHEN: 0 - 24
WHO: Actor = getSpecific(Dating); If Actor.isDating()
OTHER: !isWithCompanion() && random(0, 100) > 75

    SceneStart()
        If Actor.isPregnant() || Actor.isPregnant()
            rk_VB_propose_baby.setGlobal(0)
        EndIf
        Actor = getSpecific(Dating)
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor2 = Actor.getRelatedPerson(Siblings)
        While Actor2.isValid() && !Actor2.isSameGender(Actor)
            Actor2 = Actor.getRelatedPerson(Siblings)
        EndWhile
        proposeb = rk_VB_propose_baby.getGlobal()
        If !Actor2.isValid()
            Actor2 = generatePersonTemporary()
            While !Actor2.isSameGender(Actor)
                Actor2 = generatePersonTemporary()
            EndWhile
            Actor2:age => random(18, 40)
            Actor2.randomizeFace()
            Actor2.randomizeHairs()
            Actor2.makePermanent()
            addNpcRelationship(Siblings, Actor, Actor2)
        EndIf
        Actor2.setWantsBabies()
        Actor2.dress()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "While I'm here doing nothing..."
        If isModEnabled(rk_vin_Base)  
            "Something unexpected then happened."
            "I received a texto from <Actor.name>... my <Actor.boyfriend_or_girlfriend>'s <Actor2.brother_or_sister>??"
            "So I decided to start chatting with <Actor2.him_or_her> while <Actor.name> is away."
            If proposeb == 1
                "I thought <Actor2.he_or_she> was just trying to be friendly, polite and wanted to keep me company, but..."
                secondScreen(Actor2)
                Actor2.show()
                Actor2(Curious):: "So ... you're looking to become a <Player.father_or_mother>, I've heard?"
                Player(Curious):: "What? How could you possibly know that?"
                Actor2(Happy):: "My <Actor.brother_or_sister> told me about it ..."
                Actor2(Flirty):: "I'm sorry to tell you that, but my immature <Actor.brother_or_sister> will never feel ready for that kind of responsibility ..."
                If Player.isMale()
                    Actor2(Flirty):: "I, on the other hand, have always wanted a child ..."
                    Player(Curious):: "You want me to make you a child?"
                Else
                    Actor2(Flirty):: "I, on the other hand, have always wanted a child ... My <Actor.brother_or_sister> will see the difference ..."
                    Player(Curious):: "You want to make me a child?"
                EndIf
                Actor2(Flirty):: "Why, is there a problem?"
                //secondScreen()
                "What should I do?"
                0:: "Accept <Actor2.his_or_her> offer"
                1:: "Reject <Actor2.his_or_her> offer"
                
                If 0
                    Player(Flirty)::"Not at all, in fact I accept your offer!"
                    Actor2(Flirty):: "Excellent, come to my place, we won't be disturbed!"
                    Player(Flirty)::"I'm coming right away! Bye!"
                    secondScreen()
                    setBackgroundCustom(livingroom)
                    Actor2.strip()
                    Actor2.show()
                    "Once arrived, <Actor2.he_or_she> was waiting for me naked, so I undressed and we started to fuck"
                    Player.strip()
                    Sex(Actor2, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    If Player.isMale()
                        Actor2.impregnate(Player)
                    Else
                        Player.impregnate(Actor2)
                    EndIf
                Else
                    If Actor.isMale()
                        Player(Sad)::"You are very kind but I remain faithful to my husband"
                    Else
                        Player(Sad)::"You are very kind but I remain faithful to my wife"
                    EndIf
                EndIf
            Else
                "After a long discussion together we decided to leave each other to go about our own business "
                Actor2:attractiontoplayer += random(0, 1)
                Actor2:rapportwithplayer += random(0, 1)
            EndIf
        EndIf
    SceneEnd()
    timeout(500, rk_VB_Sib_Ask_baby)