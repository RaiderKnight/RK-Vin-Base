WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER:

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If Player.isPregnant()
            Days = Player.countPregnancyDays()
            DaysL = 280 - Days
            "I'm currently pregnant. It's pointless to check my fertility level since I cannot get pregnant again."
            "BTW, I have <DaysL> days left before I give birth..."
        Else
            fer = fertility*1
            ferDan = fer*10
            If Player.isMale()
                "My fertility level is <fer>%"
            Else
                "My fertility level is <fer>% on normal days and <ferDan>% on dangerous days"
            EndIf
            If !Player.isMale() && Player.isGay()
                Actor = generatePersonTemporary()
                While Actor.isMale()
                    Actor = generatePersonTemporary()
                EndWhile
            Else
                Actor = generatePersonTemporary()
                While Actor.isMale()
                    !Actor = generatePersonTemporary()
                EndWhile
            EndIf
            Actor.dress()
            Actor.show()
            "The doctor who did the test was <Actor.name>."
            If Actor:perversion > 60
                Actor(Flirty):: "You know, since you're so concerned about your fertility level, why don't we do a more 'practical' test? Numbers can be misleading, you know! Real results are what matter."
                0:: "Fuck with the doctor to test how fertile I am"
                1:: "No"
                
                If 0
                    Sex(Actor, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, 2)
                    EndIf
                EndIf
            EndIf
        EndIf
    SceneEnd() // Improved by Raddeck