WHAT: all, -work
WHERE: all
WHEN: 19-24
WHO: Actor = getPerson(tag_Master)
OTHER: isModEnabled(vin_NonConsensual)

    // Can be triggered from 19-24 pm while not working/in party, the master will call upon slave, give the slave a collar(since it always disappear) and take the slave out for a walk(outside/street background?). Which will trigger a sneaking game, failed in sneak will trigger some (possibly) Nonconsen events

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "I was going about my day when my <Actor.master_or_mistress> called me."
        secondScreen(Actor)
        Actor(Serene):: "Slave, come over at once!"
        Player(Happy):: "Of course, <Actor.master_or_mistress>."
        AGetRapper = false
        setBackgroundCustom(livingroom)
        Actor.dress()
        Actor.show()
        Player.strip()
        Player.dressBondage(Collar)
        "I have already accepted my place as <Actor.name>'s personal slave. As my rightful <Actor.master_or_mistress>, <Actor.he_or_she> can do whatever <Actor.he_or_she> wants with me!"
        setBackground3D(Modules/rk_vin_Base/World/gl_street1.lpworld)
        "My <Actor.Master_or_Mistress> asks me to go for a walk, alone and completely naked, through the streets at night..."
        "I must be careful to not attract the attention of others ... this could turn out to be dangerous ..."
        SneakGame()
        BadLuckD = Random(0, 100)
        If Random(0, 100) < sneak
            "Thankfully, I was sneaky enough that no-one saw me walking around naked."
        ElseIf Random(0, 100) > 75
            "Fuck, someone called the Police!"
            followUp(rk_VB_public_indecency)
        Else
            AGetFucked = false
            count = 0
            "Although I try to choose streets where I know there are not many people, I end up passing a stranger..."
            While count < 3
                Actor2 = generatePersonTemporary()
                While !Actor2.isInterestedIn(Player)
                    Actor2 = generatePersonTemporary()
                Endwhile
                Actor2.dress()
                Actor2.show()
                AChooseL = Random(1, 3)
                AChooseL = AChooseL.round()
                Actor2.moveToPersonStand(Player, 50)
                If AChooseL == 1
                    "The stranger seeing my naked and vulnerable body, goes towards me and asks me ..."
                    faceEachOther(Player, Actor2)
                    Actor2(Curious)::"Hi, did someone hurt you? Why are you naked? Do you want me to call the police or someone close to you?"
                    Player(Grin)::"No, I'm a fetish slave, I'm doing this for the good of my <Actor.master_or_mistress>, so everything is fine!"
                    Actor2(Curious)::"Ah ok, if you say so! Anyway, you should at least hide your parts because you might get attacked."
                    If BadLuckD > 85 && Actor2:perversion > 70
                        AGetFucked = true
                    EndIf
                ElseIf AChooseL == 2
                    "The stranger seeing my naked body and tempting, moves towards me and asks ..."
                    faceEachOther(Player, Actor2)
                    Actor2(Curious)::"Hey beautiful, where are you going like that, come to my place I'll warm you up or leave me your number, I like <Player.boys_or_girls> like you!"
                    "What should I do?"
                    0::"Go to <Actor2.his_or_her> home"
                    1::"Exchange number"
                    2::"Scold <Actor2.him_or_her>"
                    3::"Ignore <Actor2.him_or_her>"
                    
                    If 0
                        perversion += 1
                        Player(Grin)::"Why not! My <Actor.master_or_mistress> won't know!"
                        "So we went to <Actor2.his_or_her> place and once we arrived, <Actor2.he_or_she> undressed and we fucked immediately..."
                        Actor2.strip()
                        sex(Actor2, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                    ElseIf 1
                        interpersonal += 1
                        Player(Grin)::"Sure!"
                        "We exchanged numbers and parted ways."
                        Actor2.makePermanent()
                        exchangeContact(Actor2)
                    ElseIf 2
                        masochist += 1
                        Player(Annoyed)::"Only if my <Actor.master_or_mistress> allows you and unfortunately <Actor2.he_or_she> is not here!"
                        "After saying this, I chose to walk progressively faster to get away from the unwanted flirt as quickly as possible."
                        If BadLuckD > 50 && Actor2:perversion > 70
                            AGetFucked = true
                        EndIf
                    Else
                        masochist += 0.5
                        "I chose to say nothing and walk progressively faster to get away from the unwanted flirt as quickly as possible."
                        If BadLuckD > 65 && Actor2:perversion > 70
                            AGetFucked = true
                        EndIf
                    EndIf
                ElseIf AChooseL == 3
                    "The stranger, seeing my naked and attractive body, grabs me from behind and tries to rape me!"
                    Actor2(Evil)::"Come here! Bitch!"
                    "Do I decide to defend myself?"
                    0::"Yes"
                    1::"No"
                    
                    If 0
                        If Random(0, 200) < fitness + martial
                            "I managed to break free of the most important restrains holding me in place ..."
                            Actor2.hide()
                            If energy < 50 && random(0, 10) > 6
                                "I then ran away to managed to leave my kidnapper far behind ..."
                                "But I fell,... I was too exhausted to run."
                                "The noise immediately attracted the attention of the kidnapper, who arrived right away to take me back and fuck me as <Actor2.he_or_she> had promised before!"
                                Filter(Aggressive)
                                TalkNonConsensual()
                                Actor2.strip()
                                Sex(Actor2, Player)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -3)
                                EndIf
                                mood -= 5
                                masochist += 1
                                sneak -= 1
                            Else
                                sneak += 1
                                "I then ran away and after a while I managed to leave my kidnapper far behind ..."
                                "Phew, that was a close call ..."
                            Endif
                        Else
                            AGetFucked = true
                        Endif
                    Else
                        AGetFucked = true
                    EndIf
                EndIf
                If AGetFucked
                    Filter(Aggressive)
                    TalkNonConsensual()
                    Actor2.strip()
                    SexNoAffair(Actor2, Player)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -3)
                    EndIf
                    Actor2.setActorVar(tag_Rapist, 1)
                    AGetRapper = true
                    energy -= 15
                EndIf
                count += 1
            Endwhile
        Endif
        Actor(Happy):: "Well done, slave, you've worked hard for your <Actor.Master_or_Mistress> today ..."
        If AGetRapper
            Player(Sad):: "Except that I was raped..."
            mood -= 25
            energy -= 5
        Endif
    SceneEnd()
    timeout(200, slave_walk_public_cms) // Improved by Raddeck