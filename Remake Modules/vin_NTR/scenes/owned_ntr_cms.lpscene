WHAT: all
WHERE: home
WHEN: 0 - 24 
WHO: Actor = getSpecific(Dating); Actor2 = getRelative(Child); If Actor.isFemale() && Actor2.isFemale() && !Actor.isPregnant() && !Actor2.isPregnant()
OTHER: masochist > 90 && Player.isMale() && isPlayerMarried()

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.dress()
        Actor.show()
        MaisBon = false
        "By now, I have realized that I'm a massively submissive cuckold who loves nothing more than having another man dominating and fucking my wife."
        "I might as well ask a real man to take over as <Actor.name>'s husband since I'm not worthy anymore..."
        0:: "Yes"
        1:: "No"
        
        If 0
            "Who would be a suitable bull?"
            selectNPC()
            Actor3 = getSelectedNPC()
            If Actor3.isMale() && Actor3.isValid()
                "<Actor3.name> accepted my offer to replace me as <Actor.name>'s husband."
                Actor.setDatingId()
                loseDating()
                addNpcRelationship(Spouses, Actor3, Actor)
                "Tonight is <Actor3.name> and <Actor.name>'s wedding night ..."
                Actor3.dressWedding()
                Actor.dressWedding()
                Actor3.show()
                Actor3(Flirty):: "Ah, my new bride, I can't wait to show your placemat ex-husband how I impregnate you tonight ..."
                Actor(Flirty):: "Of course, honey! I stopped using birth control for this."
                0:: "Watch my ex-wife get impregnated by the bull I've chosen for her"
                1:: "That's going too far! No Pregnancy!"
                2:: "I think I've seen enough!"
                
                If 0
                    masochist -= random(0, 2)
                    Actor3(Evil):: "Look at your pathetic ex-husband. Too eager to watch his ex-wife get impregnated by a real man to say a word."
                    MaisBon = true
                    Actor.strip()
                    Actor3.strip()
                ElseIf 1
                    Player(Furious):: "No way ... that's going too far ..."
                    Actor3(Furious):: "Shut up! Who asked for your opinion? She's my wife now, remember?"
                    Player(Sad):: "Yeah but ..."
                    masochist -= random(0, 1)
                    Actor.strip()
                    Actor3.strip()
                    Actor3(Grin):: "Even more than that, I'm going to fuck her right here in front of you, to show you what a real stallion is."
                    Actor(Flirty):: "Oh yes, it's clear that with the cock you've got, I'm going to have hours of pleasure."
                    Player(Shocked):: "You're a bunch of weirdos..."
                    Actor3(Grin):: "Shut up and learn!"
                    Actor(Furious):: "Ah ah ah!"
                    MaisBon = true
                Else  
                    "I decided to go and look elsewhere, I don't want to see this"
                    masochist -= random(0, 1)
                EndIf 
                If MaisBon
                    Actor.impregnate(Actor3)
                    Sex(Actor3, Actor)
                    Actor:attractiontoplayer -= random(0, 1)
                    Actor(Flirty):: "Oh baby, I can tell by the powerful strokes of your penis that your sperm has made it to one of my eggs..."
                    Actor(Grin):: "Look at my pathetic ex-husband, trying to pretend he's not mad... Do you have any regrets now? Too late, I'm <Actor3.name>'s wife now!"
                    If isModEnabled(vin_Incest)
                        Actor(Grin):: "What do you say if we piss him off even more. I'll tell you, I'm not the only woman in this house who sees him as a cuckold."
                        Actor(Grin)::"My daughter is in her room, she never has a boyfriend but I can tell you that she likes to fuck..."
                        "Are you serious! he managed to corrupt my daughter against me, a beautiful bastard!"
                        Actor(Flirty):: "Come in, sweetheart!"
                        Actor2.dress()
                        Actor2.show()
                        Actor2(Flirty)::"Hey hi, you're doing great! "
                        Actor(Flirty)::"Sweet girl, why don't you give our new man of the house a little welcome gift?"
                        Player(Shocked)::"What! You're in too!"
                        Actor2.strip()
                        Actor2(Flirty)::"Who cares, a cock is a cock..."
                        Actor2.impregnate(Actor3)
                        Sex(Actor3, Actor2)
                    EndIf
                EndIf
                mood -= random(1, 5)
            Else
                "That's not even a guy ..."
            EndIf
            Actor.hide()   
            Actor3.hide()
            Actor2.hide()
        EndIf
    SceneEnd() // Scene improved by Raddeck
    timeout(1000, owned_ntr_cms)