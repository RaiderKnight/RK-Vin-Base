WHAT: all
WHERE: home
WHEN: 0 - 24 
WHO: Actor = getSpecific(Dating); Actor2 = getRelative(Child); If Actor.isMale() && Actor2.isMale()
OTHER: masochist > 90 && !Player.isMale() && isPlayerMarried()

    SceneStart()
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.dress()
        Actor.show()
        MaisBon = false
        "By now, I have realized that I'm a massively submissive cuckold who loves nothing more than having another woman dominating and fucking my husband."
        "I might as well ask a real woman to take over as <Actor.name>'s wife since I'm not worthy anymore..."
        0:: "Yes"
        1:: "No"
        
        If 0
            "Who would be a suitable bull?"
            selectNPC()
            Actor3 = getSelectedNPC()
            If !Actor3.isMale() && Actor3.isValid()
                "<Actor3.name> accepted my offer to replace me as <Actor.name>'s wife."
                loseDating()
                AddNpcRelationship(Spouses, Actor3, Actor)
                "Tonight is <Actor3.name> and <Actor.name>'s wedding night ..."
                Actor3.dressWedding()
                Actor.dressWedding()
                Actor3.show()
                Actor3(Flirty):: "Ah, my new bride, I can't wait to show your ex-wife how you will impregnate me tonight ..."
                Actor3(Flirty):: "I also stopped using birth control for this."
                0:: "Watch my ex-husband impregnated the new wife I've chosen for him."
                1:: "That's going to far! No Pregnancy!"
                2:: "I think I've seen enough!"
                
                If 0
                    MaisBon = true
                    masochist -= random(0, 1)
                    Actor3(Evil):: "Look at your pathetic ex-wife. Too eager to watch her ex-husband impregnated a real woman, to say a word."
                    Actor.strip()
                    Actor3.strip()
                ElseIf 1
                    Player(Furious):: "No way ... that's going too far ..."
                    Actor3(Furious):: "Shut up! Who asked for your opinion? He's my husband now, remember?"
                    Player(Sad):: "Yeah but ..."
                    masochist -= random(0, 1)
                    Actor.strip()
                    Actor3.strip()
                    Actor3(Grin):: "Even more than that, I'm going to fuck with him right here in front of you, to show you what a real stallion is."
                    Actor(Flirty):: "Oh yes, it's clear that with the cock I got, you're going to have hours of pleasure."
                    Player(Shocked):: "You're a bunch of weirdos..."
                    Actor3(Grin):: "Shut up and learn!"
                    Actor(Furious):: "Ah ah ah!"
                    MaisBon = true
                Else  
                    "I decided to go and look elsewhere, I don't want to see this"
                    masochist -= random(0, 1)
                EndIf 
                If MaisBon
                    Actor3.impregnate(Actor)
                    Sex(Actor, Actor3)
                    Actor:attractiontoplayer -= random(0, 1)
                    Actor3(Flirty):: "Oh baby, I can tell by the powerful strokes of your penis that your sperm has made it to one of my eggs..."
                    Actor(Grin):: "Look at my pathetic ex-wife, trying to pretend she's not mad ... Are you having regrets now? Too late, I'm <Actor3.name>'s husband now!"
                    If isModEnabled(vin_Incest)
                        Actor(Grin):: "What do you say if we piss her off even more. I'll tell you, I'm not the only man in this house who sees her as a cuckold."
                        Actor(Grin)::"My son is in his room, he never has a girlfriend but I can tell you that he likes to fuck..."
                        "Are you serious! He managed to coorrupt my son against me, a beautiful bastard!"
                        Actor(Flirty):: "<Actor2.name>, come here!"
                        Actor2.dress()
                        Actor2.show()
                        Actor2(Flirty)::"Hey hi, you're doing great! "
                        Actor(Flirty)::"Big guy, why don't you give our new woman of the house a little welcome gift?"
                        Player(Shocked)::"What! You're in too!"
                        Actor2.strip()
                        Actor2(Flirty)::"Who cares, a pussy is a pussy..."
                        Actor3.impregnate(Actor2)
                        Sex(Actor2, Actor3)
                    EndIf
                EndIf
                mood -= random(1, 5)
            Else
                "That's not even a woman ..."
            EndIf
            Actor.hide()   
            Actor3.hide()
            Actor2.hide()
        Endif
    SceneEnd() // Scene improved by Raddeck
    timeout(1000, rk_VB_owned_ntr_cmsMF)