WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        DaysJobCount = Player.getActorVar(rk_VB_DaysJobCount)
        BakerSalary = Player.getActorVar(rk_VB_BakerSalary)
        GotBakeryJob = rk_VB_GotBakeryJob.getGlobal()
        If GotBakeryJob >= 1
            "Am I sure that I want to resign from my current job?"
            0:: "Yes"
            1:: "No"
        
            If 0
            
                "This job just wasn't for me. I submitted a resignation letter to my boss."
                "Time for a new chapter in my career."
                If Player.isFemale()
                    rk_VB_GotBakeryJob.setGlobal(0)
                Else
                    rk_VB_GotBakeryJobM.setGlobal(0)
                Endif
                Player.setActorVar(rk_VB_DaysJobCount, 0)
                Player.setActorVar(rk_VB_BakerSalary, 0)
            Else
            
                "On second thought, I can't afford to give up this job."
            Endif
        Else
            "I do not work here. I have to 'Ask for a job' in order to work here as an employee and resign after..."
        Endif
    
    SceneEnd()