WHAT: all, -sleep, -nap
WHERE: home
WHEN: 0-24
WHO: Dating = getSpecific(Dating); If Dating.isDating() && Random(75, 90) < perversion && Random(100, 200) < [Dating:attractiontoplayer + Dating:perversion - Dating:masochist]
OTHER: !OpenRelationship()

    SceneStart()
    
        Dating.show(2)
        Dating(Flirty):: "You know what, honey? Being quite open-minded, I sometimes feel a traditional relationship is too restricted."
        Dating(Flirty):: "The commitment and the possessiveness just don't feel right. We should open up our relationship?" 
        Dating(Flirty):: "So that I can sleep with whoever I want and you can also sleep with whoever you want to."
        0:: "Yes"
        1:: "No"
        
        If 0
            Player(Happy):: "Sure"
            startOpenRelationship()
        Else
            Player():: "No, it may sound nice in theory but jealousy would kill our relationship."
            intelligence += 0.5
        Endif
        
    SceneEnd() // Scene edited by Raddeck
    timeout(300, open_relationship)