WHAT: sleep, nap
WHERE: home
WHEN: 0 - 24
WHO: Actor = getSpecific(Spouses); If Actor:attractiontoplayer > 0
OTHER: [IsAtDatingHome() || IsAtHome()] && isPlayerMarried()

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        "I'm in bed asleep when <Actor.husband_or_wife> come in room."
        Player(Happy):: "ZZzz"
        If Actor:perversion < 85 || Actor:arousal < 85
            "My <Actor.husband_or_wife> strips naked in enters our bed."
        Else
            Actor.dressExcept(Outerwear, Foot, Headwear)       
        Endif
        Actor.show(2)
        Actor.animate(na_sleep)
        Actor:rapportwithplayer += Random(0, 2)
        
        "Later that night ..."
        Actor:incest += Random(0, 1)
        Actor.closeEyes(true)
        Player(Happy):: "Damn, <Actor.his_or_her> leg is touching my private parts ... I'm so aroused."
        If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] && [!Actor.isVisible(Bottom) && !Actor.isVisible(Bottom_Under)] && !Actor.isSameGender(Player)
            "I thank god we're already naked."
            "Makes... making love real easy."
            Sex(Actor, Player)
            Actor:incest += 25
            Actor:attractiontoplayer += 25
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
        Else
            0:: "Feel <Actor.him_or_her> up"
            1:: "Maybe not"
                    
            If 0
                "I cannot take it anymore. I suppose a few touches won't hurt ..."
                Actor:incest += Random(0, 1)
                If [Actor:incest || Actor:attractiontoplayer] + Actor:perversion > 125
                    Actor.closeEyes(false)
                    Actor(Flirty):: "I see you're getting handsy ... Couldn't resist your own <Actor.husband_or_wife>, could you."
                    AnimatePair(Player, Actor, Kissing)
                    Actor(Kiss):: "..."
                    Player(Kiss):: "..."
                    "Is this real life or a dream? I'm kissing my own <Actor.relationship> and I like it ..."
                    0:: "Give in to desires"
                    1:: "Stop it there"
                            
                    If 0
                        Sex(Player, Actor)
                        Actor:incest += Random(0, 50)
                        Actor:attractiontoplayer += Random(0, 50)
                        If HungerEnable > 0
                             Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Else
                        "This is going too far. I needed to stop it."
                        "Eventually, we went back to sleep, neither of us trying anything funny for the rest of the night."
                    Endif
                Endif
                                    
            Else
                "I'd better not get too handsy."
                If Actor:arousal > 85 && Actor.isInterestedIn(Player) && [Actor:perversion > 85 || Actor:incest > 45 || Actor:attractiontoplayer > 85]
                    "While I'm sleeping my <Actor.husband_or_wife> starts fucking me."
                    Actor:attractiontoplayer += Random(1, 10)
                    Actor:perversion += Random(5, 20)
                    Actor:incest += 5
                    Sex(Player, Actor)
                    perversion += Random(0, 0.5)
                    mood += 30
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                Endif
            Endif
        
        
        Endif
    
    SceneEnd() // Scene edited by Raddeck
    timeout(100, Ravenger_VB_Spouse_cum_to_bed)