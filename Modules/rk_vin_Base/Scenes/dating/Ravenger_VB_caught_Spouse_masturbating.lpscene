WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getSpecific(Spouses); If Actor:attractiontoplayer > 0 && !Actor.isSameGender(Player)
OTHER: [IsAtDatingHome() || IsAtHome()] && isPlayerMarried()

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        "What's that sound? If it is what I think it is, then my <Actor.husband_or_wife> is getting some very sexy alone time. <Actor.He_or_She> is moaning quite loudly."
        0:: "Ignore"
        1:: masochist < 50:: "Complain"
        2:: "Peep"
    
        If 0
            "Good for <Actor.him_or_her> I guess. I can't ban my <Actor.husband_or_wife> from some healthy releases!"
        Elseif 1
            Player(Angry):: "For god's sake, keep it down. Do you want the entire building to know what you're up to?"
            Actor:rapportwithplayer -= Random(0, 5)
            masochist -= Random(0, 1)
        Else
            perversion += Random(0, 0.5)
            Actor:incest += Random(0, 1)
            Actor:attractiontoplayer += 2
            "My curiosity got the better of me and I just had to see this ..."
            Actor.strip()
            Actor.show(2)
            Filter(Solo)
            Sex(Actor)
            Actor.show(2)
            Actor(Crying):: "Yes, yes, yes!"
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            "Wow, <Actor.he_or_she> is really into it. It must have been a while since <Actor.he_or_she> last had sex. I'm getting very aroused ... "
            "Maybe I should join..."
            0:: "Offer to join <Actor.him_or_her>"
            1:: "Enough"
                
            If 0
                Player(Flirty):: "Looks like you could do with some company, my dear <Actor.husband_or_wife>? Mind if I join you?"
                If Random(50, 150) < [Actor:incest || Actor:attractiontoplayer] + Actor:perversion
                        Actor(Flirty):: "Come on and join then, what are you waiting for?"
                        Sex(Player, Actor)
                        Actor:incest += Random(0, 50)
                        Actor:attractiontoplayer += Random(0, 50)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Else
                        Actor(Angry):: "Sorry, but no."
                        Actor:rapportwithplayer -= Random(0, 3)
                    Endif
            Else
                "I should get out of here before I get caught."
                If [Actor:intelligence + Random(0, 50)] > sneak
                    "How ever later my <Actor.husband_or_wife> came to confront me." 
                    Actor(Angry):: "I know you were outside the room."
                    If Random(50, 150) < [Actor:incest || Actor:attractiontoplayer] + Actor:perversion 
                        Actor(Flirty):: "You know what. I think I'm going to have some fun with you."
                        Sex(Player, Actor)
                        Actor:incest += Random(0, 50)
                        Actor:attractiontoplayer += Random(0, 50)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                    Endif
                    
                Endif
            Endif
        Endif

    SceneEnd() // Scene edited by Raddeck
    timeout(48, Ravenger_VB_caught_Spouse_masturbating)