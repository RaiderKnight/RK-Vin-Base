WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        
        // Start edit Ravenger6660
        Saved = Actor.getActorVar(tag_Saved)
        If Saved > 0
        
            "For some reason they seem to resist my magic."
            
        Else
        
            // End edit Ravenger6660
            
            "Which spell should I use?"
            0:: "Vanish clothes!"
            1:: "Masturbate furiously!"
            2:: "Nothing"
            
            If 0
            
                "This spell not only removes all of the victim's clothes, but makes the victim unaware that they're naked ... making for an unknowing exhibitionist!"
                Actor.setNoRedress()
                Actor.strip()
                "The poor <Actor.guy_or_girl> is now showing off everything to everyone, without realizing it."
                
            Elseif 1
            
                "This spell overcomes the victim with an irrestible urge to masturbate ... even in public."
                Actor:: "Ah ... I'm so horny ... what's happening to me?"
                Filter(Solo)
                Sex(Actor)
                
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                
                // Start edit Ravenger6660
                Actor:perversion += 10
                If isModEnabled(vin_NonConsensual)
                    "Should I 'help' them?"
                    0:: "Yes"
                    1:: "Let them go."
                    
                    If 0
                        Sex(Player, Actor)
                        masochist -= 15
                        perversion += 20
                        Actor:perversion += 10
                    Endif   
                Endif
                // End edit Ravenger6660
                
            Endif
            
        Endif
        
    SceneEnd() // Improved by Raddeck - Ravenger6660