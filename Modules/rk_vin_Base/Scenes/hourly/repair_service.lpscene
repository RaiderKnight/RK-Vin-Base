WHAT: work_hourly
WHERE: computer, copyshop, department_store, doityourself, electronics, furniture, musical_instrument, photo, sports
WHEN: 8 - 20
WHO: none
OTHER: !Player.isBoss() || !Player.loseJob()

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        "The place where I work, offers warranties and repair services for all products and sometimes due to the size and weight of the product, it's not reasonable to ask the customer to bring it back to the store for repairs."
        "I've been asked to go to a customer's home to take a look at a malfunctioning product of ours ... Not my favorite part of the job, but it must be done."
        setBackgroundCustom(livingroom)
        Actor = generatePersonTemporary()
        
        If Random(0, 100) < Actor:perversion
            Actor.show()
            Actor.makeInterested(Player)
            Actor:attractiontoplayer => 30
            "The customer opened the door to welcome me, completely naked!"
            Actor(Flirty):: "You know what ... your product is actually working just fine ... It's just I've been very lonely recently after my last breakup ... having the house all to myself."
            Actor(Flirty):: "You wouldn't mind keeping me company for a while, would you? After all, you do repairs, so you must have some magic hands."
            0:: "Fuck the customer"
            1:: "Just leave"
            
            If 0
            
                arousal += 1 // Edit by Raddeck
                Player.strip() // Edit by Raddeck
                Actor.strip() // Edit by Raddeck
                Sex(Player, Actor)
                perversion += 1 // Edit by Raddeck
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                "It was just an usual repair and I even received some tips. Good way to get free money..." // Edit by Raddeck
                money += Random(20, 100)  // Edit by Raddeck
                
            Endif
            
        Else
        
            Actor.dress()
            Actor.show()
            "It was just an usual repair and I even received some tips."
            money += Random(5, 20)  // Edit by Raddeck
            
        Endif
        
    SceneEnd() // Improved by Raddeck
    timeout(250, repair_service)