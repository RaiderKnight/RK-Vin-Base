WHAT: none
WHERE: none
WHEN: 17 - 22
WHO: none
OTHER: none
    
    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        ChannelStarted = music_channel.getGlobal()
        MchannelVids = Player.getActorVar(rk_VB_MchannelVids) // Music channel
        
        If ChannelStarted == 0
        
            "I consider myself quite a good musician and singer. I reckon I might just have some success starting a music channel on YouTube. Should I?"
            0:: "Yes"
            1:: "No"
            
            If 0
            
                "Why not? It's certainly worth a shot. Might be a good way to supplement my income."
                mood += 3
                music_channel.setGlobal(1)
                
            Else
            
                "No, I should stop daydreaming and focus on my real job."
                //timeout(500, music_channel)
                
            Endif
            
        Else
        
            "I've got some spare time. Should I make a new video for my music channel?"
            0:: "Yes"
            1:: "No"
            2:: "Close channel"
            
            If 0
            
                "Yes, it's important to upload regulary to keep my audience growing."
                music += Random(0, 0.25)
                
                If Random(0, 100) < music
                    "The new video turned out to be a success, earning a decent amount of views and a bit of money for myself."
                    money += Random(0.5, 5)*music
                    mood += 10
                    music += 0.05
                Else
                
                    "Unfortunately, the new video was a miss, not earning many views at all and even less money."
                    mood -= 10
                    
                Endif
                Player.modifyActorVar(rk_VB_MchannelVids, 1)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                energy -= 5
                
            Elseif 1
            
                "I'm too busy right now to film. Let's do that another day."
                
            Else
            
                "This thing isn't working out. It's time to shut it down."
                music_channel.clearGlobal()
                //timeout(500, music_channel)
                
            Endif
            
        Endif
    
    SceneEnd()  // Scene improved by Raddeck
    // timeout(48, music_channel)