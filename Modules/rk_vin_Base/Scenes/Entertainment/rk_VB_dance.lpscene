WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
    
        Player.show()
        If Player:arousal > 75
            Player.strip()
            Player.animate(dance)
            "I decided to strip and dance naked a bit"
        Elseif Player:arousal > 25
            Player.stripOne()
            Player.animate(dance)
            "Damn it's hot in here! I decided to take off one clothe and dance a bit"
        Else
            Player.animate(dance)
            "I decided to dance a bit"
            passTime(20, 1)
        Endif
    
    SceneEnd()