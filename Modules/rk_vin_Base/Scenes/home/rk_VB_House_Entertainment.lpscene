WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
        
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        
        IBuyaComputer = Player.getActorVar(rk_VB_IBuyaComputer) // How many computers I buy?
        IBuyaPhone = Player.getActorVar(rk_VB_IBuyaPhone) // How many phones I buy?
        IBuyaTV = Player.getActorVar(rk_VB_IBuyaTV) // How many TV I buy?

        If HungerEnable > 0 && Hunger < 9
            
            "You need to eat something... You don't have enough energy!"
            
        ElseIf energy < 9
            
            "You don't have enough energy...."
            
        Else
        
            "What should I do?"
            10:: IBuyaPhone > 0 :: "Call a hooker"
            20:: IBuyaPhone > 0 && arousal > 50 && perversion > 30 :: "Give a booty call"
            30:: stock_movie > 0 && IBuyaTV > 0 :: "Watch a movie"
            35:: stock_movie > 0 && IBuyaTV > 0 :: "Watch a porn movie"
            40:: IBuyaTV > 0 :: "Watch sport"
            50:: IBuyaTV > 0 :: "Watch TV"
            99:: "Go back!"
            
            If 10
            
                followUp(call_a_hooker)
            
            ElseIf 20
            
                followUp(booty_call)
            
            ElseIf 30
                
                Player.animate(sofawatch)
                "I decided to watch a movie..."
                stock_movie -= 1
                mood += 3
                intelligence += 0.01
                
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                passTime(1, 1.5)
            
            ElseIf 35
                
                Player.animate(sofawatch)
                "I decided to watch a porn movie..."
                stock_movie -= 1
                mood += 3
                perversion += 1
                arousal += 5
                
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                passTime(1, 1.5)

            ElseIf 40
                
                Player.animate(sofawatch)
                "I decided to watch sport..."
                fitness += 0.01
                mood += 1
                intelligence += 0.01
                
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                passTime(1, 1.5)
                
            ElseIf 50
                
                Player.animate(sofawatch)
                "I decided to watch TV..."
                mood += 2
                intelligence += 0.01
                
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -1)
                EndIf
                passTime(1, 1.5)

            EndIf
            
        EndIf

    SceneEnd()