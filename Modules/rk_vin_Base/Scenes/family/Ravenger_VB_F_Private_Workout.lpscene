WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: Actor = getSpecific(PassedOn)
OTHER: none

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        Actor.dress() 
        Actor.show(2)
        fertility += 5
        "I am workingout with <Actor.name>."
        If Actor:fitness > 25
            "They are really good at teaching me how to workout." 
        
        else
            "<Actor.name> doesn't seem to know what they are doing."
        Endif
        Actor:fitness += 5
        Actor:attractiveness += 5
        Actor:muscle += 5
        fitness += 5
        attractiveness += 5
        muscle += 5
        
        
        If Player.isMale()
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)]
                "During the workout <Actor.name> can't help but stare a my dick."
                Actor:arousal += 15
                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:perversion
                Love += Actor:incest       
                If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                    "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                    Actor.strip()
                    Actor(Flirty):: "Can you come over here and help me out."
                    Actor:arousal += 20
                    Player(Flirty):: "Sure."
                    arousal += 20
                    If Player:arousal > 75
                        Player():: "How about a real work out?"
        
                        If Actor:arousal > 75 
                            Actor()::"Just the workout I needed."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            Actor:fitness += 5
                            Actor:attractiveness += 5
                            Actor:muscle += 5
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                        Endif
                        
                    Elseif Actor:arousal > 75
                        "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                        "My resistance is futile."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        perversion += Random(0, 0.5)
                        mood += 30
                        Actor:fitness += 5
                        Actor:attractiveness += 5
                        Actor:muscle += 5
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                
                    Else
                        "We continue our workout." 
        
                    Endif
        
        
                Elseif Random(100, 150) < Love || Actor.hadsex()
                    "<Actor.name> didn't say a word but smiled at me."
                
                Else
                    "They seem very uncomfitable."
                
                Endif
            
            Else
                "It is quite an intence workout. Staring to really sweat here."
                1:: "Ask to undress."
                2:: "Tough through it."
        
                If 1
                    Player():: "Mine if I take something off. I'm really getting hot."
                    Actor():: "Well if you need to go ahead."
                    1:: "Undress outer layer."
                    2:: perversion > 25:: "Get naked."
                    3:: "Go change"     
                
                    If 1
                        Player.strip(Bottom, Outerwear, Top, Foot, Headwear)
                        "I procede to remove my shirt and pants."
                        If !Player.isVisible(Bottom_Under) 
                            "I soon realize I'm not wearing underwear."
                            Actor:arousal += 15
                            Love = 0
                            Love += Actor:attractiontoplayer
                            Love += Actor:perversion
                            Love += Actor:incest       
                            If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                                "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                                Actor.strip()
                                Actor(Flirty):: "Can you come over here and help me out."
                                Actor:arousal += 20
                                Player(Flirty):: "Sure."
                                arousal += 20
                                If Player:arousal > 75
                                    Player():: "How about a real work out?"
        
                                    If Actor:arousal > 75 
                                        Actor()::"Just the workout I needed."
                                        Actor:attractiontoplayer += Random(1, 10)
                                        Actor:perversion += Random(5, 20)
                                        Sex(Player, Actor)
                                        perversion += Random(0, 0.5)
                                        mood += 30
                                        Actor:fitness += 5
                                        Actor:attractiveness += 5
                                        Actor:muscle += 5
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                    Endif
                        
                                Elseif Actor:arousal > 75 
                                    "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                                    "My resistance is futile."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    Actor:fitness += 5
                                    Actor:attractiveness += 5
                                    Actor:muscle += 5
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                
                                Else
                                    "We continue our workout." 
        
                                Endif
                    
                            Elseif Random(100, 150) < Love || Actor.hadsex() 
                                "<Actor.name> didn't say a word but smiled at me."
                
                            Else
                                "They seem very uncomfitable."
                
                            Endif
                        
                        Else
                            Player():: "There, much better."
                            "We continue our workout."
                        
                        Endif                
                    
                    Elseif 2
                        Player.strip()
                        "I procede to strip naked."
                        Actor:arousal += 15
                        Actor:arousal += 15
                        Love = 0
                        Love += Actor:attractiontoplayer
                        Love += Actor:perversion
                        Love += Actor:incest       
                        If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                            "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                            Actor.strip()
                            Actor(Flirty):: "Can you come over here and help me out."
                            Actor:arousal += 20
                            Player(Flirty):: "Sure."
                            Player:arousal += 20
                            If Player:arousal > 75
                                Player():: "How about a real work out?"
        
                                If Actor:arousal > 75 
                                    Actor()::"Just the workout I needed."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    Actor:fitness += 5
                                    Actor:attractiveness += 5
                                    Actor:muscle += 5
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                Endif
                        
                            Elseif Actor:arousal > 75 
                                "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                                "My resistance is futile."
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                Actor:fitness += 5
                                Actor:attractiveness += 5
                                Actor:muscle += 5
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                
                            Else
                                "We continue our workout." 
        
                            Endif
                    
                        Elseif Random(100, 150) < Love || Actor.hadsex() 
                            "<Actor.name> didn't say a word but smiled at me."
                
                        Else
                            "They seem very uncomfitable."
                
                        Endif
                
                    Else
                        "I decide to go change my cloths & come back."
                        Player.strip()
                        If Actor: perversion > 25
                            Player(Embarassed):: "What? Who's there?"
                            "I was getting changed when suddenly I heard a noise. Someone must be peeping on me!"
                            0:: "Get dressed quickly"
                            1:: "Take my time"
            
                            If 0 
                                Player.dressExcept(Outerwear, Top, Top_Under, Bottom, Headwear)
                                "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                            Else
                                perversion += Random(0, 0.1)        
                                "I won't lie: The thought of someone watching me getting changed turned me on ... I might as well take my time and give my peeping Tom a show to remember"
                            Endif
                                        
                        Else 
                            Player.dressExcept(Outerwear, Top, Top_Under, Bottom, Headwear)
                        
                        Endif
                    Endif
                
                
                
                Else
                    "I decide to tough through it."
                    mood -= 5
                    "I'm just a sweaty mess."
                    1:: "Shower here."
                    0:: "Shower at home."
        
                    If 1
                        "Lets hit the showers."
                        Player.strip()
                        setBackground(shower)
                        Filter(Shower)
                        Sex(Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        Player(Happy):: "Nothing feels better than a hot shower ..."
                        If Actor:perversion > 50 && Actor.isInterestedIn(Player) 
                            Player(Embarassed):: "What? Who's there?"
                            "I was enjoying a relaxing time in the shower when suddenly I heard a noise. Someone must be peeping on me!"
                            0:: "Get dressed"
                            1:: "Continue showering"
                
                            If 0 
                                Player.dress()
                                "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                            Else
                                perversion += Random(0, 0.1)        
                                "I won't lie: The thought of someone watching me shower turned me on even further ... I might as well give my peeping Tom a show to remember"
                                Actor:arousal += 15
                                If Actor:arousal > 75 && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                                    Actor.show(2)
                                    "As it turned out, it was no other than <Actor.name>, who had the cheek to come inside and offer to join me in the shower."
                                    0:: "Have sex in the shower"
                                    1:: "Turn <Actor.him_or_her> down"
                        
                                    If 0
                                    
                                        Sex(Actor, Player)
                                        Actor:fitness += 5
                                        Actor:attractiveness += 5
                                        Actor:muscle += 5
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                        
                                    Endif        
                                Endif
                            Endif
                        Endif
        
                    Endif
                Endif
            Endif       
        
        Elseif Player.isTrans()
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()]
                "During the workout <Actor.name> can't help but stare a my naked body."
                Actor:arousal += 15
                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:perversion
                Love += Actor:incest       
                If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                    "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                    Actor.strip()
                    Actor(Flirty):: "Can you come over here and help me out."
                    Actor:arousal += 20
                    Player(Flirty):: "Sure."
                    arousal += 20
                    If Player:arousal > 75
                        Player():: "How about a real work out?"
        
                        If Actor:arousal > 75 
                            Actor()::"Just the workout I needed."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            Actor:fitness += 5
                            Actor:attractiveness += 5
                            Actor:muscle += 5
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                        Endif
                        
                    Elseif Actor:arousal > 75
                        "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                        "My resistance is futile."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        perversion += Random(0, 0.5)
                        mood += 30
                        Actor:fitness += 5
                        Actor:attractiveness += 5
                        Actor:muscle += 5
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                
                    Else
                        "We continue our workout." 
        
                    Endif
        
        
                Elseif Random(100, 150) < Love || Actor.hadsex()
                    "<Actor.name> didn't say a word but smiled at me."
                
                Else
                    "They seem very uncomfitable."
                
                Endif
            
            Else
                "It is quite an intence workout. Staring to really sweat here."
                1:: "Ask to undress."
                2:: "Tough through it."
        
                If 1
                    Player():: "Mine if I take something off. I'm really getting hot."
                    Actor():: "Well if you need to go ahead."
                    1:: "Undress outer layer."
                    2:: perversion > 25:: "Get naked."
                    3:: "Go change"     
                
                    If 1
                        Player.strip(Bottom, Outerwear, Top, Foot, Headwear)(Bottom_Under, Top_Under, Eyewear)
                        "I procede to remove my shirt and pants."
                        If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()] 
                            "I soon realize I'm not wearing underwear."
                            Actor:arousal += 15
                            Love = 0
                            Love += Actor:attractiontoplayer
                            Love += Actor:perversion
                            Love += Actor:incest       
                            If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                                "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                                Actor.strip()
                                Actor(Flirty):: "Can you come over here and help me out."
                                Actor:arousal += 20
                                Player(Flirty):: "Sure."
                                Player:arousal += 20
                                If Player:arousal > 75
                                    Player():: "How about a real work out?"
        
                                    If Actor:arousal > 75 
                                        Actor()::"Just the workout I needed."
                                        Actor:attractiontoplayer += Random(1, 10)
                                        Actor:perversion += Random(5, 20)
                                        Sex(Player, Actor)
                                        perversion += Random(0, 0.5)
                                        mood += 30
                                        Actor:fitness += 5
                                        Actor:attractiveness += 5
                                        Actor:muscle += 5
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                        
                                    Endif
                        
                                Elseif Actor:arousal > 75 
                                    "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                                    "My resistance is futile."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    Actor:fitness += 5
                                    Actor:attractiveness += 5
                                    Actor:muscle += 5
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                
                                Else
                                    "We continue our workout." 
        
                                Endif
                    
                            Elseif Random(100, 150) < Love || Actor.hadsex()
                                "<Actor.name> didn't say a word but smiled at me."
                
                            Else
                                "They seem very uncomfitable."
                
                            Endif
                        
                        Else
                            Player():: "There, much better."
                            "We continue our workout."
                        
                        Endif                
                    
                    Elseif 2
                        Player.strip()
                        "I procede to strip naked."
                        Actor:arousal += 15
                        Love = 0
                        Love += Actor:attractiontoplayer
                        Love += Actor:perversion
                        Love += Actor:incest       
                        If Random(100, 200) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                            "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                            Actor.strip()
                            Actor(Flirty):: "Can you come over here and help me out."
                            Actor:arousal += 20
                            Player(Flirty):: "Sure."
                            arousal += 20
                            If Player:arousal > 75
                                Player():: "How about a real work out?"
        
                                If Actor:arousal > 75 
                                    Actor()::"Just the workout I needed."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    
                                Endif
                        
                            Elseif Actor:arousal > 75 
                            
                                "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                                "My resistance is futile."
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                Actor:fitness += 5
                                Actor:attractiveness += 5
                                Actor:muscle += 5
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                
                            Else
                                "We continue our workout." 
        
                            Endif
                    
                        Elseif Random(100, 150) < Love || Actor.hadsex()
                            "<Actor.name> didn't say a word but smiled at me."
                
                        Else
                            "They seem very uncomfitable."
                
                        Endif
                
                    Else
                        "I decide to go change my cloths & come back."
                        Player.strip()
                        If Actor: perversion > 25
                            Player(Embarassed):: "What? Who's there?"
                            "I was getting changed when suddenly I heard a noise. Someone must be peeping on me!"
                            0:: "Get dressed quickly"
                            1:: "Take my time"
            
                            If 0 
                                Player.dressExcept(Outerwear, Top, Top_Under, Bottom, Headwear)
                                "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                            Else
                                perversion += Random(0, 0.1)        
                                "I won't lie: The thought of someone watching me getting changed turned me on ... I might as well take my time and give my peeping Tom a show to remember"
                            Endif
                                        
                        Else 
                            Player.dressExcept(Outerwear, Top, Top_Under, Bottom, Headwear)
                        
                        Endif
                    Endif
                
                
                
                Else
                    "I decide to tough through it."
                    mood -= 5
                    "I'm just a sweaty mess."
                    1:: "Shower here."
                    0:: "Shower at home."
        
                    If 1
                        "Lets hit the showers."
                        Player.strip()
                        setBackground(shower)
                        Filter(Shower)
                        Sex(Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        Player(Happy):: "Nothing feels better than a hot shower ..."
                        Actor:arousal += 15
                        Love = 0
                        Love += Actor:attractiontoplayer
                        Love += Actor:perversion
                        Love += Actor:incest       
                        If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                            Player(Embarassed):: "What? Who's there?"
                            "I was enjoying a relaxing time in the shower when suddenly I heard a noise. Someone must be peeping on me!"
                            0:: "Get dressed"
                            1:: "Continue showering"
                
                            If 0 
                                Player.dress()
                                "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                            Else
                                perversion += Random(0, 0.1)        
                                "I won't lie: The thought of someone watching me shower turned me on even further ... I might as well give my peeping Tom a show to remember"
                                Actor:arousal += 15
                                If Actor:arousal > 75 
                                    Actor.show(2)
                                    "As it turned out, it was no other than <Actor.name>, who had the cheek to come inside and offer to join me in the shower."
                                    0:: "Have sex in the shower"
                                    1:: "Turn <Actor.him_or_her> down"
                        
                                    If 0
                                        Sex(Actor, Player)
                                        Actor:fitness += 5
                                        Actor:attractiveness += 5
                                        Actor:muscle += 5
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                    Endif        
                                Endif
                            Endif
                        Endif
        
                    Endif
                Endif
            Endif   
        
        Else
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()]
                "During the workout <Actor.name> can't help but stare a my naked body."
                Actor:arousal += 15
                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:perversion
                Love += Actor:incest 
                
                If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                
                    "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                    Actor.strip()
                    Actor(Flirty):: "Can you come over here and help me out."
                    Actor:arousal += 20
                    Player(Flirty):: "Sure."
                    arousal += 20
                    
                    If Player:arousal > 75
                    
                        Player():: "How about a real work out?"
        
                        If Actor:arousal > 75 
                        
                            Actor()::"Just the workout I needed."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            Actor:fitness += 5
                            Actor:attractiveness += 5
                            Actor:muscle += 5
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                        "My resistance is futile."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        perversion += Random(0, 0.5)
                        mood += 30
                        Actor:fitness += 5
                        Actor:attractiveness += 5
                        Actor:muscle += 5
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                
                    Else
                        "We continue our workout." 
        
                    Endif
        
        
                Elseif Random(100, 150) < Love || Actor.hadsex()
                    "<Actor.name> didn't say a word but smiled at me."
                
                Else
                    "They seem very uncomfitable."
                
                Endif
            
            Else
                "It is quite an intence workout. Staring to really sweat here."
                1:: "Ask to undress."
                2:: "Tough through it."
        
                If 1
                    Player():: "Mine if I take something off. I'm really getting hot."
                    Actor():: "Well if you need to go ahead."
                    1:: "Undress outer layer."
                    2:: perversion > 25:: "Get naked."
                    3:: "Go change"     
                
                    If 1
                        Player.strip(Bottom, Outerwear, Top, Foot, Headwear)
                        "I procede to remove my shirt and pants."
                        If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()] 
                            "I soon realize I'm not wearing underwear."
                            Actor:arousal += 15
                            Love = 0
                            Love += Actor:attractiontoplayer
                            Love += Actor:perversion
                            Love += Actor:incest       
                            If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                                "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                                Actor.strip()
                                Actor(Flirty):: "Can you come over here and help me out."
                                Actor:arousal += 20
                                Player(Flirty):: "Sure."
                                arousal += 20
                                If Player:arousal > 75
                                
                                    Player(Flirty):: "How about a real work out?" // Edit Raddeck
        
                                    If Actor:arousal > 75 
                                    
                                        Actor()::"Just the workout I needed."
                                        Actor:attractiontoplayer += Random(1, 10)
                                        Actor:perversion += Random(5, 20)
                                        Sex(Player, Actor)
                                        perversion += Random(0, 0.5)
                                        mood += 30
                                        Actor:fitness += 5
                                        Actor:attractiveness += 5
                                        Actor:muscle += 5
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                        
                                    Endif
                        
                                Elseif Actor:arousal > 75 
                                
                                    "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                                    "My resistance is futile."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    Actor:fitness += 5
                                    Actor:attractiveness += 5
                                    Actor:muscle += 5
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                
                                Else
                                    "We continue our workout." 
        
                                Endif
                    
                            Elseif Random(100, 150) < Love || Actor.hadsex()
                                "<Actor.name> didn't say a word but smiled at me."
                
                            Else
                                "They seem very uncomfitable."
                
                            Endif
                        
                        Else
                            Player():: "There, much better."
                            "We continue our workout."
                        
                        Endif                
                    
                    Elseif 2
                        Player.strip()
                        "I procede to strip naked."
                        Actor:arousal += 15
                        Love = 0
                        Love += Actor:attractiontoplayer
                        Love += Actor:perversion
                        Love += Actor:incest       
                        If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                            "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                            Actor.strip()
                            Actor(Flirty):: "Can you come over here and help me out."
                            Actor:arousal += 20
                            Player(Flirty):: "Sure."
                            arousal += 20
                            If Player:arousal > 75
                                Player():: "How about a real work out?"
        
                                If Actor:arousal > 75 
                                    Actor()::"Just the workout I needed."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    Actor:fitness += 5
                                    Actor:attractiveness += 5
                                    Actor:muscle += 5
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                        
                                Endif
                        
                            Elseif Actor:arousal > 75 
                                "<Actor.name> is starting to get too close for comfort. And now they are in a very suggestive position..."
                                "My resistance is futile."
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                Actor:fitness += 5
                                Actor:attractiveness += 5
                                Actor:muscle += 5
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                
                            Else
                                "We continue our workout." 
        
                            Endif
                    
                        Elseif Random(100, 150) < Love || Actor.hadsex()
                            "<Actor.name> didn't say a word but smiled at me."
                
                        Else
                            "They seem very uncomfitable."
                
                        Endif
                
                    Else
                        "I decide to go change my cloths & come back."
                        Player.strip()
                        If Actor: perversion > 25
                            Player(Embarassed):: "What? Who's there?"
                            "I was getting changed when suddenly I heard a noise. Someone must be peeping on me!"
                            0:: "Get dressed quickly"
                            1:: "Take my time"
            
                            If 0 
                                Player.dressExcept(Outerwear, Top, Top_Under, Bottom, Headwear)
                                "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                            Else
                                perversion += Random(0, 0.1)        
                                "I won't lie: The thought of someone watching me getting changed turned me on ... I might as well take my time and give my peeping Tom a show to remember"
                            Endif
                                        
                        Else 
                            Player.dressExcept(Outerwear, Top, Top_Under, Bottom, Headwear)
                        
                        Endif
                    Endif
                
                
                
                Else
                    "I decide to tough through it."
                    mood -= 5
                    "I'm just a sweaty mess."
                    1:: "Shower here."
                    0:: "Shower at home."
        
                    If 1
                        "Lets hit the showers."
                        Player.strip()
                        setBackground(shower)
                        Filter(Shower)
                        Sex(Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        Player(Happy):: "Nothing feels better than a hot shower ..."
                        If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50 
                            Player(Embarassed):: "What? Who's there?"
                            "I was enjoying a relaxing time in the shower when suddenly I heard a noise. Someone must be peeping on me!"
                            0:: "Get dressed"
                            1:: "Continue showering"
                
                            If 0 
                                Player.dress()
                                "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                            Else
                                perversion += Random(0, 0.1)        
                                "I won't lie: The thought of someone watching me shower turned me on even further ... I might as well give my peeping Tom a show to remember"
                                Actor:arousal += 15
                                If Actor:arousal > 75 && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                                    Actor.show(2)
                                    "As it turned out, it was no other than <Actor.name>, who had the cheek to come inside and offer to join me in the shower."
                                    0:: "Have sex in the shower"
                                    1:: "Turn <Actor.him_or_her> down"
                        
                                    If 0
                                        Sex(Actor, Player)
                                        Actor:fitness += 5
                                        Actor:attractiveness += 5
                                        Actor:muscle += 5
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                        
                                    Endif  
                                    
                                Endif
                                
                            Endif
                            
                        Endif
        
                    Endif
                    
                Endif
                
            Endif
            
        Endif
        
        Saved = getPerson(tag_Saved); 
        If Saved:perversion > 15 && !Saved.isMarried()
            Saved:perversion -= 1000
            Saved:masochist => 0
            Saved:incest => 0
        Endif
        
    SceneEnd() // Edited by Raddeck