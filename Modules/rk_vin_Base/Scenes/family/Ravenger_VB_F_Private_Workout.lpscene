WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: Actor = getSpecific(PassedOn)
OTHER: none

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        Actor.dressUniform(Sports) 
        Actor.show(2)
        fertility += 5
        "I am workingout with <Actor.name>."
        If Actor:fitness > 25
            "<Actor.name> is really good at teaching me how to workout." 
        
        else
            "<Actor.name> doesn't seem to know what <Actor.he_or_she> is doing."
        Endif
        Actor:fitness += 5
        Actor:attractiveness += 5
        Actor:muscle += 5
        fitness += 5
        attractiveness += 5
        muscle += 5
        
        
        If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)]
            If Player.ismale()
                "During the workout <Actor.name> can't stop staring at my dick."
            Else
                "During the workout <Actor.name> can't stop staring at my naked body."
            Endif   
            Actor:arousal += 15
            Love = 0
            Love += Actor:attractiontoplayer
            Love += Actor:perversion
            Love += Actor:incest       
            If Random(100, 200) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()] && Actor.isInterestedIn(Player)
                "I think <Actor.name> is  starting to get turned on. Because <Actor.he_or_she> strip naked too."
                Actor.strip()
                Actor(Flirty):: "Can you come over here and help me out."
                Actor:arousal += 20
                Player(Flirty):: "Sure."
                arousal += 20
                If Player:arousal > 75 && Player.isInterestedIn(Actor)
                    Player():: "How about a real work out?"
        
                    If Actor:arousal > 75 
                        Actor()::"Just the workout I needed."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        perversion += Random(0, 0.5)
                        mood += 30
                        Actor:fitness += 5
                        Actor:attractiveness += 5
                        Actor:muscle += 5
                    Endif
                        
                Elseif Actor:arousal > 75 && Player.isInterestedIn(Actor)
                    "<Actor.name> is starting to get too close for my comfort. And now <Actor.he_or_she> is in a very suggestive position..."
                    "My resistance is futile."
                    Actor:attractiontoplayer += Random(1, 10)
                    Actor:perversion += Random(5, 20)
                    Sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -1)
                    EndIf
                    perversion += Random(0, 0.5)
                    mood += 30
                    Actor:fitness += 5
                    Actor:attractiveness += 5
                    Actor:muscle += 5
                
                Else
                    "We continue our workout." 
                Endif
        
        
            Elseif Random(50, 150) < Love || Actor.isInterestedIn(Player)
                "<Actor.name> didn't say a word but smiled at me."
                
            Else
                "<Actor.He_or_She> seem very uncomfortable."
                
            Endif
            
        Else
            "It is quite an intence workout. Starting to really sweat in here."
            1:: "Ask if I can undress."
            2:: "I endure my sweat and keep going"
        
            If 1
                Player():: "Mind if I take something off. I'm really getting hot."
                Actor():: "Well if you need to go ahead."
                1:: "Undress outer layer."
                2:: perversion > 25:: "Get naked."
                3:: "Go change"     
                
                If 1
                    Player.strip(Bottom, Outerwear, Top, Foot, Headwear)
                    "I procede to remove my shirt and pants."
                    If !Player.isVisible(Bottom_Under) 
                        "I soon realize I'm not wearing underwear."
                        Actor:arousal += 15
                        Love = 0
                        Love += Actor:attractiontoplayer
                        Love += Actor:perversion
                        Love += Actor:incest       
                        If Random(100, 200) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()] && Actor.isInterestedIn(Player)
                            "I think <Actor.name> is  starting to get turned on. Because <Actor.he_or_she> strip naked too."
                            Actor.strip()
                            Actor(Flirty):: "Can you come over here and help me out."
                            Actor:arousal += 20
                            Player(Flirty):: "Sure."
                            arousal += 20
                            If Player:arousal > 75 && Player.isInterestedIn(Actor)
                                Player():: "How about a real work out?"
        
                                If Actor:arousal > 75 
                                    Actor()::"Just the workout I needed."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    Actor:fitness += 5
                                    Actor:attractiveness += 5
                                    Actor:muscle += 5
                                Endif
                        
                            Elseif Actor:arousal > 75 && Player.isInterestedIn(Actor)
                                "<Actor.name> is starting to get too close for my comfort. And now <Actor.he_or_she> is in a very suggestive position..."
                                "My resistance is futile."
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                perversion += Random(0, 0.5)
                                mood += 30
                                Actor:fitness += 5
                                Actor:attractiveness += 5
                                Actor:muscle += 5
                
                            Else
                                "We continue our workout." 
        
                            Endif
                    
                        Elseif Random(50, 150) < Love || Actor.isInterestedIn(Player) 
                            "<Actor.name> didn't say a word but smiled at me."
                
                        Else
                            "<Actor.He_or_She> seem very uncomfortable."
                
                        Endif
                        
                    Else
                        Player():: "There, much better."
                        "We continue our workout."
                        
                    Endif                
                    
                Elseif 2
                    Player.strip()
                    "I procede to strip naked."
                    Actor:arousal += 15
                    Love = 0
                    Love += Actor:attractiontoplayer
                    Love += Actor:perversion
                    Love += Actor:incest       
                    If Random(100, 200) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()] && Actor.isInterestedIn(Player)
                        "I think <Actor.name> is  starting to get turned on. Because <Actor.he_or_she> strip naked too."
                        Actor.strip()
                        Actor(Flirty):: "Can you come over here and help me out."
                        Actor:arousal += 20
                        Player(Flirty):: "Sure."
                        Player:arousal += 20
                        If Player:arousal > 75 && Player.isInterestedIn(Actor)
                            Player():: "How about a real work out?"
        
                            If Actor:arousal > 75
                                Actor()::"Just the workout I needed."
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                perversion += Random(0, 0.5)
                                mood += 30
                                Actor:fitness += 5
                                Actor:attractiveness += 5
                                Actor:muscle += 5
                            Endif
                        
                        Elseif Actor:arousal > 75 && Player.isInterestedIn(Actor)
                            "<Actor.name> is starting to get too close for my comfort. And now <Actor.he_or_she> is in a very suggestive position..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            perversion += Random(0, 0.5)
                            mood += 30
                            Actor:fitness += 5
                            Actor:attractiveness += 5
                            Actor:muscle += 5
                
                        Else
                            "We continue our workout." 
        
                        Endif
                    
                    Elseif Actor:perversion > 25 && Actor.isInterestedIn(Player) 
                        "<Actor.name> didn't say a word but smiled at me."
                
                    Else
                        "<Actor.He_or_She> seem very uncomfortable."
                
                    Endif
                
                Else
                    "I decide to go change my cloths & come back."
                    Player.strip()
                    If Actor: perversion > 25
                        Player(Embarassed):: "What? Who's there?"
                        "I was getting changed when suddenly I heard a noise. Someone must be peeping on me!"
                        0:: "Get dressed quickly"
                        1:: "Take my time"
            
                        If 0 
                            Player.dressUniform(Sports)
                           "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                        Else
                            perversion += Random(0, 0.1)        
                            "I won't lie: The thought of someone watching me getting changed turned me on ... I might as well take my time and give my peeping Tom a show to remember"
                        Endif
                                        
                    Else 
                        Player.dressUniform(Sports)
                        
                    Endif
                Endif
                                
            Else 
                "I decide to tough through it."
                mood -= 5
                "I'm now just a sweaty mess."
                
                If WHERE == sports_centre
                    1:: "Shower here."
                    0:: "Shower at home."
        
                    If 1
                        "Lets hit the showers."
                        Actor.hide()
                        Player.strip()
                        Player.setNoRedress()
                        Actor.strip()
                        Actor.setNoRedress()
                        Love = 0
                        Love += Actor:attractiontoplayer
                        Love += Actor:perversion
                        Love += Actor:incest      
                        setBackground3D(Modules\Ravenger_Scenes\vin_Base\World\Public_Shower.lpworld)
                        Player.animate(Shower)
                        Player(Happy):: "Nothing feels better than a hot shower ..."
                        Filter(Shower)
                        Sex(Player)
                        If Actor:perversion > 75 && Actor.isInterestedIn(Player) && !Actor.issamegender(Player) 
                            Actor.show()
                            Actor.animate(Shower)
                            "What the hell! <Actor.name> is joining me in the showers too."
                            Actor:arousal += 20
                            Player:arousal += 20
                            Filter(Shower)
                            Sex(Actor)
                            Love += Actor:arousal
                            If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()] && Player.isInterestedIn(Actor)
                                If Player:arousal > 75 && Player.isInterestedIn(Actor)
                                    Player():: "Do you need help washing your back?"
        
                                    If Actor:arousal > 75
                                        Actor():: "It why I'm here."
                                        Actor:attractiontoplayer += Random(1, 10)
                                        Actor:perversion += Random(5, 20)
                                        Sex(Player, Actor)
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                        perversion += Random(0, 0.5)
                                        mood += 30
        
                                    Endif
                        
                                Elseif Actor:arousal > 75 && [Player.isInterestedIn(Actor) || Actor:masochist < -30]
                                    "<Actor.name> is starting to get too close for my comfort. And now <Actor.he_or_she> is in a very suggestive position..."
                                    "My resistance is futile."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    perversion += Random(0, 0.5)
                                    mood += 30
                
                                Else
                                    "We soon finish our shower." 
                                Endif
                            Else
                                "We soon finish our shower."    
                            Endif
                            
                        Elseif Actor:perversion > 50 && [Actor.issamegender(Player) || Player.istrans() || Actor.istrans()]
                            Actor.show()
                            Actor.animate(Shower)
                            "<Actor.name> is joining me in the showers too."
                            If Player.isInterestedIn(Actor)
                                Player:arousal += 20
                            Endif
                            If Actor.isInterestedIn(Player)
                                Actor:arousal += 20
                            Endif
                            Filter(Shower)
                            Sex(Actor)
                            Love += Actor:arousal
                            If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()] && Player.isInterestedIn(Actor)
                                If Player:arousal > 75 && Player.isInterestedIn(Actor)
                                    Player():: "Do you need help washing your back?"
        
                                    If Actor:arousal > 75 && Actor.isInterestedIn(Player)
                                        Actor():: "It why I'm here."
                                        Actor:attractiontoplayer += Random(1, 10)
                                        Actor:perversion += Random(5, 20)
                                        Sex(Player, Actor)
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -1)
                                        EndIf
                                        perversion += Random(0, 0.5)
                                        mood += 30
                                    Else
                                        Actor():: "No thanks I'm good."
                                    
                                    Endif
                        
                                Elseif Actor:arousal > 75 && [Player.isInterestedIn(Actor) || Actor:masochist < -30] && Actor.isInterestedIn(Player)
                                    "<Actor.name> is starting to get too close for my comfort. And now <Actor.he_or_she> is in a very suggestive position..."
                                    "My resistance is futile."
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    perversion += Random(0, 0.5)
                                    mood += 30
                
                                Else
                                    "We soon finish our shower." 
                                Endif
                            Else
                                "We soon finish our shower."
                            
                            Endif   
                        Else
                            "I was enjoying a relaxing time in the shower"
                            mood += 10              
                        Endif
                    Endif
                Elseif WHERE == home
                    "Lets hit the showers."
                    Player.strip()
                    setBackground(shower)
                    Filter(Shower)
                    Sex(Player)
                    Player(Happy):: "Nothing feels better than a hot shower ..."
                    Love = 0
                    Love += Actor:attractiontoplayer
                    Love += Actor:perversion
                    Love += Actor:incest            
                    If Actor:perversion > 50 && Actor.isInterestedIn(Player) 
                        Player(Embarassed):: "What? Who's there?"
                        "I was enjoying a relaxing time in the shower when suddenly I heard a noise. Someone must be peeping on me!"
                        0:: "Get dressed"
                        1:: "Continue showering"
                
                        If 0 
                            Player.dress()
                            "I quickly put some clothes on and went looking around but couldn't find anyone. Whoever the peeping Tom was must have quick feet."
                        Else
                            perversion += Random(0, 0.1)        
                            "I won't lie: The thought of someone watching me shower turned me on even further ... I might as well give my peeping Tom a show to remember"
                            Actor:arousal += 15
                            Love += Actor:arousal
                            If Random(150, 250) < Love && [isModEnabled(vin_Incest) || !Actor.isRelative()]
                                Actor.show(2)
                                Actor.strip()
                                "As it turned out, it was no other than <Actor.name>, who had the cheek to come inside and offer to join me in the shower."
                                0:: "Have sex in the shower"
                                1:: "Turn <Actor.him_or_her> down"
                        
                                If 0
                                    Sex(Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                Endif        
                            Endif
                        Endif
                    Else
                        "I was enjoying a relaxing time in the shower"
                        mood += 10
                    Endif   
                Else
                    mood -= 10
                Endif
            Endif       
        Endif
        
        Saved = getPerson(tag_Saved); 
        If Saved:perversion > 15 && !Saved.isMarried()
            Saved:perversion -= 1000
            Saved:masochist => 0
            Saved:incest => 0
        Endif
        
    SceneEnd() // Edited by Raddeck