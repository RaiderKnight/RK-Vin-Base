WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: Actor = getSpecific(PassedOn)
OTHER: none

    SceneStart()
    
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        Actor.dress() 
        Actor.show(2)
        "I am reading a book with <Actor.name>."
        If Actor:intelligence > 25
            "They have a strong understanding of what is going on in book." 
            intelligence += 5
        else
            "<Actor.name> doesn't seem to understand the book."
        Endif
        Actor:intelligence += 5
        intelligence += 5
        
        If Player.isMale()
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)]
                "While reading <Actor.name> can't help but stare a my dick."
                Actor:arousal += 15
                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:incest
                Love += Actor:perversion
                        
                If Random(150, 250) < Love && isModEnabled(vin_Incest)
                
                    "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                    Actor.strip()
                    Actor(Flirty):: "I really like this part here..."
                    Actor:arousal += 20
                    Player(Flirty):: "Think that is my favorite part too..."
                    arousal += 20
                    
                    If Player:arousal > 75
                    
                        Player():: "Would you like to play with my favorite part?"
        
                        If Actor:arousal > 75 
                        
                            Actor()::"I was thinking the same thing."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                        "My resistance is futile."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        perversion += Random(0, 0.5)
                        mood += 30
                        // Start edit Raddeck
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        // End edit Raddeck
                
                    Else
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:rapportwithplayer += 1 // Edit by Raddeck
                        
                    Endif
        
                Elseif Random(100, 150) < Love || Actor.hadsex()
                
                    "<Actor.name> didn't say a word but smiled at me."
                
                Else
                
                    "They seem very uncomfitable."
                
                Endif
            
            
            Else
            
                "I whant to read something else."
                1:: "Look for exotic book."
                2:: "Look for new book."
        
                If 1
                
                    Player():: "Would you like to read this book?"
                    Actor():: "Not sure what is it about?"
                    1:: "Romance."
                    2:: perversion > 25:: "Henti."
                    3:: "Erotic Romance"     
                
                    If 1
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:attractiontoplayer += Random(1, 5)
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                        
                    Elseif 2
                    
                        "I think <Actor.name> is starting to get turned on from their book."
                        Actor(Flirty):: "I really like this part here..."
                        Actor:arousal += 5
                        "I begin reading it. It is quite erotic"
                        arousal += 5
                        perversion += 0.5
                        If Player:arousal > 75 && isModEnabled(vin_Incest)
                            Player(Flirty):: "Would you like to roleplay?"
        
                            If Actor:arousal > 75 
                                Actor(Flirty)::"I was thinking the same thing."
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                
                                // Start edit Raddeck
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                // End edit Raddeck
                        
                            Endif    
                        Elseif Actor:arousal > 75 && isModEnabled(vin_Incest)
                            "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                
                        Else
                            "We continue our reading." 
                            Actor:intelligence += 5 
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
        
                        Endif
                
                
                    Else
                        If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50
                        
                            "<Actor.name> leaves the room."
                            "I should see where <Actor.he_or_she> is going..."
                            
                                SneakGame()
                                If Random(0, 50) < sneak
                                
                                    Actor.strip()
                                    Actor.show(2)
                                    Filter(Solo)
                                    Sex(Actor)
                                    Actor.show(2)
                                    Actor(Crying):: "Yes, yes, yes!"
                                    // Start edit Raddeck
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    // End edit Raddeck
                                    
                                    "Wow, <Actor.he_or_she> is really into it. I'm getting very aroused ... I can't help but wish that I had the courage to join <Actor.him_or_her>..."
                                    "Maybe I do have the courage for that ..."
                                    0:: isModEnabled(vin_Incest) :: "Offer to join <Actor.him_or_her>"
                                    1:: "Enough"
                
                                    If 0
                                        Player.show(0)
                                        Player(Flirty):: "Looks like you could do with some company, my dear <Actor.relationship>? Mind if I join you?"
                                        If Random(25, 125) < Actor:attractiontoplayer + Actor:perversion
                                            Actor(Flirty):: "Come and join me then, what are you waiting for?"
                                            Actor:attractiontoplayer += Random(1, 10)
                                            Actor:perversion += Random(5, 20)
                                            Sex(Player, Actor)
                                            perversion += Random(0, 0.5)
                                            mood += 30
                                            // Start edit Raddeck
                                            If HungerEnable > 0
                                                Player.modifyActorVar(rk_VB_Hunger, -1)
                                            EndIf
                                            // End edit Raddeck
        
                                        Else
                                        
                                            Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal?"
                                            Actor:rapportwithplayer -= Random(0, 3)
                                            
                                        Endif
                                        
                                    Else
                                    
                                        "I should get out of here before I get caught."
                                        
                                    Endif
                                    
                                Else
                                
                                    "Despite my best efforts, I couldn't get a look."
                                    sneak -= 0.5 // Edit Raddeck
                                    
                                Endif
                
                            Endif
                            
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                            
                        Endif
                
                    Endif
                    
                Else
                
                    "I decide to just read a normal book."
                    mood += 5
                    
                    If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50 && isModEnabled(vin_Incest)
                    
                        "I think <Actor.name> is starting to get turned on from their book."
                        Actor(Flirty):: "I really like this part here..."
                        Actor:arousal += 5
                        "I begin reading it. It is quite erotic"
                        arousal += 5
                        perversion += 0.5
                        If Player:arousal > 75
                            Player(Flirty):: "Would you like to roleplay?" // Edit Raddeck
        
                            If Actor:arousal > 75 
                            
                                Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                // Start edit Raddeck
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                // End edit Raddeck
                            Endif 
                            
                        Elseif Actor:arousal > 75 && isModEnabled(vin_Incest)
                        
                            "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                
                        Else
                            "We continue our reading." 
                            Actor:intelligence += 5
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck                            
        
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> leaves the room."
                        "I should see where <Actor.he_or_she> is going..."
                        
                        SneakGame()
                        If Random(0, 50) < sneak
                            Actor.strip()
                            Actor.show(2)
                            Filter(Solo)
                            Sex(Actor)
                            Actor.show(2)
                            Actor(Crying):: "Yes, yes, yes!"
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                            
                            "Wow, <Actor.he_or_she> is really into it. I'm getting very aroused ... I can't help but wish that I had the courage to join <Actor.him_or_her>..."
                            "Maybe I do have the courage for that ..."
                            0:: isModEnabled(vin_Incest) :: "Offer to join <Actor.him_or_her>"
                            1:: "Enough"
                
                            If 0
                            
                                Player.show(0)
                                Player(Flirty):: "Looks like you could do with some company, my dear <Actor.relationship>? Mind if I join you?"
                                
                                If Random(25, 125) < Actor:attractiontoplayer + Actor:perversion
                                
                                    Actor(Flirty):: "Come and join me then, what are you waiting for?"
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    // Start edit Raddeck
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    // End edit Raddeck
        
                                Else
                                
                                    Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal?"
                                    Actor:rapportwithplayer -= Random(0, 3)
                                    
                                Endif
                                
                            Else
                            
                                "I should get out of here before I get caught."
                                
                            Endif
                            
                        Else
                        
                            "Despite my best efforts, I couldn't get a look."
                            sneak -= 0.5 // Edit Raddeck
                            
                        Endif
                
                    Else
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                        
                    Endif
                    
                Endif
            Endif       
        
        Elseif Player.isTrans()
        
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()]
            
                "While reading <Actor.name> can't help but stare a my naked body."
                Actor:arousal += 15
                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:incest
                Love += Actor:perversion
                        
                If Random(150, 250) < Love && isModEnabled(vin_Incest)
                
                    "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                    Actor.strip()
                    Actor(Flirty):: "I really like this part here..."
                    Actor:arousal += 20
                    
                    Player(Flirty):: "Think that is my favorite part too..."
                    arousal += 20
                    
                    If Player:arousal > 75
                    
                        Player(Flirty):: "Would you like to play with my favorite part?" // Edit Raddeck
        
                        If Actor:arousal > 75 
                        
                            Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                                    
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                        "My resistance is futile."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        perversion += Random(0, 0.5)
                        mood += 30
                        // Start edit Raddeck
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        // End edit Raddeck
                
                    Else
                    
                        "We continue our reading." 
                        Actor:intelligence += 5 
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                        
                    Endif
        
                Elseif Random(100, 150) < Love || Actor.hadsex()
                
                    "<Actor.name> didn't say a word but smiled at me."
                
                Else
                
                    "They seem very uncomfitable."
                
                Endif
            
            Else
            
                "I whant to read something else."
                1:: "Look for exotic book."
                2:: "Look for new book."
        
                If 1
                
                    Player(Serene):: "Would you like to read this book?" // Edit Raddeck
                    Actor(Anxious):: "Not sure what is it about?" // Edit Raddeck
                    1:: "Romance."
                    2:: perversion > 25:: "Henti."
                    3:: "Erotic Romance"     
                
                    If 1
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:attractiontoplayer += Random(1, 5)
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                        
                    Elseif 2
                    
                        "I think <Actor.name> is starting to get turned on from their book."
                        Actor(Flirty):: "I really like this part here..."
                        Actor:arousal += 5
                        "I begin reading it. It is quite erotic"
                        arousal += 5
                        perversion += 0.5
                        
                        If Player:arousal > 75 && isModEnabled(vin_Incest)
                        
                            Player(Flirty):: "Would you like to roleplay?" // Edit Raddeck
        
                            If Actor:arousal > 75 
                            
                                Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                // Start edit Raddeck
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                // End edit Raddeck
                                
                            Endif
                            
                        Elseif Actor:arousal > 75 && isModEnabled(vin_Incest)
                        
                            "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
        
                        Endif
                
                
                    Else
                        If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50
                        
                            "<Actor.name> leaves the room."
                            "I should see where <Actor.he_or_she> is going..."
                                SneakGame()
                                If Random(0, 50) < sneak
                                
                                    Actor.strip()
                                    Actor.show(2)
                                    Filter(Solo)
                                    Sex(Actor)
                                    Actor.show(2)
                                    Actor(Crying):: "Yes, yes, yes!"
                                    "Wow, <Actor.he_or_she> is really into it. I'm getting very aroused ... I can't help but wish that I had the courage to join <Actor.him_or_her>..."
                                    "Maybe I do have the courage for that ..."
                                    0:: isModEnabled(vin_Incest) :: "Offer to join <Actor.him_or_her>"
                                    1:: "Enough"
                
                                    If 0
                                    
                                        Player.show(0)
                                        Player(Flirty):: "Looks like you could do with some company, my dear <Actor.relationship>? Mind if I join you?"
                                        
                                        If Random(25, 125) < Actor:attractiontoplayer + Actor:perversion
                                        
                                            Actor(Flirty):: "Come and join me then, what are you waiting for?"
                                            Actor:attractiontoplayer += Random(1, 10)
                                            Actor:perversion += Random(5, 20)
                                            Sex(Player, Actor)
                                            perversion += Random(0, 0.5)
                                            mood += 30
                                            // Start edit Raddeck
                                            If HungerEnable > 0
                                                Player.modifyActorVar(rk_VB_Hunger, -1)
                                            EndIf
                                            // End edit Raddeck
        
                                        Else
                                        
                                            Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal?"
                                            Actor:rapportwithplayer -= Random(0, 3)
                                            
                                        Endif
                                        
                                    Else
                                    
                                        "I should get out of here before I get caught."
                                        
                                    Endif
                                    
                                Else
                                
                                    "Despite my best efforts, I couldn't get a look."
                                    sneak -= 0.5 // Edit Raddeck
                                    
                                Endif
                
                            Endif
                            
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                            
                        Endif
                
                    Endif
                    
                Else
                
                    "I decide to just read a normal book."
                    mood += 5
                    
                    If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50
                    
                        "I think <Actor.name> is starting to get turned on from their book."
                        Actor(Flirty):: "I really like this part here..."
                        Actor:arousal += 5
                        "I begin reading it. It is quite erotic"
                        arousal += 5
                        perversion += 0.5
                        
                        If Player:arousal > 75 && isModEnabled(vin_Incest)
                        
                            Player(Flirty):: "Would you like to roleplay?" // Edit Raddeck
        
                            If Actor:arousal > 75 
                            
                                Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                // Start edit Raddeck
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                // End edit Raddeck
                                
                            Endif  
                            
                        Elseif Actor:arousal > 75 && isModEnabled(vin_Incest)
                        
                            "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck                            
        
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> leaves the room."
                        "I should see where <Actor.he_or_she> is going..."
                        SneakGame()
                        If Random(0, 50) < sneak
                        
                            Actor.strip()
                            Actor.show(2)
                            Filter(Solo)
                            Sex(Actor)
                            Actor.show(2)
                            Actor(Crying):: "Yes, yes, yes!"
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                            "Wow, <Actor.he_or_she> is really into it. I'm getting very aroused ... I can't help but wish that I had the courage to join <Actor.him_or_her>..."
                            "Maybe I do have the courage for that ..."
                            0:: isModEnabled(vin_Incest) :: "Offer to join <Actor.him_or_her>"
                            1:: "Enough"
                
                            If 0
                            
                                Player.show(0)
                                Player(Flirty):: "Looks like you could do with some company, my dear <Actor.relationship>? Mind if I join you?"
                                
                                If Random(25, 125) < Actor:attractiontoplayer + Actor:perversion
                                
                                    Actor(Flirty):: "Come and join me then, what are you waiting for?"
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    // Start edit Raddeck
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    // End edit Raddeck
        
                                Else
                                
                                    Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal?"
                                    Actor:rapportwithplayer -= Random(0, 3)
                                    
                                Endif
                                
                            Else
                            
                                "I should get out of here before I get caught."
                                
                            Endif
                            
                        Else
                        
                            "Despite my best efforts, I couldn't get a look."
                            sneak -= 0.5 // Edit Raddeck
                            
                        Endif
                
                    Else
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                        
                    Endif
                    
                Endif
                
            Endif       
        
        Else
        
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()]
            
                "While reading <Actor.name> can't help but stare a my naked body."
                Actor:arousal += 15
                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:incest
                Love += Actor:perversion
                        
                If Random(150, 250) < Love && isModEnabled(vin_Incest)
                
                    "I think <Actor.name> is starting to get turned on. Because they strip naked too."
                    Actor.strip()
                    Actor(Flirty):: "I really like this part here..."
                    Actor:arousal += 20
                    
                    Player(Flirty):: "Think that is my favorite part too..."
                    arousal += 20
                    
                    If Player:arousal > 75 
                    
                        Player():: "Would you like to play with my favorite part?"
        
                        If Actor:arousal > 75 
                        
                            Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                            
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                        "My resistance is futile."
                        Actor:attractiontoplayer += Random(1, 10)
                        Actor:perversion += Random(5, 20)
                        Sex(Player, Actor)
                        perversion += Random(0, 0.5)
                        mood += 30
                        // Start edit Raddeck
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -1)
                        EndIf
                        // End edit Raddeck
                
                    Else
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck                        
                        
                    Endif
        
                Elseif Random(100, 150) < Love || Actor.hadsex()
                
                    "<Actor.name> didn't say a word but smiled at me."
                
                Else
                
                    "They seem very uncomfitable."
                
                Endif
            
            Else
            
                "I whant to read something else."
                1:: "Look for exotic book."
                2:: "Look for new book."
        
                If 1
                
                    Player(Serene):: "Would you like to read this book?" // Edit Raddeck
                    Actor(Anxious):: "Not sure what is it about?" // Edit Raddeck
                    1:: "Romance."
                    2:: perversion > 25:: "Henti."
                    3:: "Erotic Romance"     
                
                    If 1
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:attractiontoplayer += Random(1, 5)
                        
                    Elseif 2
                    
                        "I think <Actor.name> is starting to get turned on from their book."
                        Actor(Flirty):: "I really like this part here..."
                        Actor:arousal += 5
                        "I begin reading it. It is quite erotic"
                        arousal += 5
                        perversion += 0.5
                        
                        If Player:arousal > 75 && isModEnabled(vin_Incest)
                        
                            Player():: "Would you like to roleplay?"
        
                            If Actor:arousal > 75 
                            
                                Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                // Start edit Raddeck
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                // End edit Raddeck
                        
                            Endif
                            
                        Elseif Actor:arousal > 75 && isModEnabled(vin_Incest)
                        
                            "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                        
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5 
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
        
                        Endif
                
                
                    Else
                    
                        If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50
                        
                            "<Actor.name> leaves the room."
                            "I should see where <Actor.he_or_she> is going..."
                                SneakGame()
                                If Random(0, 50) < sneak
                                    Actor.strip()
                                    Actor.show(2)
                                    Filter(Solo)
                                    Sex(Actor)
                                    Actor.show(2)
                                    Actor(Crying):: "Yes, yes, yes!"
                                    // Start edit Raddeck
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    // End edit Raddeck
                                    "Wow, <Actor.he_or_she> is really into it. I'm getting very aroused ... I can't help but wish that I had the courage to join <Actor.him_or_her>..."
                                    "Maybe I do have the courage for that ..."
                                    0:: isModEnabled(vin_Incest) :: "Offer to join <Actor.him_or_her>"
                                    1:: "Enough"
                
                                    If 0
                                    
                                        Player.show(0)
                                        Player(Flirty):: "Looks like you could do with some company, my dear <Actor.relationship>? Mind if I join you?"
                                        
                                        If Random(25, 125) < Actor:attractiontoplayer + Actor:perversion
                                        
                                            Actor(Flirty):: "Come and join me then, what are you waiting for?"
                                            Actor:attractiontoplayer += Random(1, 10)
                                            Actor:perversion += Random(5, 20)
                                            Sex(Player, Actor)
                                            perversion += Random(0, 0.5)
                                            mood += 30
                                            // Start edit Raddeck
                                            If HungerEnable > 0
                                                Player.modifyActorVar(rk_VB_Hunger, -1)
                                            EndIf
                                            // End edit Raddeck
        
                                        Else
                                        
                                            Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal?"
                                            Actor:rapportwithplayer -= Random(0, 3)
                                            
                                        Endif
                                        
                                    Else
                                    
                                        "I should get out of here before I get caught."
                                        
                                    Endif
                                    
                                Else
                                
                                    "Despite my best efforts, I couldn't get a look."
                                    sneak -= 0.5 // Edit Raddeck
                                    
                                Endif
                
                            Endif
                            
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                            
                        Endif
                
                    Endif
                    
                Else
                
                    "I decide to just read a normal book."
                    mood += 5
                    
                    If Actor:perversion > 50 && (Actor:attractiontoplayer || Actor:incest) > 50
                    
                        "I think <Actor.name> is starting to get turned on from their book."
                        Actor(Flirty):: "I really like this part here..."
                        Actor:arousal += 5
                        "I begin reading it. It is quite erotic"
                        arousal += 5
                        perversion += 0.5
                        
                        If Player:arousal > 75 && isModEnabled(vin_Incest)
                        
                            Player():: "Would you like to roleplay?"
        
                            If Actor:arousal > 75 
                            
                                Actor(Flirty)::"I was thinking the same thing." // Edit Raddeck
                                Actor:attractiontoplayer += Random(1, 10)
                                Actor:perversion += Random(5, 20)
                                Sex(Player, Actor)
                                perversion += Random(0, 0.5)
                                mood += 30
                                // Start edit Raddeck
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -1)
                                EndIf
                                // End edit Raddeck
                                
                            Endif    
                            
                        Elseif Actor:arousal > 75 && isModEnabled(vin_Incest)
                        
                            "<Actor.name> is starting to get too close for comfort. I think the part they were mentioning was mine..."
                            "My resistance is futile."
                            Actor:attractiontoplayer += Random(1, 10)
                            Actor:perversion += Random(5, 20)
                            Sex(Player, Actor)
                            perversion += Random(0, 0.5)
                            mood += 30
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                
                        Else
                        
                            "We continue our reading." 
                            Actor:intelligence += 5 
                            Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
        
                        Endif
                        
                    Elseif Actor:arousal > 75
                    
                        "<Actor.name> leaves the room."
                        "I should see where <Actor.he_or_she> is going..."
                        SneakGame()
                        If Random(0, 50) < sneak
                        
                            Actor.strip()
                            Actor.show(2)
                            Filter(Solo)
                            Sex(Actor)
                            Actor.show(2)
                            Actor(Crying):: "Yes, yes, yes!"
                            // Start edit Raddeck
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -1)
                            EndIf
                            // End edit Raddeck
                            "Wow, <Actor.he_or_she> is really into it. I'm getting very aroused ... I can't help but wish that I had the courage to join <Actor.him_or_her>..."
                            "Maybe I do have the courage for that ..."
                            0:: isModEnabled(vin_Incest) :: "Offer to join <Actor.him_or_her>"
                            1:: "Enough"
                
                            If 0
                            
                                Player.show(0)
                                Player(Flirty):: "Looks like you could do with some company, my dear <Actor.relationship>? Mind if I join you?"
                                
                                If Random(25, 125) < Actor:attractiontoplayer + Actor:perversion
                                    Actor(Flirty):: "Come and join me then, what are you waiting for?"
                                    Actor:attractiontoplayer += Random(1, 10)
                                    Actor:perversion += Random(5, 20)
                                    Sex(Player, Actor)
                                    perversion += Random(0, 0.5)
                                    mood += 30
                                    // Start edit Raddeck
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -1)
                                    EndIf
                                    // End edit Raddeck
        
                                Else
                                
                                    Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal?"
                                    Actor:rapportwithplayer -= Random(0, 3)
                                    
                                Endif
                                
                            Else
                            
                                "I should get out of here before I get caught."
                                
                            Endif
                            
                        Else
                        
                            "Despite my best efforts, I couldn't get a look."
                            sneak -= 0.5 // Edit Raddeck
                            
                        Endif
                
                    Else
                    
                        "We continue our reading." 
                        Actor:intelligence += 5
                        Actor:rapportwithplayer += Random(0, 1) // Edit Raddeck
                        
                    Endif
                    
                Endif
                
            Endif  
            
        Endif
        
    SceneEnd() // Edited by Raddeck