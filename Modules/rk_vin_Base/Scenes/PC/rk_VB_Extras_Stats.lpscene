WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    
        "Information page for extra player stats"
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        
        If HungerEnable > 0
            "Hunger level | <Hunger>/100"
        EndIf

        
    SceneEnd()