WHAT: all, -sleep, -sleep_hotel
WHERE: all, -work
WHEN: 9-23
WHO: Friend = getSpecific(Impregnated); If Friend.isInterestedIn(Player) && !Friend.isDating() && Friend.hadSex() && Friend.isPregnant() && Random(50, 100) < Friend:perversion && Random(50, 100) < Friend:attractiontoplayer
OTHER: !IsWithCompanion() && !isWithCompanionCreature()

    sceneStart()
        isLLSlave = Friend.getActorVar(Slave)
        
        If Friend.isPregnant() && isLLSlave == 0
            pregday = Friend.countPregnancyDays()
            XBPisPregnant = Friend.getActorVar(BPisPregnant)
            
            If pregday > 60 || XBPisPregnant > 0
                
                Friend.dress()
                "My phone rang ... It's <Friend.name> calling."
                Friend.show(2)
                
                If masochist < 75 && Friend:masochist > -75
                    Friend(Flirty):: "Hey <Player.beautiful_or_handsome>! Listen, I'm pregnant ... uhm ... my hormones ... and ... I'm feeling lonely."
                    Friend(Flirty):: "I've got to see that sexy <Player.body_or_ass> of yours again today!"
                Else
                
                    Friend(Flirty):: "Hey <Player.beautiful_or_handsome>! I feels lonely, do you want to hang out with your favorite pregnant lady?" // Edited by Raddeck
                    
                Endif
                
                "Go on a date with <Friend.name>?"
                0:: "Yes"
                1:: "No"

                If choice != 0 && masochist > 75 && Friend:masochist < -75 && isModEnabled(vin_NTR)
                
                    Player(Anxious):: "(I can't ... I'm too afraid to say no ...)"
                    Player(Anxious):: "(I'll do what <Friend.he_or_she> wants ...)"
                    Friend(Flirty)::"Good? You are such a good <Player.boy_or_girl>." // Edited by Raddeck
                    passTime(0.25, 1)
                    Friend.startDate()
                    Friend.show(4)
                    Player.show(0)
                    masochist += 0.5  // Edited by Raddeck
                    
                Elseif 0
                
                    Player(Happy):: "Of course. I'll see you in a bit!"
                    Friend(Serene):: "See you soon!"  // Edited by Raddeck
                    Friend:arousal => 100
                    Friend:attractiontoplayer += Random(0,10)
                    passTime(0.25, 1)
                    Friend.StartDate()
                    perversion += Random(0, 0.5)
                    
                Else
                    Player:: "Sorry, I'm busy today. Maybe next time ..."
                    Friend:: "Oh? That's a shame. I'll see you around then."
                    
                Endif
                 
            Endif
            timeout(50, call_invite, call_invite_co, call_invite_f, Ravenger_VB_family_invite_workout, Ravenger_VB_invite_workout, Ravenger_VB_call_invite_co_pregnant)
        Endif  // Scene edited by Raddeck
    sceneEnd()