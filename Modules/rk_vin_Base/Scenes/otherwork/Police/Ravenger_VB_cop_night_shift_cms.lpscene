WHAT: Cop_paper_work
WHERE: Police:work, all
WHEN: 18 - 4
WHO: Actor = getPerson(tag_Police); Actor.isInterestedIn(Player) && Player.isInterested(Actor) && Actor:perversion > 70 && Actor:attractiontoplayer > 20
OTHER:

    SceneStart()
        
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        Actor.dressUniform(police)
        Actor.show()
        "I'm on the night shift today, which has been very uneventful and boring. At least <Actor.name> is there to keep my company."
        Actor(Annoyed):: "This is too boring! You know what, since only us two are here ... let's sneak away and have some fun to pass the time ..."
        "Wow, that's a direct proposal ..."
        0:: "Fuck <Actor.name>"
        1:: "No"
        
        If 0
        
            "Sounds like a fun way to kill some time till the shift finishes ..."
            Sex(Player, Actor)
            passTime(1, 3)
            
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            
        Endif
        
        If HungerEnable > 0
            Player.modifyActorVar(rk_VB_Hunger, -1)
        EndIf
        
    SceneEnd() // Scene edited by Raddeck
    timeout(200, Ravenger_VB_cop_night_shift_cms)