WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
        
        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        Player(Flirty):: "Hey Babe! Would you like to give a massage to my to <Player.cock_or_pussy>?"
        If [Player.isFemale() && Actor:arousal > 15] || [Player.isFemale() && Actor:perversion > 50] || Actor:arousal > 15
            Actor(Excited):: "Uh, of course I want, what's that question..."
            Player.strip()
            Filter(Fingering)
            sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            Player.dress()
            Actor:attractiontoplayer += Random(0, 3)
            Actor:arousal += Random(0, 2)
            
        ElseIf [Player.isMale() && Actor:arousal > 15] || [Player.isMale() && Actor:perversion > 50] || Actor:arousal > 15
            Actor(Excited):: "Uh, of course I want, what's that question..."
            Player.strip()
            Filter(Handjob)
            sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -1)
            EndIf
            Player.dress()
            Actor:attractiontoplayer += Random(0, 3)
            Actor:arousal += Random(0, 2)
            
        Else
            Actor(Embarassed)::"Nah! Not now babe!"
            Actor:attractiontoplayer += Random(0, 1)
        EndIf
            
    SceneEnd()