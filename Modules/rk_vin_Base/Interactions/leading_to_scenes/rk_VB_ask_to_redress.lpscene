WHAT: none
WHERE: home
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    
        If Actor:perversion > 50 && Actor:arousal > 35
        
            Actor(Sad)::"Not now, I want to stay naked, I'm so horny...!"
            
        Else
        
            Actor.show()
            Actor(Sad)::"Ok, I will...!"
            Actor.setNoRedress(false)
            Actor.dress()
            Actor.show()
        
        EndIf
        
    SceneEnd()