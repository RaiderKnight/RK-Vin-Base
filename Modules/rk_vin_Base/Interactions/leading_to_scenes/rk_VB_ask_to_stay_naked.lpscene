WHAT: none
WHERE: home
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()
    
        If Actor:perversion > 50 && Actor:arousal > 25
        
            Actor.show()
            Actor.strip()
            Actor.setNoRedress(true)
            Actor:perversion += 1
            Actor:arousal += 1
            
        Elseif Actor:perversion > 25 && Actor:perversion < 49 && Actor:arousal > 15
        
            Actor.show()
            Actor(Flirty)::"Only the top, I'm not in the mood to walk around totally naked!"
            Actor.strip()
            Actor.dressExcept(Top, Top_Under)
            Actor.setNoRedress(true)
            Actor:perversion += 1
            Actor:arousal += 1
        
        Else
        
            Actor(Sad)::"Not now, I'm not in the mood to walk around naked!"
        
        EndIf
        
    SceneEnd()