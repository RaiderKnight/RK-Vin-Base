WHAT: none
WHERE: none
WHEN: 0 - 24
WHO: none
OTHER: none

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If Actor.isEmployee() || [Actor.isColleague() && jobexperience - Actor:jobexperience > 30]
            Subor = true
        Else
            Subor = false
        Endif
        Player.stripOne()
        "I quickly flashed <Actor.name> my private parts."
        perversion += 0.5
        //Edited by Ravenger6660
        Love = 0
        Love += Actor:attractiontoplayer
        Love += Actor:perversion
        //End edit
        
        If Random(100, 200) < Love || [Random(66, 133) < Actor:attractiontoplayer + perversion + Actor:masochist && Subor]
        
            Actor:attractiontoplayer += 0.5
            Actor:perversion += 0.5
            Actor(Flirty):: "I must say that's not a bad sight at all. Wanna show me what you can do with it?"
            0:: "It's my lucky day!"
            1:: "Actually, I didn't intend for this to get this far ..."
        
            If 0
            
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                mood += 3
                Actor:attractiontoplayer += 0.5
                Actor:perversion += 0.5
            
            Endif
            
        Elseif Actor.hadSex() || [Random(40, 100) < Love] || [Random(26, 66) < Actor:attractiontoplayer + perversion + Actor:masochist && Subor]
            If Subor
                "Of course, I can't tell if <Actor.he_or_she> actually enjoyed that or is just trying not to upset me since <Actor.he_or_she> is one of my subordinates."
            Else
                "<Actor.He_or_She> didn't say a word but smiled at me. <Actor.He_or_She> definitely enjoyed what <Actor.he_or_she> saw."
            Endif
            Actor:attractiontoplayer += 0.5
            Actor:perversion += 0.5
            
        Elseif [Actor:attractiontoplayer > 40 && Actor:attractiontoplayer] < 60 || [Actor:rapportwithplayer > 40 && Actor:rapportwithplayer < 60] || [[Actor:attractiontoplayer > 40 && Actor:attractiontoplayer < 60] && [Actor:rapportwithplayer > 40 && Actor:rapportwithplayer < 60] && Subor]
            If Subor
                Actor(Shocked):: "Please cover up! It's making me a bit uncomfortable. You're my boss after all ..."
            Else
                Actor(Shocked):: "What the fuck are you doing! Cover up idiot!"
            Endif
            Actor:attractiontoplayer -= 1
            Actor:perversion -= 0.5
        Else
            If Subor
                Actor(Angry):: "Please cover up! It's making me a bit uncomfortable. You're my boss after all! I can report this if I want ..."
            Else
                Actor(Angry):: "What the fuck! That's so inappropriate! Cover up you perv!"
            Endif
            Actor:attractiontoplayer -= 1
            Actor:rapportwithplayer -= 1
            
            If WHERE != home && WHERE != fraternity && WHERE != brothel && WHERE != stripclub && WHERE != prison
               
               followUp(public_indecency)
            
            Endif
            
        Endif
        
    SceneEnd() // Scene improved by Raddeck - Edited by Ravenger6660