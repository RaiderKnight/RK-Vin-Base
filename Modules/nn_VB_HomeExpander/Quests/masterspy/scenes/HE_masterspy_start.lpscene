WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER: none

    SceneStart()
    
    reward = 20000
    rewardLocal = reward.convertToLocalCurrency()
    
    "Install a spy cam in every of the 7 rooms of your home. If you complete the task you will be rewarded <rewardLocal>."
    
    // Count already installed cams
    cnt = 0
    tmp = RE_has_spy_cam_bathroom.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    tmp = RE_has_spy_cam_bedroom.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    tmp = RE_has_spy_cam_second_room.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    tmp = RE_has_spy_cam_third_room.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    tmp = RE_has_spy_cam_living_room.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    tmp = RE_has_spy_cam_kitchen.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    tmp = RE_has_spy_cam_office.getGlobal() 
    If tmp>0
        cnt+=1
    EndIf
    
    If cnt >= 7
        "Hey, congratulations! You already have all cams installed!"
        "Here is your reward! <rewardLocal> were just added to your account!"
        money+=reward
        questEnd()
    Else
        HE_masterspy_quest_active.setGlobal(1)
    EndIf
    
    SceneEnd()