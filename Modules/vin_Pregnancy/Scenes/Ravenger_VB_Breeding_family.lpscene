WHAT: all
WHERE: home 
WHEN: 0 - 24
WHO: Breeder = getRelative(); If Breeder.livesWithPlayer() && !Breeder.isMale() && Breeder.isOnPeriod() && !Breeder.isPregnant() && [Breeder:perversion > 60 || Breeder:incest > 80 || Breeder:attractiontoplayer > 80 || Breeder.WantsBabies(true)]
OTHER: !IsWithCompanion() && isModEnabled(vin_Incest) && !Player.isFemale()

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        setBackground(home)
        Breeder.show()
        
        If Breeder.WantsBabies(true)
            Breeder(Happy):: "Hi <Player.name> come here, I'm ovulating right now." // Edited by Raddeck
            Breeder(Grin):: "So, I want you to fuck me right now till I'm pregnant."
        Else
            Breeder(Annoyed):: "My sex drive is going crazy right now. Not sure why."
            Player(Curious):: "What do you want me to do about it?"
            Breeder(Grin):: "Can you to fuck me until it stops?"
        Endif
        
        0:: "Sure."
        1:: "Sorry not today."
    
        If 0
            Player(flirty):: "Let get started then."
            Breeder:likes_vaginal += 100
            Breeder:attractiontoplayer += 10
            Breeder.Strip()
            Player.Strip()
            Filter(vaginal)
            Sex(Player, Breeder)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
            Player(flirty):: "Wow, that was fun."
            If !Breeder.isPregnant()
                Breeder():: "We are not done yet..."
                If !wasCondomUsedDuringLastSex()
                    Breeder:fertility_npc += 5
                Endif   
                energy -= 15
                Filter(vaginal)
                SexNoAffair(Player, Breeder)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                Player(tired):: "Done?"
                If !Breeder.isPregnant()
                    Breeder():: "No keep on cumming"
                    If !wasCondomUsedDuringLastSex()
                        Breeder:fertility_npc += 5
                    Endif   
                    energy -= 15
                    Filter(vaginal)
                    SexNoAffair(Player, Breeder)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    Breeder:attractiontoplayer += Random(0, 30)
                    Player(tired):: "Good lord... I'm empty..."
                Endif
            Endif   
            
        Else
            "Sorry not today."
            If Breeder:masochist < -50 && isModEnabled(vin_NonConsensual) && Breeder.WantsBabies(true)
                "My <Breeder.relationship> wasn't one to take no for an answer ..."
                Filter(AggressiveFM)
                player.strip()
                Sex(Breeder, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                If !Breeder.isPregnant()
                    Breeder():: "We are not done yet..."
                    If !wasCondomUsedDuringLastSex()
                        Breeder:fertility_npc += 5
                    Endif   
                    energy -= 15
                    Filter(vaginal)
                    SexNoAffair(Player, Breeder)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    Player(tired):: "Done?"
                    If !Breeder.isPregnant()
                        Breeder():: "No keep on cuming"
                        If !wasCondomUsedDuringLastSex()
                            Breeder:fertility_npc += 5
                        Endif   
                        energy -= 15
                        Filter(vaginal)
                        SexNoAffair(Player, Breeder)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        Breeder:attractiontoplayer += Random(0, 30)
                        Player(tired):: "Good lord... I'm empty..."
                    Endif
                Endif
            Endif
        Endif
    
    SceneEnd() // Improved by Raddeck
    timeoutPrecise(100, Ravenger_VB_Breeding_family)