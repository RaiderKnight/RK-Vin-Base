WHAT: all
WHERE: home
WHEN: 8-24
WHO: Actor = getSpecific(ExDating); If Actor:attractiontoplayer > 40 && !Actor.isPregnant() && [Actor.isOnPeriod() || Player.isOnPeriod()] && Actor:fertility > 0 && [!Actor.isSameGender(Player) || Player.isTrans() || Actor.isTrans()]
OTHER: Player:fertility > 0 && !Player.isPregnant()

    // An ex who can't get over you suggests one last night together for old time's sake, secretly with the sole intention of starting a pregnancy.
    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.dress()
        Actor.show()
        "I've broken up with <Actor.name> but <Actor.he_or_she> can't seem to get over me. <Actor.He_or_She> even showed up at my place today asking for one last intimate time together for old time's sake."
        0:: "Grant <Actor.his_or_her> wish"
        1:: "Kick <Actor.him_or_her> out"
        
        If 0
            "<Actor.name> wanted this last time to be special so insisted on no protection ..."
            0:: "Okay ..."
            1:: "No way"
            
            If 0
                Actor.setWantsBabies(true)
                Player.setWantsBabies(true)
            Endif
            Actor:fertility => 50
            Actor:likes_vaginal => 100
            Sex(Actor, Player)
            
            // Start edit Raddeck
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, 2)
            EndIf
            // End edit Raddeck
            
            // Start edit Ravenger6660
        Elseif isModEnabled(vin_NonConsensual) && [Actor:perversion - Actor:masochist] > Random(100, 200) && karma < 50
            
            "Smack!"
            "I soon wake up to <Actor.name> raping me."
            
            If !Actor.isMale() && Player.isMale()
                Filter(AggressiveFM)
            Else
                Filter(Aggressive)
            Endif
            Actor:fertility => 50
            Actor:likes_vaginal => 100
            TalkNonConsensual()
            Sex(Actor, Player)
            // End edit Ravenger6660
            
            // Start edit Raddeck
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, 2)
            EndIf
            // End edit Raddeck
            
        Endif
    
    SceneEnd()
    timeout(500, ex_pregnancy_plot) // Improved by Ravenger6660 - Raddeck