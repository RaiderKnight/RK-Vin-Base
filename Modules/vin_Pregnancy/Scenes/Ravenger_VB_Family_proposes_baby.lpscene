WHAT: all
WHERE: all
WHEN: 0 - 24
WHO: Actor = getRelative(); If Actor.livesWithPlayer() && !Actor.isMale() && !Actor.isPregnant() && !Actor.isDating() && Random(40, 100) < Actor:rapportwithplayer && Actor:fertility > 0 && !Actor.hasRelationship(Spouses, Dating) && Actor.WantsBabies(true)
OTHER: isModEnabled(vin_Incest) && !Player.isFemale() && Player:fertility > 0

SceneStart()
    Actor6 = Actor.getRelatedPerson(Child)
    If !Actor6.isvalid()
    

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor.setWantsBabies()
        Actor:: "<Player.name>, can we talk?"
        "Must be important ..."
        Player:: "Yes, what is it?"
        Actor:: "You know I've always wanted to become a mother ..."
        Actor:: "But I've found stable relationships so hard to come by and I'd rather not go to a sperm bank and just get pregnant with a complete stranger who neither I nor my baby will ever meet ..."
        Actor:: "On the other hand, you're a <Player.relationship> who I trust deeply ... would you like the honour of being my child's father?"
        0:: "Yes"
        1:: "No"
        
        If 0
            Player:: "Of course, I'd love to help."
            Actor:likes_vaginal += 100
            setBackground(home)
            "Soon enough, we were begining our first 'natural insemination' session ..."
            Sex(Player, Actor)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
        Else
            Player:: "Sorry, but it would too weird ..."
        Endif
    
        timeout(500, Ravenger_VB_Family_proposes_baby)
    Endif   
    timeoutActor(1000, Ravenger_VB_Family_proposes_baby, Actor)
SceneEnd() // Improved by Raddeck