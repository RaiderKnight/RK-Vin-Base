WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getRelative(); If Actor.livesWithPlayer() && 85 < Actor:perversion && Player.isInterestedIn(Actor)
OTHER: isAtHome()


    sceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
    
        "I was hanging around the house not doing much."
        Actor.strip()
        Actor.setNoRedress()
        Actor.show(2)
        "When I see my <Actor.relationship> walking around the house completely naked!"
        arousal += Random(0, 20)
        Actor:arousal += Random(5, 20)
        Love = 0
        Love += Actor:attractiontoplayer
        Love += Actor:incest
        
        If Love > Random(100, 150) 
            Actor(Flirty):: "Hi <Actor.callplayer>. Like what you see?"
            Player(Curious):: "Why are you naked!"
            Actor(Flirty):: "It just feel so natural."
        
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)] || [!Player.isVisible(Top) && !Player.isVisible(Top_Under) && !Player.isMale()] 
                Actor(Flirty):: "I mean your naked too. Can't let you have all the fun."
                Player(Flirty):: "You're right it is fun."
                Actor(Flirty):: "..."
                Actor:arousal += 20
                Player(Flirty):: "..."
                arousal += 20
        
            Else
                Actor(Flirty):: "You should try it?"        
                0:: "Sure I'll give it a try."
                1:: "No, thank you."
        
                If 0 
                    Player():: "Sure I'll give it a try."
                    Player.strip()
                    Player():: "You're right it is fun."
                    Actor(Flirty):: "..."
                    Actor:arousal += 20
                    Player(Flirty):: "..."
                    arousal += 20
        
                Else
                    Player():: "No, thank you."             
        
                Endif
            Endif
                
            If Player:arousal > 75 && Player.isnaked() && Player.isInterestedIn(Actor)
                Player(Kiss)
                Actor(Attentive)
                "You take the opportunity while you have <Actor.p> attention to press your lips to <Actor.p>"
        
                If Actor:arousal > 75 
                    Actor(Kiss)
                    "<Actor.S> responds positively."
                    Actor:perversion += Random(5, 20)
                    Actor:incest += Random(0, 50)
                    Actor:attractiontoplayer += Random(0, 50)
                    Sex(Player, Actor)
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                    perversion += Random(0, 0.5)
                    mood += 30
                Else
                    "We just enjoy each other's company for a while."
                Endif
                
            Elseif Actor:arousal > 75 && Actor.isInterestedIn(Player) && Player.isnaked() && Player.isInterestedIn(Actor)
                "In an another surprise twist. My <Actor.relationship> Starts making out with me."
                "My resistance is futile."
                Actor:perversion += Random(5, 20)
                Actor:incest += Random(0, 50)
                Actor:attractiontoplayer += Random(0, 50)
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                perversion += Random(0, 0.5)
                mood += 30
                
            Else
                "We just enjoy each other's company for a while." 
        
            Endif
            
        Else
            Actor(Angry):: "What the hell <Player.Name>! Get out I'm naked!"
            Player:: "Why are you naked!"
            Actor.hide()
            Actor(Angry):: "None of your business!"
            Actor:attractiontoplayer -= Random(0, 5)
        
        Endif
        
        passTime(0.5, 1.5) 
    sceneEnd()
    timeoutActor(200, Ravenger_VB_Nude_sibling, Actor)
    timeoutPrecise(100, Ravenger_VB_Nude_sibling) 