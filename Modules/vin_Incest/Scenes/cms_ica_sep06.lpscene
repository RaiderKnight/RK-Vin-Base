WHAT: sleep, nap
WHERE: home
WHEN: 22 - 4
WHO: Actor = getRelative(); If Actor.livesWithPlayer() && Actor:age <= age
OTHER: isAtHome()

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If perversion > 65 || arousal > 25
            Player.strip()
            arousal += 5
        Endif
        ChanceToWakeUp = Random(1, 100)
        ChanceToWakeUp = ChanceToWakeUp.round()
        Player.animate(na_sleep)
        Actor.dressExcept(Outerwear, Foot, Headwear)
        Actor.show(2)
        "It's the middle of the night when my <Actor.relationship> comes into my room."
        "<Actor.He_or_She> is disturbed and scared of this loud thunderstorm and comes into my room asking if <Actor.he_or_she> can sleep alongside me."
        // Start edit Raddeck
        If perversion > 65 || arousal > 15
            Player(Happy)::"No problem, but I'm currently naked, so I hope you don't mind too much?"
            Actor(Scared)::"No, not at all!"
        Endif
        // End edit Raddeck
        0:: "Yes"
        1:: "No"
            
        If 0
            
            Player(Happy)::"Of course, come and sleep by my side!"
            Actor.animate(na_sleep)
            If [Actor:perversion > 50 || Actor:arousal > 25] && !Actor.isNaked() && [Actor.isInterestedIn(Player) || Actor.hadsex()]
                Actor.strip()
                arousal += 5
                "My <Actor.relationship> undresses to sleep in nude." // Edit Raddeck
            Endif   
            "After a while ..." // Edit Raddeck
            "<Actor.name> is sleeping like a baby..." // Edit Raddeck
            If Player.isNaked() && Actor.isNaked() && [Actor.isInterestedIn(Player) || Actor.hadsex()]
                FuckActorYes = false
                
                "I thank god, looking under the sheets, I saw that we were both naked..."  // Edit Raddeck
                "Makes... making love real easy."
                // Start edit Raddeck
                "What should I do?"
                0::Player.isMale() && !Actor.isMale()::"Slide my dick inside <Actor.his_or_her> pussy!"
                1::Player.isMale() && Actor.isMale() || !Player.isMale()::"Slide my dick inside <Actor.his_or_her> ass!"
                2::"Masturbate <Actor.him_or_her>"
                99::"Nothing"
                
                If 0
                
                    "I decided to insert my cock slowly and gently into her pussy and started fucking her very slowly, to not wake her up."
                    If ChanceToWakeUp > 60
                        If Actor:arousal < 25
                            Actor(Annoyed)::"Not tonight! Let me sleep!"
                            Actor:incest -= 2
                            SceneEnd()
                        Endif
                    Endif
                    Filter(Vaginal)
                    Sex(Player, Actor)
                    FuckActorYes = true
                
                Elseif 1
                
                    "I decided to insert my oily cock slowly and gently into <Actor.his_or_her> ass and started fucking <Actor.him_or_her> very slowly, to not wake <Actor.him_or_her> up."
                    If ChanceToWakeUp > 50
                        If Actor:arousal < 25
                            Actor(Annoyed)::"Not tonight! Let me sleep!"
                            Actor:incest -= 2
                            SceneEnd()
                        Endif
                    Endif
                    Filter(Anal)
                    Sex(Player, Actor)
                    FuckActorYes = true
                    
                Elseif 2
                    If !Actor.isMale()
                        "I decided to insert my fingers slowly and gently into her pussy and started fucking <Actor.him_or_her> very slowly, to not wake <Actor.him_or_her> up."
                        If ChanceToWakeUp > 70
                            If Actor:arousal < 25
                                Actor(Annoyed)::"Not tonight! Let me sleep!"
                                Actor:incest -= 2
                                SceneEnd()
                            Endif
                        Endif
                        Filter(Fingering)
                        Sex(Player, Actor)
                        FuckActorYes = true
                    Else
                        "I grabbed his dick and decided to stroke him very slowly until he cum, to not wake <Actor.him_or_her> up."
                        If ChanceToWakeUp > 70
                            If Actor:arousal < 25
                                Actor(Annoyed)::"Not tonight! Let me sleep!"
                                Actor:incest -= 2
                                SceneEnd()
                            Endif
                        Endif
                        Filter(Handjob)
                        Sex(Player, Actor)
                        FuckActorYes = true
                    Endif
                Endif
                
                If FuckActorYes
                    Actor:incest += 5
                    Actor:attractiontoplayer += 5
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -2)
                    EndIf
                EndIf
                // End edit Raddeck
            Else

                Love = 0
                Love += Actor:attractiontoplayer
                Love += Actor:incest
                "After a while ..."
                "<Actor.name> is sleeping like a baby..."
                "Now that <Actor.he_or_she>'s lying in bed with me, how should I behave."
                0:: "Touch <Actor.him_or_her>"
                1:: "Masturbate"
                99:: "Nothing"
                
                If 0
                    
                    // Start edit Raddeck
                    "I know <Actor.he_or_she>'s not sleeping, <Actor.he_or_she> seem to enjoyed the way I caress <Actor.him_or_her>."
                    "What should I do?"
                    0::Player.isMale() && !Actor.isMale()::"Slide my dick inside <Actor.his_or_her> pussy!"
                    1::Player.isMale() && Actor.isMale() || !Player.isMale()::"Slide my dick inside <Actor.his_or_her> ass!"
                    2::"Masturbate <Actor.him_or_her>"
                    99::"Only a small touch"
                
                    If 0
                
                        "I decided to insert my cock slowly and gently into her pussy and started fucking her very slowly."
                        If ChanceToWakeUp > 60
                            If Actor:arousal < 25 && [!Actor.isInterestedIn(Player) || !Actor.hadsex()]
                                Actor(Annoyed)::"Not tonight! Let me sleep!"
                                Actor:incest -= 2
                                SceneEnd()
                            Else
                                Actor(Angry)::"WTF are you doing? I'm not interested! I trusted you!"
                                Actor:incest -= Random(2, 15)
                                Actor:rapportwithplayer -= Random(2, 5)
                                Actor:attractiontoplayer -= Random(0, 5)
                                "Just like that, <Actor.name> return back to <Actor.his_or_her> bedroom!"
                                SceneEnd()
                            Endif
                        Endif
                        Filter(Vaginal)
                        Sex(Player, Actor)
                        FuckActorYes = true
                
                    Elseif 1
                
                        "I decided to insert my oily cock slowly and gently into <Actor.his_or_her> ass and started fucking <Actor.him_or_her> very slowly."
                        If ChanceToWakeUp > 50
                            If Actor:arousal < 25 && [!Actor.isInterestedIn(Player) || !Actor.hadsex()]
                                Actor(Annoyed)::"Not tonight! Let me sleep!"
                                Actor:incest -= 2
                                SceneEnd()
                            Else
                                Actor(Angry)::"WTF are you doing? I'm not interested! I trusted you!"
                                Actor:incest -= Random(2, 15)
                                Actor:rapportwithplayer -= Random(2, 5)
                                Actor:attractiontoplayer -= Random(0, 5)
                                "Just like that, <Actor.name> return back to <Actor.his_or_her> bedroom!"
                                SceneEnd()
                            Endif
                        Endif
                        Filter(Anal)
                        Sex(Player, Actor)
                        FuckActorYes = true
                    
                    Elseif 2
                        If !Actor.isMale()
                            "I decided to insert my fingers slowly and gently into her pussy and started fucking <Actor.him_or_her> very slowly."
                            If ChanceToWakeUp > 70
                                If Actor:arousal < 25 && [!Actor.isInterestedIn(Player) || !Actor.hadsex()]
                                    Actor(Annoyed)::"Not tonight! Let me sleep!"
                                    Actor:incest -= 2
                                    SceneEnd()
                                Else
                                    Actor(Angry)::"WTF are you doing? I'm not interested! I trusted you!"
                                    Actor:incest -= Random(2, 15)
                                    Actor:rapportwithplayer -= Random(2, 5)
                                    Actor:attractiontoplayer -= Random(0, 5)
                                    "Just like that, <Actor.name> return back to <Actor.his_or_her> bedroom!"
                                    SceneEnd()
                                Endif
                            Endif
                            Filter(Fingering)
                            Sex(Player, Actor)
                            FuckActorYes = true
                        Else
                            "I grabbed his dick and decided to stroke him very slowly until he cum."
                            If ChanceToWakeUp > 70
                                If Actor:arousal < 25 && [!Actor.isInterestedIn(Player) || !Actor.hadsex()]
                                    Actor(Annoyed)::"Not tonight! Let me sleep!"
                                    Actor:incest -= 2
                                    SceneEnd()
                                Else
                                    Actor(Angry)::"WTF are you doing? I'm not interested! I trusted you!"
                                    Actor:incest -= Random(2, 15)
                                    Actor:rapportwithplayer -= Random(2, 5)
                                    Actor:attractiontoplayer -= Random(0, 5)
                                    "Just like that, <Actor.name> return back to <Actor.his_or_her> bedroom!"
                                    SceneEnd()
                                Endif
                            Endif
                            Filter(Handjob)
                            Sex(Player, Actor)
                            FuckActorYes = true
                        Endif

                    Endif
                    
                    If FuckActorYes
                        Actor:incest += 5
                        Actor:attractiontoplayer += 5
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                    EndIf

                    arousal += 1
                    perversion += 1
                    // End edit Raddeck
                    Actor:incest += 5

                    If Love > Random(100, 150) && [Actor.isInterestedIn(Player) || Actor.hadsex()]
                        Actor(Flirty):: "Let's extend the fun..."
                        Player(Flirty):: "Sure..."
                        Sex(Player, Actor)
                        Actor:incest += 5
                        Actor:attractiontoplayer += 5
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                    Endif
                Elseif 1
                
                    "After a while ..." // Edit Raddeck
                    "<Actor.name> is sleeping like a baby..." // Edit Raddeck
                    "I lower the sheets quietly, until I see <Actor.his_or_her> entire body, I undress and start to masturbate looking at <Actor.him_or_her>..." // Edit Raddeck
                    Player.strip()) // Edit Raddeck
                    Filter(Solo) // Edit Raddeck
                    Sex(Player)
                    perversion += 2
                    arousal += 1 // Edit Raddeck
                    
                    If ChanceToWakeUp > 50 // Edit Raddeck
                    
                        "Shit, I think <Actor.he_or_she> hear me."
                        Actor:perversion += 1
                        Actor:arousal += 2
                        Love += Actor:perversion
                        Love += Actor:arousal
                        If [Love > Random(200, 350) && Actor.isInterestedIn(Player) || Actor.hadsex()]
                            Actor(Flirty):: "Hey, it's up to me to do this, since you woke me up, let's make love ..." // Edit Raddeck
                            Actor.strip() // Edit Raddeck
                            Sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:incest += 5
                            Actor:attractiontoplayer += 5
                        Else
                            Actor(Angry):: "What are you doing! That's fucking gross!" // Edit Raddeck
                            Actor:incest -= Random(2, 15)
                            Actor:attractiontoplayer -= Random(2, 5)
                            "Just like that, <Actor.name> return back to <Actor.his_or_her> bedroom!" // Edit Raddeck
                            SceneEnd() // Edit Raddeck
                        Endif
                    Elseif perversion > 75
                        "I can hold myself I'm so horny, my libido is so high that I start to grope my <Actor.relationship>" // Edit Raddeck
                        Actor:incest += 5
                        Actor:perversion += 5
                        Actor:arousal += 4
                        Love += Actor:perversion
                        Love += Actor:arousal
                        If [Love > Random(200, 350) && Actor.isInterestedIn(Player) || Actor.hadsex()]
                            Actor(Flirty):: "Hey, it's up to me to do this, since you woke me up, let's make love ..." // Edit Raddeck
                            Actor.strip() // Edit Raddeck
                            Sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:incest += 5
                            Actor:attractiontoplayer += 5
                        Else
                            Actor(Angry):: "What are you doing! That's fucking gross!" // Edit Raddeck 
                            Actor:incest -= Random(5, 15)
                            Actor:attractiontoplayer -= Random(2, 5)
                            "Just like that, <Actor.name> return back to <Actor.his_or_her> bedroom!" // Edit Raddeck
                            SceneEnd() // Edit Raddeck
                        Endif
                    Else
                        Actor:incest += 2
                        "<Actor.name> didn't even wake up, <Actor.he_or_she> didn't hear anything!" // Edit Raddeck
                    Endif
                Else
                    Love += Actor:perversion
                    Love += Actor:arousal
                    Love -= masochist           
                    If Love > Random(200, 450) && Actor.isInterestedIn(Player)
                        "<Actor.name> is touching me!"
                        0:: "Touch <Actor.him_or_her> back"
                        1:: "Reject <Actor.his_or_her> advance"
                        
                        If 0
                            Actor:incest += 5
                            Player(Flirty):: "Hey, since you woke me up, let's make love ..." // Edit Raddeck
                            Sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:incest += 5
                            Actor:attractiontoplayer += 5              
                        Else
                            If isModEnabled(vin_NonConsensual) && Actor:masochist < -50 
                                "<Actor.name> tell me to shut up and let <Actor.him_or_her> do <Actor.his_or_her> job..." // Edit Raddeck
                                Filter(Aggressive)
                                Sex(Actor, Player)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -2)
                                EndIf
                            Elseif !Player.isInterestedIn(Actor)
                                Player(Angry):: "What are you doing! That's fucking gross!" // Edit Raddeck 
                                incest -= Random(5, 15)
                                perversion -= Random(1, 5)
                                SceneEnd() // Edit Raddeck
                            Else
                            
                                Player(Annoyed)::"Not tonight! Let me sleep!" // Edit Raddeck
                                
                            Endif
                        Endif
                    Endif
                Endif
            Endif
            // Start edit Raddeck
        Else
            Player(Annoyed)::"No, just hide yourself under your sheets and sleep!"
            Actor:incest -= Random(5, 25)
            Actor:attractiontoplayer -= Random(0, 3)
        Endif // End edit Raddeck
    SceneEnd()
    timeoutActor(600, cms_ica_sep06, Actor)
    timeout(300, cms_ica_sep06) // Improved by Raddeck - Ravenger6660