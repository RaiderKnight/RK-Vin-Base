WHAT: all, -sleep, -nap
WHERE: home
WHEN: 9 - 12
WHO: Actor = getRelative(); If !Actor.isFemale() && Actor.livesWithPlayer() && Player.isInterestedIn(Actor) && Actor:arousal > Random(0, 70)
OTHER: isAtHome() && !Player.isMale()

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "It's already late in the morning yet my <Actor.relationship> still hasn't gotten out of <Actor.his_or_her> room yet. <Actor.He_or_She> is already very late for work."
        0:: "Go wake <Actor.him_or_her> up"
        1:: "Let <Actor.him_or_her> sleep"
        
        If 0
            Love = 0
            Love += Actor:attractiontoplayer
            Love += Actor:incest
            Love += Actor:perversion
            Actor.show()
            Actor.animate(na_sleep)
            Player(Anxious):: "Wake up, <Actor.Relationship>! Aren't you late for work?"
            sneak += 5
            Sneaking = false
            If sneak > 50 && perversion > 75 && isModEnabled(vin_NonConsensual)
                "Should I sneak in?"
                1:: "Yes"
                2:: "no"
                
                If 1
                    Sneaking = true
                Endif
            Endif
            If Sneaking
                "I fall quiet..."
                "I couldn't help but notice that my <Actor.relationship> had a massive Hard-On and it has fallen out of their pants."
                Player(Whisper):: "No wonder <Actor.he_or_she> slept so soundly, <Actor.he_or_she> was probably having some wet dreams."
                "I sneak closer."
                SneakGame()
                If Random(0, 100) < sneak
                    "Wow, my <Actor.relationship> is sleeping naked ... <Actor.He_or_She> is my <Actor.relationship> so I really shouldn't feel like this but ... I'm getting very aroused ... In fact, as much as the thought is so wrong on so many different levels, I can't help but wish I had the courage to join <Actor.him_or_her> in <Actor.his_or_her> bed ... or maybe I can touch <Actor.him_or_her> just a little bit."
                    0:: "Feel <Actor.him_or_her> up"
                    1:: "No way"
                
                    If 0
                        "A few touches wouldn't hurt."
                        Actor:incest += Random(0, 3)
                        "Damn it, now I want more ..."
                        0:: "Fuck my sleeping <Actor.relationship>"
                        1:: "Resist the urge"
                    
                        If 0
                            TalkNonConsensual()
                            Filter(Sleeping)
                            Actor.closeEyes(true)
                            Sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:incest += Random(0, 20)
                        Else
                            If Actor:intelligence + Random(-50, 50) > sneak
                                "How ever later my <Actor.relationship> came to confront me about what I did." 
                                Actor(Angry):: "I know you were touching me."
                                Love -= Actor:masochist
                                If Random(200, 350) < Love && Actor.isInterestedIn(Player) && isModEnabled(vin_NonConsensual)
                                    Actor(Flirty):: "You know what. I think I'm going to have some fun with you."
                                    Sex(Actor, Player)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -2)
                                    EndIf
                                    Actor:incest += Random(0, 50)
                                Else
                                    Actor(Angry):: "I better not catch you again!"
                                    Actor:rapportwithplayer -= Random(0, 9)
                                Endif
                        
                            Endif
                    
                        Endif
                    Else
                        "I should not. If <Actor.he_or_she> wakes up, everything would be so messed up."
                    Endif
                Else
                    Actor.animate()
                    Actor(Angry):: "What the hell, <Actor.callplayer>! Is this some sort of prank? Get out of my room right now!"
                    Actor:rapportwithplayer -= Random(0, 2)
                Endif
            
            Else
                Player(Anxious):: "Wake up, <Player.Relationship>! Aren't you late for work?"
                Actor(Scared):: "Oh shit! I completely overslept. What time is it now?"
                Actor:incest += Random(0, 1)
                Actor:attractiontoplayer += Random(0, 1)
                "I couldn't help but notice that my <Actor.relationship> had a massive morning wood and it has fallen out of his pants. No wonder he slept so soundly, he was probably having some wet dreams."
                0:: "Say nothing"
                1:: "Your thing is hard ..."
                
                If 1
                    Actor:incest += Random(0, 1)
                    If Random(150, 250) < Love
                        Actor(Flirty):: "Like what you see?"
                        0:: "Yeah ..."
                        1:: "That's inappropriate!"
                        
                        If 0
                            Player(Flirty):: "Maybe just a little bit ..."
                            Actor:incest += Random(0, 2)
                            Actor:attractiontoplayer += Random(0, 1)
                                Actor(Flirty):: "Then come on, why don't feel how hard it is for you?"
                                0:: "Okay ..."
                                1:: "That's going too far!"
                                
                                If 0
                                    Player(Flirty):: "Okay ... I guess just a touch won't hurt."
                                    Sex(Player, Actor)
                                    If HungerEnable > 0
                                        Player.modifyActorVar(rk_VB_Hunger, -2)
                                    EndIf
                                Else
                                    Player(Angry):: "Ew, what the fuck was that, <Actor.relationship>? I'm your <Actor.callplayer>!"
                                    If isModEnabled(vin_NonConsensual) && Friend:masochist < -50
                                        Actor(Angry):: "I don't think you have a choice."
                                        Filter(Aggressive)
                                        Sex(Actor, Player)
                                        If HungerEnable > 0
                                            Player.modifyActorVar(rk_VB_Hunger, -2)
                                        EndIf
                                    Endif
                                Endif
                        Else
                            Player(Angry):: "Ew, what the fuck was that, <Actor.relationship>? I'm your <Actor.callplayer>!"
                        Endif
                    Else
                        Actor(Embarassed):: "Sorry. God, this is embarassing ... this sometimes happens in the morning ..."
                        Player(Embarassed):: "It's okay. It happens."                
                    Endif
                        
                Endif       
            Else
                "Maybe, I should get I got stuff to do today."
            Endif
        Else
            "Maybe <Actor.he_or_she> took the day off. <Actor.He_or_She> would have set the alarm otherwise. I'll just let <Actor.him_or_her> sleep."
        Endif    
    SceneEnd()
    timeout(100, Ravenger_VB_family_morning_wood2)