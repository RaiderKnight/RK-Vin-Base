WHAT: all, -sleep, -nap, -go_to_the_bathroom, -bathe
WHERE: home
WHEN: 0 - 24
WHO: Actor = getRelative(); If Actor.livesWithPlayer() && Player.isInterestedIn(Actor) && Random(50, 200) < Actor:attractivenes
OTHER: isAtHome() && Random(10, 300) < perversion

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        setBackground(hallway)
        Love = 0
        Love += Actor:attractiontoplayer
        Love += Actor:incest
        Love += Actor:perversion
        "I can hear water running in the bathroom: my <Actor.relationship> must be taking shower ..."
        0:: "Peep on <Actor.him_or_her>"
        1:: "Better not"
        
        If 0
            SneakGame()
            Stealth = 0
            Stealth += Actor:perversion
            Stealth += sneak
            If 150 < Stealth
                "Fortunately for me, <Actor.he_or_she> didn't lock the door, allowing me to sneak behind it to get a decent view of <Actor.him_or_her> in all <Actor.his_or_her> glory."
                Actor.strip()
                Actor.show(2)
                setBackground(shower)
                If [Actor:perversion + Actor:arousal] > Random(100, 200)
                    Filter(solo)
                    Actor:perversion += Random(0, 5)
                    arousal += Random(10, 30)
                    Actor:arousal += Random(10, 30)
                Else
                    Filter(Shower)
                Endif   
                arousal += Random(0, 20)
                Sex(Actor)
                Actor.show(2)
                Player.show(0)
                    
                If [Actor:perversion + Actor:arousal] > Random(100, 200)
                    "Wow <Actor.he_or_she> watching masturbate got me all horny. Maybe I can just join <Actor.him_or_her>?"
                    0:: "Join in"
                    1:: "Leave"
           
                    If 0
                        Player.strip()
                        Player(Flirty):: "Damn it baby, you got me all horny. Mind if I join you in there?"            
                        Love += Actor:arousal
                    
                        If Random(200, 350) < Love
                            Sex(Player, Actor)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:perversion += Random(0, 5)
                            Actor:attractiontoplayer += Random(0, 5)
    
                        Else
                            Actor(Angry):: "What are you doing here, peeping Tom? And what's up with that perverted proposal? Get out!"
                            Actor:attractiontoplayer -= Random(0, 10)
    
                        Endif
    
                    Else
                        "I better not push my luck."
                        If [Actor:intelligence + Random(0, 15)] > sneak
                            If !Actor.ismale()
                                Actor.dress(Towel_1_F)
                            Endif
                            "How ever later my <Actor.relationship> confronted me about what I saw." 
                            Actor(Angry):: "I know you were outside my room."
                            Love -= Actor:masochist
                    
                            If Random(200, 350) < Love && Actor.isInterestedIn(Player) && isModEnabled(vin_NonConsensual)
                                Actor(Flirty):: "You know what. I think I'm going to have some fun with you."
                                Sex(Actor, player)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -2)
                                EndIf
                                Actor:attractiontoplayer += Random(0, 50)
                            Else
                                Actor(Angry):: "I better not catch you again!"
                                Actor:rapportwithplayer -= Random(0, 9)
                            Endif
                    
                        Endif
    
                    Endif
    
                Elseif Love > Random(150, 250) || Actor.hadsex()
                    "Considering we're already having an incestuous relationship, I'm sure my <Actor.relationship> wouldn't mind too much if I joined <Actor.him_or_her>."
                    0:: "Join <Actor.him_or_her>"
                    1:: "Leave"
                    
                    If 0
                        Sex(Actor, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -2)
                        EndIf
                        Actor:incest += Random(0, 5)
                    Else
                        "Better leave"
                        If [Actor:intelligence + Random(0, 15)] > sneak
                            If !Actor.ismale()
                                Actor.dress(Towel_1_F)
                            Endif
                            "How ever later my <Actor.relationship> confronted me about what I saw." 
                            Actor(Angry):: "I know you were outside my room."
                            Love -= Actor:masochist
                    
                            If Random(200, 350) < Love && Actor.isInterestedIn(Player)
                                Actor(Flirty):: "You know what. I think I'm going to have some fun with you."
                                Sex(Actor, player)
                                If HungerEnable > 0
                                    Player.modifyActorVar(rk_VB_Hunger, -2)
                                EndIf
                                Actor:incest += Random(0, 50)
                            Else
                                Actor(Angry):: "I better not catch you again!"
                                Actor:rapportwithplayer -= Random(0, 9)
                            Endif
                        Endif
                    Endif
                Else
                    "As much as I'd love to join <Actor.him_or_her> in there, that's enough viewing for now. I should get out of there before I get caught."
                    arousal += Random(0, 20)
                    If [Actor:intelligence + Random(0, 15)] > sneak
                        If !Actor.ismale()
                            Actor.dress(Towel_1_F)
                        Endif
                        "How ever later my <Actor.relationship> confronted me about what I saw." 
                        Actor(Angry):: "I know you were outside the bathroom."
                        Love -= Actor:masochist
                    
                        If Random(200, 350) < Love && Actor.isInterestedIn(Player)
                            Actor(Flirty):: "You know what. I think I'm going to have some fun with you."
                            Sex(Actor, Player)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            Actor:incest += Random(0, 50)
                        Else
                            Actor(Angry):: "I better not catch you again!"
                            Actor:rapportwithplayer -= Random(0, 9)
                            sneak -= 2
                        Endif
                    
                    Endif
                
                Endif
            Else
                "Unfortunately for me, my <Actor.relationship> locked the bathroom door. There's no way for me to have a look."
            Endif
            sneak += Random(0, 0.5)
        Else
            "I don't want to get in trouble. I'll just leave <Actor.him_or_her> alone."
        Endif
    SceneEnd()
    timeoutActor(200, peep_family_shower, Actor) // Improved by Ravenger6660 - Raddeck
    timeout(48, peep_family_shower)