WHAT: watch_tv, watch_a_movie_in, watch_sport_in
WHERE: all
WHEN: 0 - 24
WHO: Actor = getRelative(); If Actor.livesWithPlayer() && !Actor.isFemale() && Player.isInterestedIn(Actor)
OTHER: isAtHome() && !Player.isMale()

    SceneStart()
    "I came home only to find my family gathering in the living room watching a movie."
    setBackgroundCustom(livingroom)
    Actor.dress()
    Actor.show(2)
    Actor(Happy):: "Come join us!"
    "As if there is any space left on the sofa for me to sit ..."
    0:: "Stand"
    1:: "Sit on <Actor.Relationship>'s lap"
    
    If 0
        "I'll just stand behind the sofa to watch the movie."
    Else
        "I to sit on <Actor.relationship> lap. No big deal."
        "A while later ..."
        "I feel something moving underneath me ... Wait, is that a boner? Is my <Actor.relationship> getting hard because of me? Sitting on <Actor.his_or_her> lap wasn't the best idea after all ..."
        Actor:incest += Random(0, 2)
        Actor:attractiontoplayer += Random(0, 2)
        Actor:arousal += 25
        Love = 0
        Love += Actor:attractiontoplayer
        Love += Actor:incest
        If Love > 75
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)]
                "I regret not where anything below the waist."
                "Because the next thing I know, they are slipping their member inside."
                Sex(Actor, Player)
                Actor:incest += 50
            
            Else
                "Suddenly, my <Actor.relationship> started whispering into my ear."
                Actor(Flirty):: "You're doing this deliberately, aren't you?"
                0:: "Apologize"
                1:: "Having fun?"
            
                If 0
                    Player(Embarrassed):: "I'm so sorry. I didn't mean to ..."
                    Actor:incest -= 1
                    Rape = 0
                    Rape -= Actor:masochist
                    Rape += Actor:arousal
                    Rape += Actor:perversion
                    If Random(150, 250) < Rape && isModEnabled(vin_NonConsensual)
                        "My <Actor.relationship> has other plans. <Actor.He_or_She> pulls my pants down and makes me sit on their dick."
                        Sex(Actor, Player)
                        Actor:incest += 25
                        Actor:attractiontoplayer += 25
                    Endif
                                
                Else
                    "I whispered back into <Actor.his_or_her> ear."
                    Player(Flirty):: "Let's sneak away from here, shall we?"
                    setBackground(home)
                    Sex(Actor, Player)
                    Actor:incest += 50
                Endif   
            Endif
        
        Elseif Actor:arousal > 85 && Love > 50
            "We're alone just the two of us."
            "My <Actor.relationship> starting to grope me."
            player:arousal += 25
            
            If [!Player.isVisible(Bottom) && !Player.isVisible(Bottom_Under)]
                "I regret not where anything below the waist."
                "Because the next thing I know, they are slipping their menmber inside."
                Sex(Actor, Player)
                Actor:incest += 50
                                
            Else
                Actor(Flirty):: "You're doing this deliberately, aren't you?"
                0:: "Apologize"
                1:: "Lead <Actor.him_or_her> to the bedroom"
            
                If 0
                    Player(Embarrassed):: "I'm so sorry. I didn't mean to ..."
                    Actor:incest -= 1
                    Rape = 0
                    Rape -= Actor:masochist
                    Rape += Actor:arousal
                    Rape += Actor:perversion
                    If Random(150, 250) < Rape && isModEnabled(vin_NonConsensual)
                        "My <Actor.relationship> has other plans. <Actor.He_or_She> pulls my pants down and makes me sit on their dick."
                        Sex(Actor, Player)
                        Actor:incest += 25
                        Actor:attractiontoplayer += 25
                    Endif

                Else
                    "I whispered back into <Actor.his_or_her> ear."
                    Player(Flirty):: "Let's sneak away from here, shall we?"
                    setBackground(home)
                    Sex(Player, Actor)
                    Actor:incest += 50
                Endif
            Endif           
        Else 
            "Beyond that nothing happend."      
        
        Endif
    Endif
    SceneEnd()
    timeoutActor(200, Ravenger_VB_sit_on_lap2, Actor)
    timeout(100, Ravenger_VB_sit_on_lap2)