WHAT: watch_porn
WHERE: all, -work
WHEN: 0 - 24
WHO: Family = getRelative(); If Family:pornfame > 5
OTHER:

SceneStart()
"I'm looking for porn when I find a porn video of my <Family.relationship> <Family.name>"
0:: "Watch"
1:: "Don't watch"

If 0    
    "Let see who my <Family.relationship> fuck today."
    Random
        setBackground(home)
        setBackground(bar)
        setBackground(hairdresser)
        setBackground(cafe)
        setBackground(clothes)
        setBackground(dentist)
        setBackground(doctor)
        setBackground(hotel)
        setBackground(laundry)
        setBackground(marketplace)
        setBackground(musical_instrument)
        setBackground(nightclub)
        setBackground(work)
        setBackground(home)
        setBackground(police)
        setBackground(prison)
        setBackground(pub)
        setBackground(restaurant)
        setBackground(shoes)
        setBackground(sports_centre)
        setBackground(supermarket)
        setBackground(theatre)
        setBackground(toilets)
    endRandom
    Number = Random(0, 3)
    
    Actor = generatePersonTemporary()
    While !Family.isInterestedIn(Actor)
        Actor = generatePersonTemporary()
    Endwhile
    
    If Number < 2.5
        Sex(Family, Actor)
        Family:pornfame += 1
        perversion += 1
    Else
        Actor2 = generatePersonTemporary()
        While !Family.isInterestedIn(Actor2)
            Actor2 = generatePersonTemporary()
        Endwhile 
        Sex(Family, Actor, Actor2)
        Family:pornfame += 2
        perversion += 2
    Endif   
Else
    timeout(100, Ravenger_VB_Family_porn_video)
Endif    
SceneEnd()
timeout(48, Ravenger_VB_Family_porn_video)