WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getSpecific(Dating); If !Actor.hasRelationship(Poly) && Random(50, 100) < Actor:perversion
OTHER: OpenRelationship() && isModEnabled(vin_NTR)

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        setBackgroundCustom(livingroom)
        Actor2 = generatePersonTemporary()
        While !Actor.isInterestedIn(Actor2)
            Actor2 = generatePersonTemporary()
        Endwhile
        Actor2.makeInterested(Actor)
        Actor2.dress()
        Actor.dress()
        Actor.show()
        "I was spending time with one of my lovers <Actor.name> when <Actor.he_or_she> hands me <Actor.his_or_her> phone to show me someone else's photo."
        secondScreen(Actor2)
        Actor2.show()
        Actor(Curious):: "What do you think about this <Actor2.guy_or_girl>?"
        Player(Curious):: "Why are you asking me? Who is <Actor2.he_or_she>?"
        Actor(Serene):: "You know ... you and me, we're in an open relationship after all ..."
        Player(Curious):: "Yes ... and?"
        Actor(Grin):: "I've been sleeping with this <Actor2.guy_or_girl>, <Actor2.name> ... <Actor2.He_or_She> knows about our polyamory, so <Actor2.he_or_she> asks me to be <Actor2.his_or_her> <Actor.boyfriend_or_girlfriend> as well ..."
        Actor(Curious):: "I wanted to say yes right away ... but I thought I would ask you to be sure ... you're okay with me having my own relationship with someone else, right? It's not breaking any of our rules ..."
        0:: "I'm cool with that"
        1:: "No way ..."
        
        If 0
            Actor:attractiontoplayer += 10
            Actor2:perversion => Random(50, 100)
            Actor2.makePermanent()
            If Actor2.isInterestedIn(Player) && Actor2:attractiontoplayer < 0
                Actor2:attractiontoplayer => Random(0, 50)
            Endif
            addNpcRelationship(Dating, Actor, Actor2)
            addNpcRelationship(Poly, Actor, Actor2)
            Actor(Happy):: "You're most generous lover ever! Don't worry, you'll always be my first love!"
            Actor(Flirty):: "Here, let me show you how much I love you ..."
            0:: "Have sex with <Actor.name>"
            1:: "Any videos of you and <Actor2.him_or_her>?"
            2:: "I'm not in the mood"
            
            If 0
                Sex(Player, Actor)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
            Elseif 1
                masochist += 2
                Actor(Grin):: "I didn't realize you prefer watching ..."
                Actor(Grin):: "Yes, we did record everything the last time we messed up around after a date ..."
                Actor(Flirty):: "Let me show you ..."
                Player.hide()
                setBackground(home)
                Sex(Actor2, Actor)
            Endif
        Else
            Actor(Sad):: "Alright ... I guess we had a different idea of what an open relationship means ..."
            Actor:attractiontoplayer -= 3
        Endif
    SceneEnd() // Scene improved by Raddeck
    timeout(500, poly_poly)