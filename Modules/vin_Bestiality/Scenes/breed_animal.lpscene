WHAT: none
WHERE: none
WHEN: 0-24
WHO: none
OTHER:

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        "Breeding animals is a big business and having been domesticated for so long, many animals are losing their reproductive instinct making breeding them harder and harder."
        "I heard there were some shady business people who would pay for someone to have bestiality sex with their animal."
        "The idea is to basically use such interspecies intercourse as 'practice' for the animal so that when it comes to actual mating with their fellow kind, they won't be so shy anymore."
        "Is this something I would want to make money from?"
        0:: "Explore offers"
        1:: "There are better ways to make money"
        
        If 0
            perversion += 1
            setBackground(home)
            Actor = generatePersonTemporary()
            Actor.dress()
            Actor.show()
            Actor2 = generateCreatureTemporary()
            Actor2.show()
            Price = Random(100, 1000)
            Price_loc = Price.convertToLocalCurrency()
            "<Actor.name> is offering me <Price_loc> to fuck <Actor.his_or_her> animal <Actor2.name> and get <Actor2.him_or_her> ready for actual breeding."
            0:: "Deal!"
            1:: "No"
            
            If 0
                money += Price
                Player.strip()
                Sex(Actor2, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -2)
                EndIf
                
                If !Player.isMale() && Random(0, 100) < 50
                    If Random(0, 100) < 50 && Actor2.isCreatureType(Dog)
                        Actor(Flirty):: "That looks like fun actually. Maybe I should join you and get some practice. Might save the money and do it myself next time."
                        Price = Random(200, 500)
                        Price_loc = Price.convertToLocalCurrency()
                        "<Actor.name> is offering me <Price_loc> to fuck with <Actor.him_or_her> and the pet?"
                        0:: "Deal!"
                        1:: "No"
                        
                        If 0
                            Player.strip()
                            Sex(Actor2, Actor, Player)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                            money += Price
                        EndIf
                    Else
                        Actor(Flirty):: "You fucked like a pro. I have a similar animal also in need of priming for breeding. Can you do both at the same time for some extra cash?"
                        Price = Random(500, 1000)
                        Price_loc = Price.convertToLocalCurrency()
                        Player(Flirty):: "Double the money? Sign me up!"
                        "<Actor.name> is offering me <Price_loc> to fuck with <Actor.him_or_her> and <Actor.his_or_her> pet?"
                        0:: "Yes"
                        1:: "No"
                        
                        If 0
                            If Actor2.isCreatureType(Dog)
                                Actor3 = generateCreatureTemporary(Dog)
                            Elseif Actor2.isCreatureType(Horse)
                                Actor3 = generateCreatureTemporary(Horse)
                            Endif
                            Actor3.show()
                            Actor.strip()
                            Player.strip()
                            Player(Flirty):: "Let's start!"
                            Sex(Actor2, Actor3, Player)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                            money += Price
                        Endif
                    Endif
                Endif        
            Endif
        Endif
    SceneEnd() // Scene improved by Raddeck