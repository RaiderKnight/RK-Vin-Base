WHAT: all 
WHERE: home
WHEN: 0-24
WHO: Actor = getCreature(Dog); Actor2 = getSpecific(Dating); If Actor.isMale() && Actor2.isInterestedInMen()
OTHER:

    // Your SO is doing housework and gets mounted, you can either help push the pet away or wait and see what happens. 
    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor2.dress()
        Actor2.show()
        Actor.show()
        Actor2.animate(sweep)
        "My <Actor2.boyfriend_or_girlfriend> was just doing some housework. <Actor2.He_or_She> needed to bend over to grab something that <Actor2.he_or_she> dropped by accident."
        animatePair(Actor, Actor2, Doggy)
        "Just as <Actor2.he_or_she> was trying to stand back up, <Actor.name> tried to mount <Actor2.name> from behind and started dry-humping."
        Actor2(Sad):: "<Player.name>, help me!"
        0:: "Help <Actor2.name> and push the dog away"
        1:: "Wait and see what happens"
        
        If 0
            "I pushed our horny dog away and rescue my <Actor2.boyfriend_or_girlfriend>."
            Actor2:rapportwithplayer += Random(0.1, 2) // Edit by Raddeck
            Actor2:attractiontoplayer += Random(0.1, 2) // Edit by Raddeck
        Else
            perversion += 1 // Edit by Raddeck
            "My dog is visibly becoming even more excited, I could see his dick getting harder and poking around <Actor2.name>'s hole."
            0:: "Let the dog fuck my <Actor2.boyfriend_or_girlfriend> <Actor2.his_or_her> over clothes"
            1:: "Put an end to this before it's too late"
            
            If 0
                //Start edit Raddeck
                arousal += 0.5
                If Actor2:perversion > 70 && Actor2:arousal > 15
                    Actor2(Angry)::"Oh, let me help you big boy..."
                    Actor2.strip()
                    sex(Actor, Actor2)
                    "What I should do?"
                    1::"Just watch"
                    2::"Join them"
                    3::"Intervening and yelling at my <Actor2.boyfriend_or_girlfriend>"
                    4::"Ignore"
                    
                    If 1
                        arousal += 1
                        perversion += 1
                        sex(Actor, Actor2)
                    Elseif 2
                        Player(Flirty)::"Don't mind if I join?"
                        Actor2(Flirty)::"Sure honey, come..."
                        arousal += 1
                        perversion += 1
                        sex(Actor, Actor2, Player)
                        If HungerEnable > 0
                            Player.modifyActorVar(rk_VB_Hunger, -3)
                        EndIf
                    Elseif 3
                        Player(Angry):: "That's disgusting! I can't believe you fuck with our dog."
                        Actor2:attractiontoplayer -= Random(0, 15)
                        Player:arousal -= Random(0, 15)
                        Actor2:rapportwithplayer -= Random(0, 15)
                        Player:perversion -= Random(0, 15)
                    Else
                        "I decided to just ignore what I just saw..."
                        arousal -= 1
                        perversion -= 1
                        masochist += 1
                    Endif
                Else
                    animatePair(Actor, Actor2, Doggy)
                    Actor2(Angry)::"Hey, stop! Get out of here now! Bad dog! You're not nice today, go play somewhere else and let me clean up...Damn"
                    Actor2:rapportwithplayer += Random(0.1, 2)
                    Actor2:attractiontoplayer += Random(0.1, 2)
                    "Hum! She managed to get rid of the dog..." 
                EndIf // End edit by Raddeck
                
            Else
                "I pushed our horny dog away and rescue my <Actor2.boyfriend_or_girlfriend>."
            Endif
        Endif
    SceneEnd()
    timeout(150, bestiality_housework_ntr) // scene improved by Raddeck