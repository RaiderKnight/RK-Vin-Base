WHAT: all
WHERE: home
WHEN: 0-24
WHO: Actor = getCreature(Dog); Actor2 = getRelative(); If Actor.isMale() && Actor.hadSex() && Actor2.livesWithPlayer() && Actor2.isInterestedIn(Actor)
OTHER:

    // While you're being knotted and stuck that way, one of the relatives living with you caught you in the act, their reaction and willingness to join in depends on their perversion, which increases gradually each time they see it.
    
    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        
        setBackgroundCustom(livingroom)
        Actor.show()
        "I'm simply watching TV in the living room with my dog <Actor.name> around. I can't help but notice <Actor.name> is getting too excited around me again."
        "We've already tasted the forbidden fruit and no-one else is in the apartment right now."
        "But is it really safe to fuck my dog in the living room? Someone could walk in at any moment ..."
        0:: "Strip naked and fuck <Actor.name>"
        1:: "Let's not risk it"
        
        If 0
            Player.strip()
            "I know <Actor2.name> is supposed to arrive soon, but I'm sure I'll have time to fuck <Actor.name>..." // Edit by Raddeck
            Sex(Actor, Player)
            animatePair(Actor, Player, Doggy)
            "<Actor.name>'s knot seems to stay swollen forever today ... It gives me the best orgasm in the world, but we're pretty much stuck in this position for the time being ..."
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
            //Start edit Raddeck
            Actor2isHere = false
            BadNews = Random(0, 100)
            If BadNews > 50
                Actor2isHere = true
            Endif
            If Actor2isHere
                Actor2.dress() // Edit Vin
                Actor2.show() // Edit Vin
                Actor2(Happy):: "<Player.name>, I'm home. How was your ... What the fuck!" // Edit Vin
                "Oh no, my <Actor2.relationship> <Actor2.name> got home early and caught me in the act while <Actor.name>'s knot remains swollen inside of me." // Edit Vin
                Actor2:perversion += Random(0, 5) // Edit Vin
                If Actor2:perversion > 50
                    Actor2:arousal += Random(0, 5)
                    Actor2:perversion += Random(0, 2)
                    Actor2(Flirty):: "That looks like a mind-blowing orgasm ... You wouldn't mind your <Actor2.relationship> having a go at your dog as well, would you?" // Edit Vin
                    Actor2.strip()
                    Player(Happy):: "Of course not, come join us!" // Edit Vin
                    Sex(Actor, Actor2, Player) // Edit Vin
                    If HungerEnable > 0
                        Player.modifyActorVar(rk_VB_Hunger, -3)
                    EndIf
                Elseif Actor2:perversion > 25 // Edit Vin
                    Actor2:: "Okay ... definitely not my cup of tea ... I'll just go to my room and pretend I saw nothing." // Edit Vin
                    Actor2:arousal -= Random(0, 5)
                Else    
                    Actor2(Angry):: "That's disgusting! I can't believe you fuck with our dog."
                    Actor2:attractiontoplayer -= Random(0, 5)
                    Actor2:arousal -= Random(0, 5)
                    Actor2:rapportwithplayer -= Random(0, 5)
                    Actor2:perversion -= Random(0, 5)
                Endif
            Endif  // End edit Raddeck
        Endif
    
    SceneEnd()
    timeout(200, bestiality_caught_by_family) // scene improved by Raddeck