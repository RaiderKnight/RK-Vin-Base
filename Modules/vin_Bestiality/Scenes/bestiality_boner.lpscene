WHAT: all
WHERE: home
WHEN: 0 - 24
WHO: Actor = getCreature(); If Random(30, 80) < Actor:perversion && Actor.isMale()
OTHER: isAtHome()

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        If Actor.isCreatureType(Horse)
            setBackgroundCustom(stable)
        Endif
        Actor2 = getSpecific(Dating)
        "<Actor.name> has sported an erection all day, which is getting quite worrying. <Actor.He_or_She> is clearly in some discomfort. They always say 'If your erection doesn't go away, go see a doctor'. This must apply to animals also."
        0:: "Take <Actor.him_or_her> to the vet"
        1:: perversion > 50:: "Take care of the erection the 'traditional' way"
        2:: Actor2.isValid() && Actor2:perversion > 50:: "Ask my <Actor2.boyfriend_or_girlfriend> to do it instead"
        3:: "Hire a specialist prostitute to do it"
        
        If 0
            setBackground(veterinary)
            Actor.show(2)
            "The vet took care of <Actor.name>'s uncomfortable issue. Maybe some medicine did the trick ... or maybe the vet just jerked him off. I don't wanna know."
        Elseif 1
            Sex(Actor, Player)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
            perversion += Random(0, 2)
            Actor:attractiontoplayer += Random(0, 2)
            
            If !Player.isMale()
                If Actor.isCreatureType(Dog)
                    Actor3 = getCreature()
                    While Actor3.isValid() && [!Actor3.isCreatureType(Dog) || Actor3.isSameAs(Actor)]
                        Actor3 = getCreature()
                    Endwhile
                Elseif Actor.isCreatureType(Horse)
                    Actor3 = getCreature()
                    While Actor3.isValid() && [!Actor3.isCreatureType(Horse) || Actor3.isSameAs(Actor)]
                        Actor3 = getCreature()
                    Endwhile        
                Endif
            Endif
            
            If Actor3.isValid()
                "Suddenly, <Actor3.name> saw what's happening and decided to join in the fun."
                Sex(Actor3, Actor, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
            Endif
        Elseif 2
            Sex(Actor, Actor2)
            Actor2:perversion += Random(0, 2)
            Actor2:attractiontoplayer += Random(0, 2)
    
            If !Actor2.isMale()
                If Actor.isCreatureType(Dog)
                    Actor3 = getCreature()
                    While Actor3.isValid() && [!Actor3.isCreatureType(Dog) || Actor3.isSameAs(Actor)]
                        Actor3 = getCreature()
                    Endwhile
                Elseif Actor.isCreatureType(Horse)
                    Actor3 = getCreature()
                    While Actor3.isValid() && [!Actor3.isCreatureType(Horse) || Actor3.isSameAs(Actor)]
                        Actor3 = getCreature()
                    Endwhile        
                Endif
            Endif
            
            If Actor3.isValid()
                "Suddenly, <Actor3.name> saw what's happening and decided to join in the fun."
                Sex(Actor3, Actor, Actor2)
            Endif
        Else
            SexCost = Random(150, 300) //Edit Raddeck
            SexCost_loc = SexCost.convertToLocalCurrency() //Edit Raddeck
            "I did some online searches and just as I suspected, there was indeed bestiality service being offered by some prostitutes. The cost is significantly higher than your usual prostitutes though."
            "It will cost me <SexCost_loc>" //Edit Raddeck
            "Should I accept?" //Edit Raddeck
            0::"Yes" //Edit Raddeck
            1::"No" //Edit Raddeck
            
            If 0
                Actor2 = generatePersonTemporary()
                While !Actor.isInterestedIn(Actor2)
                    Actor2 = generatePersonTemporary()
                Endwhile
                Actor2:attractiveness => Random(50, 100)
                Actor2:age => Random(18, 35)
                Actor2.dressUniform(sexwork)
                Actor2.show()
                money -= SexCost
                "The prostitute eventually showed up. Not even slightly nervous, <Actor2.he_or_she> attended to <Actor.name>'s needs right away. <Actor2.He_or_She> must have done this hundreds of times."
                "What an opportunity to watch the bestiality master at work."
                Actor2.strip()
                Sex(Actor, Actor2)
                Actor2:perversion += Random(0, 2)
                Actor2:attractiontoplayer += Random(0, 2)

                If !Actor2.isMale()
                    If Actor.isCreatureType(Dog)
                        Actor3 = getCreature()
                        While Actor3.isValid() && [!Actor3.isCreatureType(Dog) || Actor3.isSameAs(Actor)]
                            Actor3 = getCreature()
                        Endwhile
                    Elseif Actor.isCreatureType(Horse)
                        Actor3 = getCreature()
                        While Actor3.isValid() && [!Actor3.isCreatureType(Horse) || Actor3.isSameAs(Actor)]
                            Actor3 = getCreature()
                        Endwhile        
                    Endif
                Endif
                
                If Actor3.isValid()
                    SexCost1 = Random(100, 200) //Edit Raddeck
                    SexCost1_loc = SexCost1.convertToLocalCurrency() //Edit Raddeck
                    "Suddenly, <Actor3.name> saw what's happening and asked to join in..." //Edit Raddeck
                    Player(Grin)::"Looks like my horse wants to join in..." //Edit Raddeck
                    Actor2(Flirty)::"No problem, but it will cost <SexCost1_loc>" //Edit Raddeck
                    "Should I pay?" //Edit Raddeck
                    0::"Yes" //Edit Raddeck
                    1::"No" //Edit Raddeck
                    
                    If 0
                        "<Actor3.name> decided to join in the fun without hesitation." //Edit Raddeck
                        Actor2.strip()
                        Sex(Actor3, Actor, Actor2)
                        money -= SexCost1
                    Else
                        "My horse may be horny, but <Actor2.he_or_she>'s not so horny that I'd pay that amount of money to my horse fuck ..." //Edit Raddeck
                    Endif
                Endif
                // Start Edit Raddeck
                If Player:arousal > 25 && Player:perversion > 50
                    "Wow, looking at all this, now I'm the horny one, should I pay for a session?"
                    0::"Yes"
                    1::"No"
                    
                    If 0
                        Price1 = Random(50, 100)
                        Price2 = Random(100, 200)
                        Price3 = Random(200, 300)
                        Price1_loc = Price1.convertToLocalCurrency()
                        Price2_loc = Price2.convertToLocalCurrency()
                        Price3_loc = Price3.convertToLocalCurrency()
                        "I'm willing to pay for what kind of fuck?"
                        0::"Fuck with <Actor2.name> only - (<Price1_loc>)"
                        1::"Fuck with <Actor2.name> and <Actor.name> only - (<Price2_loc>)"
                        2::"Fuck with <Actor2.name>, <Actor.name> and <Actor3.name> - (<Price3_loc>)"
                        99::"Nevermind!"
                        
                        If 0
                            Player.strip()
                            Actor2.strip()
                            Sex(Player, Actor2)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -2)
                            EndIf
                            money -= Price1
                        Elseif 1
                            Player.strip()
                            Actor2.strip()
                            Sex(Player, Actor, Actor2)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -3)
                            EndIf
                            money -= Price2
                        Elseif 2
                            Player.strip()
                            Actor2.strip()
                            Sex(Player, Actor3, Actor, Actor2)
                            If HungerEnable > 0
                                Player.modifyActorVar(rk_VB_Hunger, -4)
                            EndIf
                            money -= Price3
                        Else
                            "That's enough for now! I spend enough money for today..."
                        Endif
                    Else
                        "That's enough for now!"
                    EndIf
                EndIf
                // End Edit Raddeck
            Else
                "My dog may be horny, but <Actor2.he_or_she>'s not so horny that I'd pay that amount of money to my dog fuck ..." //Edit Raddeck       
            EndIf    
        Endif
    
    SceneEnd()
    timeout(100, bestiality_boner) // scene improved by Raddeck