WHAT: all
WHERE: sports_centre
WHEN: 8 - 20
WHO: none
OTHER: !isWithCompanion() && Random(0, 100) < 1 && masochist > 50

    SceneStart()

        Hunger = Player.getActorVar(rk_VB_Hunger)
        HungerEnable = rk_VB_EnableHunger.getGlobal()
        UrgeToPeeEnable = rk_VB_UrgeToPeeEnable.getGlobal()
        UrgeToPee = Player.getActorVar(rk_VB_UrgeToPee)
        Actor = generatePersonTemporary()
        Actor.dress()
        Actor.show()
        "I was at the gym's locker room alone and this random <Actor.guy_or_girl> comes in having finished a workout, looks at me and declares:"
        Actor(Evil):: "You look nice as a post workout snack!" 
        Actor(Evil):: "I'm going to fuck you!"
        Actor(Evil):: "Your only choices are 'yes' or 'screaming'. I love the screamers!"
        0:: "Yes"
        1:: "Scream"
        2:: "Fight <Actor.him_or_her>"
        
        If 0
            perversion += 1
            Sex(Player, Actor)
            If HungerEnable > 0
                Player.modifyActorVar(rk_VB_Hunger, -2)
            EndIf
        Elseif 1
            "I screamed out for help!"
            If Random(0, 100) < 80
                "Unfortunately no-one came to help ..."
                If Actor.isFemale() && Player.isMale()
                    Filter(AggressiveFM)
                Else
                    Filter(Aggressive)
                Endif
                TalkNonConcensual()
                SexNoAffair(Actor, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
                perversion -= 15
                arousal -= 15
            Else
                "Thankfully, the gym staff came in and saved the day."
                mood += 5
            Endif
        Else
            "I tried to fight my way out!"
            If Random(0, 100) > martial
                "Unfortunately <Actor.he_or_she> was too strong for me ..."
                If Actor.isFemale() && Player.isMale()
                    Filter(AggressiveFM)
                Else
                    Filter(Aggressive)
                Endif
                mood -= 25
                TalkNonConcensual()
                SexNoAffair(Actor, Player)
                If HungerEnable > 0
                    Player.modifyActorVar(rk_VB_Hunger, -3)
                EndIf
            Else
                "Thankfully, I managed to fight <Actor.him_or_her> off."
                mood += 5
            Endif        
        Endif 
        energy -= 15
    SceneEnd()
    timeout(500, cms_ica_sep04) // Improved by Ravenger6660 - Raddeck